erc20_vault::complete_withdraw: instruction 140 (persistent_hash): wire Identifier("%pi.122") feeds bytes<32> (segment 2) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
erc20_vault::complete_withdraw: instruction 140 (persistent_hash): wire Identifier("%pi.123") feeds bytes<32> (segment 2) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
erc20_vault::refund: instruction 128 (persistent_hash): wire Identifier("%pi.72") feeds bytes<32> (segment 1) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
erc20_vault::refund: instruction 139 (persistent_hash): wire Identifier("%pi.121") feeds bytes<32> (segment 2) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
erc20_vault::refund: instruction 139 (persistent_hash): wire Identifier("%pi.122") feeds bytes<32> (segment 2) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
erc20_vault::refund: instruction 266 (persistent_hash): wire Identifier("%add.208") feeds bytes<32> (segment 1) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
erc20_vault::refund: instruction 277 (persistent_hash): wire Identifier("%pi.217") feeds bytes<32> (segment 2) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
erc20_vault::refund: instruction 277 (persistent_hash): wire Identifier("%pi.218") feeds bytes<32> (segment 2) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
erc20_vault::complete_swap: instruction 225 (persistent_hash): wire Identifier("%add.157") feeds bytes<16> (segment 3) limb 0 (16 byte(s)) but is not provably bounded — required ≤ 128 bits; the packing is only injective for in-range values (api-safety-survey §B3)
erc20_vault_opt::complete_withdraw: instruction 140 (persistent_hash): wire Identifier("%pi.118") feeds bytes<32> (segment 2) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
erc20_vault_opt::complete_withdraw: instruction 140 (persistent_hash): wire Identifier("%pi.119") feeds bytes<32> (segment 2) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
erc20_vault_opt::refund: instruction 229 (persistent_hash): wire Identifier("%sel.187") feeds bytes<32> (segment 1) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
erc20_vault_opt::complete_swap: instruction 218 (persistent_hash): wire Identifier("%add.160") feeds bytes<32> (segment 1) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
erc20_vault_borsh::complete_withdraw: instruction 144 (persistent_hash): wire Identifier("%pi.119") feeds bytes<32> (segment 2) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
erc20_vault_borsh::complete_withdraw: instruction 144 (persistent_hash): wire Identifier("%pi.120") feeds bytes<32> (segment 2) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
erc20_vault_borsh::refund: instruction 232 (persistent_hash): wire Identifier("%sel.188") feeds bytes<32> (segment 1) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
erc20_vault_borsh::complete_swap: instruction 223 (persistent_hash): wire Identifier("%add.162") feeds bytes<32> (segment 1) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
erc20_vault_modern::complete_withdraw: instruction 144 (persistent_hash): wire Identifier("%pi.118") feeds bytes<32> (segment 2) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
erc20_vault_modern::complete_withdraw: instruction 144 (persistent_hash): wire Identifier("%pi.119") feeds bytes<32> (segment 2) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
erc20_vault_modern::refund: instruction 232 (persistent_hash): wire Identifier("%sel.184") feeds bytes<32> (segment 1) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
erc20_vault_modern::complete_swap: instruction 223 (persistent_hash): wire Identifier("%add.161") feeds bytes<32> (segment 1) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
kernel_tokens::s_send_shielded: instruction 61 (persistent_hash): wire Identifier("%add.20") feeds bytes<16> (segment 3) limb 0 (16 byte(s)) but is not provably bounded — required ≤ 128 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::deposit_shielded: instruction 52 (persistent_hash): wire Identifier("%pi.19") feeds bytes<32> (segment 1) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::deposit_shielded: instruction 52 (persistent_hash): wire Identifier("%pi.20") feeds bytes<32> (segment 1) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::deposit_shielded: instruction 52 (persistent_hash): wire Identifier("%pi.21") feeds bytes<32> (segment 2) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::deposit_shielded: instruction 52 (persistent_hash): wire Identifier("%pi.22") feeds bytes<32> (segment 2) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::deposit_shielded: instruction 52 (persistent_hash): wire Identifier("%pi.23") feeds bytes<16> (segment 3) limb 0 (16 byte(s)) but is not provably bounded — required ≤ 128 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::deposit_shielded: instruction 52 (persistent_hash): wire Identifier("%pi.25") feeds bytes<32> (segment 5) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::deposit_shielded: instruction 52 (persistent_hash): wire Identifier("%pi.26") feeds bytes<32> (segment 5) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::deposit_shielded: instruction 60 (persistent_hash): wire Identifier("%pi.25") feeds bytes<32> (segment 5) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::deposit_shielded: instruction 60 (persistent_hash): wire Identifier("%pi.26") feeds bytes<32> (segment 5) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::deposit_shielded: instruction 80 (persistent_hash): wire Identifier("%pi.21") feeds bytes<32> (segment 2) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::deposit_shielded: instruction 80 (persistent_hash): wire Identifier("%pi.22") feeds bytes<32> (segment 2) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::deposit_shielded: instruction 80 (persistent_hash): wire Identifier("%pi.25") feeds bytes<32> (segment 5) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::deposit_shielded: instruction 80 (persistent_hash): wire Identifier("%pi.26") feeds bytes<32> (segment 5) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::deposit_shielded: instruction 101 (persistent_hash): wire Identifier("%pi.21") feeds bytes<32> (segment 2) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::deposit_shielded: instruction 101 (persistent_hash): wire Identifier("%pi.22") feeds bytes<32> (segment 2) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::deposit_shielded: instruction 101 (persistent_hash): wire Identifier("%sel.51") feeds bytes<32> (segment 5) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::deposit_shielded: instruction 101 (persistent_hash): wire Identifier("%sel.52") feeds bytes<32> (segment 5) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::deposit_shielded: instruction 121 (persistent_hash): wire Identifier("%sel.60") feeds bytes<32> (segment 5) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::deposit_shielded: instruction 121 (persistent_hash): wire Identifier("%sel.61") feeds bytes<32> (segment 5) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 237 (keccak256): wire Identifier("%pi.180") feeds bytes<32> (segment 4) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 237 (keccak256): wire Identifier("%pi.181") feeds bytes<32> (segment 4) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 761 (persistent_hash): wire Identifier("%pi.673") feeds bytes<32> (segment 1) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 761 (persistent_hash): wire Identifier("%pi.674") feeds bytes<32> (segment 1) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 761 (persistent_hash): wire Identifier("%pi.675") feeds bytes<32> (segment 2) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 761 (persistent_hash): wire Identifier("%pi.676") feeds bytes<32> (segment 2) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 761 (persistent_hash): wire Identifier("%pi.677") feeds bytes<16> (segment 3) limb 0 (16 byte(s)) but is not provably bounded — required ≤ 128 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 761 (persistent_hash): wire Identifier("%pi.694") feeds bytes<32> (segment 5) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 761 (persistent_hash): wire Identifier("%pi.695") feeds bytes<32> (segment 5) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 780 (persistent_hash): wire Identifier("%pi.675") feeds bytes<32> (segment 2) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 780 (persistent_hash): wire Identifier("%pi.676") feeds bytes<32> (segment 2) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 792 (persistent_hash): wire Identifier("%pi.675") feeds bytes<32> (segment 2) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 792 (persistent_hash): wire Identifier("%pi.676") feeds bytes<32> (segment 2) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 811 (persistent_hash): wire Identifier("%pi.675") feeds bytes<32> (segment 2) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 811 (persistent_hash): wire Identifier("%pi.676") feeds bytes<32> (segment 2) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 811 (persistent_hash): wire Identifier("%add.703") feeds bytes<16> (segment 3) limb 0 (16 byte(s)) but is not provably bounded — required ≤ 128 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 811 (persistent_hash): wire Identifier("%pi.694") feeds bytes<32> (segment 5) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 811 (persistent_hash): wire Identifier("%pi.695") feeds bytes<32> (segment 5) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 839 (persistent_hash): wire Identifier("%sel.740") feeds bytes<32> (segment 2) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 839 (persistent_hash): wire Identifier("%sel.741") feeds bytes<32> (segment 2) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 839 (persistent_hash): wire Identifier("%sel.742") feeds bytes<16> (segment 3) limb 0 (16 byte(s)) but is not provably bounded — required ≤ 128 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 839 (persistent_hash): wire Identifier("%sel.747") feeds bytes<32> (segment 5) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 839 (persistent_hash): wire Identifier("%sel.748") feeds bytes<32> (segment 5) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 864 (persistent_hash): wire Identifier("%pi.673") feeds bytes<32> (segment 1) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 864 (persistent_hash): wire Identifier("%pi.674") feeds bytes<32> (segment 1) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 864 (persistent_hash): wire Identifier("%pi.675") feeds bytes<32> (segment 2) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 864 (persistent_hash): wire Identifier("%pi.676") feeds bytes<32> (segment 2) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 864 (persistent_hash): wire Identifier("%pi.677") feeds bytes<16> (segment 3) limb 0 (16 byte(s)) but is not provably bounded — required ≤ 128 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 864 (persistent_hash): wire Identifier("%pi.756") feeds bytes<32> (segment 5) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 864 (persistent_hash): wire Identifier("%pi.757") feeds bytes<32> (segment 5) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 888 (persistent_hash): wire Identifier("%add.765") feeds bytes<16> (segment 3) limb 0 (16 byte(s)) but is not provably bounded — required ≤ 128 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 888 (persistent_hash): wire Identifier("%pi.756") feeds bytes<32> (segment 5) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 888 (persistent_hash): wire Identifier("%pi.757") feeds bytes<32> (segment 5) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 909 (persistent_hash): wire Identifier("%add.765") feeds bytes<16> (segment 3) limb 0 (16 byte(s)) but is not provably bounded — required ≤ 128 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 909 (persistent_hash): wire Identifier("%sel.782") feeds bytes<32> (segment 5) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 909 (persistent_hash): wire Identifier("%sel.783") feeds bytes<32> (segment 5) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 1051 (persistent_hash): wire Identifier("%sel.838") feeds bytes<32> (segment 5) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 1051 (persistent_hash): wire Identifier("%sel.839") feeds bytes<32> (segment 5) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 1081 (persistent_hash): wire Identifier("%pi.845") feeds bytes<32> (segment 1) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 1081 (persistent_hash): wire Identifier("%pi.846") feeds bytes<32> (segment 1) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 1081 (persistent_hash): wire Identifier("%pi.847") feeds bytes<32> (segment 2) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 1081 (persistent_hash): wire Identifier("%pi.848") feeds bytes<32> (segment 2) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 1081 (persistent_hash): wire Identifier("%pi.849") feeds bytes<16> (segment 3) limb 0 (16 byte(s)) but is not provably bounded — required ≤ 128 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 1081 (persistent_hash): wire Identifier("%pi.851") feeds bytes<32> (segment 5) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 1081 (persistent_hash): wire Identifier("%pi.852") feeds bytes<32> (segment 5) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 1089 (persistent_hash): wire Identifier("%pi.851") feeds bytes<32> (segment 5) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 1089 (persistent_hash): wire Identifier("%pi.852") feeds bytes<32> (segment 5) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 1109 (persistent_hash): wire Identifier("%pi.847") feeds bytes<32> (segment 2) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 1109 (persistent_hash): wire Identifier("%pi.848") feeds bytes<32> (segment 2) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 1109 (persistent_hash): wire Identifier("%pi.851") feeds bytes<32> (segment 5) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 1109 (persistent_hash): wire Identifier("%pi.852") feeds bytes<32> (segment 5) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 1130 (persistent_hash): wire Identifier("%pi.847") feeds bytes<32> (segment 2) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 1130 (persistent_hash): wire Identifier("%pi.848") feeds bytes<32> (segment 2) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 1130 (persistent_hash): wire Identifier("%sel.877") feeds bytes<32> (segment 5) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 1130 (persistent_hash): wire Identifier("%sel.878") feeds bytes<32> (segment 5) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 1151 (persistent_hash): wire Identifier("%sel.887") feeds bytes<32> (segment 5) limb 0 (1 byte(s)) but is not provably bounded — required ≤ 8 bits; the packing is only injective for in-range values (api-safety-survey §B3)
manager::execute: instruction 1151 (persistent_hash): wire Identifier("%sel.888") feeds bytes<32> (segment 5) limb 1 (31 byte(s)) but is not provably bounded — required ≤ 248 bits; the packing is only injective for in-range values (api-safety-survey §B3)
