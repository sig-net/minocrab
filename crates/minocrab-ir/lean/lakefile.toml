name = "minocrab-proofs"
defaultTargets = ["MinocrabProofs"]

[[lean_lib]]
name = "MinocrabProofs"

# M27: the real-IR theorems (DedupIr.lean) are stated over the syntax the
# zkir crate's package defines and round-trips against the corpus.
[[require]]
name = "minocrab-zkir-lean"
path = "../../minocrab-zkir/lean"
