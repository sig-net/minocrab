# The published ABI of `xcall-target-interface`, derived from the
# declarations in src/lib.rs. A DIFF HERE IS A SEMVER DECISION:
# every line below feeds the communications commitment the ledger
# matches, so any change breaks deployed callers (major).
# Regenerate: cargo test -p xcall-target-interface -- --ignored

circuit deposit
  entry-point 976d0459f96ba18cfef2200b2ff4ee9a74c5e506de04150569449fe34a65cfb6
  arguments 3 slots, alignment [bytes 32, bytes 16]
      0  uint<8>      constrain_bits 8
      1  uint<248>    constrain_bits 248
      2  uint<128>    constrain_bits 128
  result 0 slots, alignment []

circuit depositEmit
  entry-point 0c85756f9730c75ff8eedeece9b06bb3a6689cbcb050b27345f6844c3a521ecc
  arguments 3 slots, alignment [bytes 32, bytes 16]
      0  uint<8>      constrain_bits 8
      1  uint<248>    constrain_bits 248
      2  uint<128>    constrain_bits 128
  result 0 slots, alignment []

circuit depositBig
  entry-point cf2948c7dcb4f525776f9833073101ea7161cad2efcadf63e021b253da671173
  arguments 9 slots, alignment [bytes 256]
      0  uint<64>     constrain_bits 64
      1  uint<248>    constrain_bits 248
      2  uint<248>    constrain_bits 248
      3  uint<248>    constrain_bits 248
      4  uint<248>    constrain_bits 248
      5  uint<248>    constrain_bits 248
      6  uint<248>    constrain_bits 248
      7  uint<248>    constrain_bits 248
      8  uint<248>    constrain_bits 248
  result 0 slots, alignment []
