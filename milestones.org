#+title: MinoCrab Milestones

Working method: one milestone at a time, each session starts by reading this
file and any =notes/= file it links. Findings and decisions get written to
=notes/= as they're made, so no session ever re-derives them. Mark tasks DONE
in place.

* M0 — Foundations, corpus, and layer map
Everything here is research and setup; no eDSL code yet. The output is
written-down knowledge that every later milestone reads instead of rediscovering.
- [X] Nix flake + direnv; pin the Compact tooling (compactc etc.) in it.
      (compactc 0.33.0-rc.2 pinned by hash; verified: compiles corpus
      counter.compact → .zkir/.bzkir + keys. Mainnet stable 0.31.1 noted.)
- [X] Gather the corpus: clone sig-net Compact contracts (via gh) and public
      Midnight examples into =corpus/= (or as flake inputs). Compile each with
      the pinned compactc and keep the ZKIR output alongside the source.
      (corpus/: 8 pinned sources, 478 files (+195 negative), 312 compile OK
      incl. ALL 20 sig-net contracts — they need --feature-zkir-v3, wired as
      per-source flags. ./update.sh bumps pins. Failures recorded in
      compile-report.tsv are data, mostly older-pragma/multi-file cases.)
- [X] Survey Midnight's repos: what ZKIR crates/types/serializers exist that we
      can import or fork, which ZKIR version supports cross-contract calling,
      and how compactc drives the pipeline. Write to =notes/zkir.org= and
      =notes/midnight-code-reuse.org=.
- [X] Write the layer map (per plan.org: ZKIR bindings / rust IR + stdlib /
      optimisations / …) to =notes/architecture.org= before any implementation.
Exit criteria: corpus compiles reproducibly under nix; architecture doc exists;
we know exactly which Midnight code we're reusing and how.

* M1 — ZKIR bindings layer
- [X] Rust types + (de)serialization for ZKIR, reusing/forking Midnight's code
      per M0 findings. Never below ZKIR.
      (crates/minocrab-zkir: re-exports midnight-zkir 2.2.0 pinned to the same
      midnight-ledger tag as the toolchain binaries; adds the .zkir JSON
      version-envelope writer the crate lacks. See Cargo.toml [patch] notes —
      the ledger-9 line needed their transient-crypto-old workspace patch
      mirrored.)
- [X] Round-trip test: parse compactc's ZKIR output, re-emit it, verify
      equivalence. (Green on the counter fixture.)
- [X] Extend the round-trip test to sweep every .zkir in corpus/ once the
      corpus build lands. (722 v2 files round-trip clean.)
- [X] ZKIR v3 bindings: the sig-net contracts (incl. the erc20-vault
      benchmark target, now M6)
      compile with --feature-zkir-v3 and emit v3 (typed IR, inputs/outputs
      schema). Bind =midnight-zkir-v3= (git; rev the compact flake pins) the
      same way as v2, round-trip the 66 skipped v3 corpus files.
      (minocrab_zkir::v3 module + read_any dispatch. Gotcha for bumps: the
      ledger rc.3 tag's zkir-v3 predates the bundled binary despite identical
      version strings — all midnight-ledger deps now pin the flake's rev
      04c9c5d9. All 788 corpus files round-trip, 722 v2 + 66 v3.)
- [X] Emit a trivial circuit by hand through our bindings and check Midnight's
      tooling accepts it. (zkir mock-compile accepts; parses back equal.)
Exit criteria: we can read and write ZKIR that their toolchain accepts, proven
against the corpus.

* M2 — Core eDSL + native simulator
- [X] Minimal eDSL: wires, field/int ops, assertions, witness inputs, public
      outputs; lowering to ZKIR via M1.
      (crates/minocrab-ir: typed builder over all 26 ZKIR v2 instructions with
      arity tracking. crates/minocrab: Circuit/Wire API.)
- [X] Type-level disclosure tracking: =Wire<Private>= can't reach a public
      output without =disclose()=; overridable escape hatch; greppable.
      (Meet trait joins visibilities; declare_public/output only accept
      Wire<Public>; disclose(w, label) is the single gate and carries an
      audit label into the report. TODO M3: trybuild compile-fail test
      proving the type error.)
- [X] Native simulator running circuits in plain Rust inside =cargo test=, with
      structured output: performance metrics + what was disclosed.
      (crates/minocrab-sim mirrors midnight-ledger's reference VM;
      every run convertible to a ProofPreimage and cross-checked against
      IrSource::check in tests, so the simulator is never trusted alone.
      Report = op counts + witnesses consumed + labelled disclosed values,
      serde-serializable. cost() exposes their CircuitModel k/rows.)
Exit criteria: a small contract written in the eDSL lowers to valid ZKIR and
simulates with a disclosure report. MET — age-gate demo lowers, zkir
mock-compile accepts it, reference VM agrees with the simulator
(crates/minocrab-sim/tests/end_to_end.rs).

* M3 — Verification harness + cost profiler
- [X] Spec-vs-circuit quickcheck/proptest harness: spec in ordinary Rust,
      millions of random inputs asserting circuit ≡ intent.
      (crates/minocrab-sim/tests/property.rs; every accepted case re-validated
      by the reference VM. PROPTEST_CASES env scales to millions in CI.
      Disclosure typing proven by compile_fail doctest in minocrab.)
- [ ] Differential testing against compactc: same contract in Compact and in
      the eDSL, same inputs, same results. (Needs the first eDSL ports —
      starts with M4; compactc's ledger-state ABI must be modeled first.)
      (ABI modeled: notes/ledger-abi.org — state layout, Impact/PI framing,
      FAB, v3 schema, worked example, and the equivalence criterion: same
      typed inputs/outputs + identical Preprocessed::pis/pi_skips on a
      shared ProofPreimage; instruction streams free to differ.)
- [X] User-friendly cost profiler (constraint/RAM/proving-cost proxies, broken
      down by code region).
      (Circuit::region() spans; minocrab_sim::profile() → per-region
      instruction/op breakdown + Midnight's CircuitModel k/rows; Display
      renders a human table, serde for CI.)
Exit criteria: the "obviously correct" story exists and is cheap to run in CI.

* M4 — stdlib + corpus rewrites
- [ ] Port the Compact stdlib pieces the corpus needs, mechanically translating
      Midnight's own implementations where possible.
      (Mostly DONE — crates/minocrab-std: Bundle wire-typing (+FAB atoms),
      Maybe/Either, full merkleTreePathRoot, persistent/transient
      hash+commit, degrade/upgrade, pad/literals, coin types +
      coinCommitment/coinNullifier/tokenType/nativeToken/evolveNonce,
      jubjubSchnorrVerify; v3 module: secp256k1EcdsaVerify +
      secp256k1EthereumAddress over the typed frontend, tested against
      real signatures. First compactc differential green:
      tiny.compact public_key vs corpus set.zkir, digests equal.
      REMAINING: the kernel.* circuits (mint/send/receive, blockTime*,
      unshieldedBalance*) — blocked on the ledger-op layer below.)
- [X] Prerequisite: ZKIR v3 emission + execution (sig-net contracts are v3).
      (minocrab-ir::v3::Builder3 — typed, zkir-v3 mock-compile accepts;
      minocrab::v3::Circuit3 — Wire3<T, Vis>, capability-trait ops, the
      type lists as Rust compile errors; minocrab-sim::v3 — preprocess()
      port driving upstream's own pub ir_instructions helpers, full
      pis/pi_skips/comm_comm, 10 cross-checked tests. Needed because
      upstream's Preprocessed path is pub(crate) — notes/ledger-abi.org §6.)
- [ ] Ledger-op layer (crates/minocrab-ledger): Impact ops via onchain-vm's
      Op::field_repr; constant ops through the real Op type, value-carrying
      ops splice wires into field_repr's layout (unit-tested equal).
      WRITES DONE + accepted: baseline noop/base rebuilt and call-compatible
      (equal schemas + pis/pi_skips; tests/differential_baseline.rs).
      READS DONE + accepted: cell/counter read, counter lessThan, map
      member/lookup/size/isEmpty, kernel self — public_input gates +
      popeq[c] embeds per compactc's vm-code; accepted via the initialise/
      initialize differentials below. KERNEL EFFECTS DONE + accepted:
      mintShielded + the three zswap claims (mint-tokens differentials).
      EVENTS DONE + accepted: emit/log op + Serializer (events experiment,
      all 4 circuits). REMAINING: Map rem, MerkleTree ops,
      guarded/conditional reads, and the ledger-declaration DSL on top.
      Priority per notes/signet-corpus.org (erc20-vault inventory).
- [ ] Rewrite the sig-net corpus contracts in the eDSL, one by one; each becomes
      a differential + property test. (ABI modeled: notes/ledger-abi.org.
      Equivalence = same typed I/O schema + equal Preprocessed pis/pi_skips
      on a shared ProofPreimage; instruction shape free to differ.
      STARTED — crates/minocrab-contracts. Every non-cross-contract
      experiment in signet-midnight-experiments is COMPLETE and
      differential-green: baseline (noop/base), attest (all 4, incl.
      shaVerify/keccakVerify via the deserialize<T,N> port), events
      (base/emit1/2/4), hashing (all 10), keccak (all 9), mint-tokens
      (both), serde-builtin (checkRoundtrip). Plus test-caller-contract
      initialise and erc20-vault initialize (the benchmark target's
      setup circuit), each with reject-agreement tests. M5 done: the
      xcall/xcontract-events/xcall-with-payment experiments are ALL
      differential-green — notify/pay landed 2026-08-14 (M6 session)
      via the receiveShielded + writeCoin ports (createZswapOutput is a
      Void witness native, nothing in-circuit; minocrab-ledger
      cell_write_coin; compositions in minocrab-contracts::common).
      REMAINING rewrites: Signet.compact, the rest of test-caller and
      of erc20-vault (deposit/claim/withdraw… also need the event-map
      composite + guarded reads).
      M6 session 2026-08-14: ALL NINE erc20-vault circuits
      differential-green (initialize, deposit, claim, approveRouter,
      withdraw, completeWithdraw, refund, swap, completeSwap) — every
      circuit with guard-rejection agreement and (deposit/claim/
      withdraw/completeWithdraw/refund/completeSwap) full tamper
      sweeps, multi-scenario where circuits branch (claim's four
      recipient shapes, completeWithdraw success/refund, refund's two
      routes, completeSwap exact-spend). Built on: the Signet-module
      port (minocrab-contracts::signet), map_remove + guarded
      reads/witnesses/asserts, the coin ports (receiveShielded, burn
      via sendImmediateShielded's folded form, coinNullifier,
      nonce evolution, dynamic + static-left + guarded mints).
      Signet singleton DONE 2026-08-14: its on-chain circuits turn out
      to live in signet-contract.compact (Signet.compact is the module,
      already ported; circuits.compact is --skip-zk, no zkir) — all 3
      emit-only circuits (signBidirectional/respond/respondBidirectional)
      differential-green with tamper rejection agreement
      (minocrab-contracts::signet_contract). See notes/signet-corpus.org.
      With that, every M6-scope circuit is ported: REMAINING here is
      nothing — the benchmark harness is M6's own bullet.)
Exit criteria: the sig-net contracts run on MinoCrab with the M3 harness green.

* M5 — Cross-contract calling
Pulled ahead of the benchmark (dmd, 2026-08-14): the erc20-vault runtime
circuits call the Signet singleton cross-contract, so this is a benchmark
prerequisite.
- [X] Cross-contract calls in the eDSL on the ZKIR version that supports them
      (v3; caller-side commitment + Kernel.claimContractCall, see
      notes/ledger-abi.org §2 on the communications commitment and
      desugar-contract-calls.ss).
      (minocrab-ledger: kernel_claim_contract_call + contract_call — the
      full desugar: result-limb witnesses, cc-rand/ep witnesses, in-circuit
      transientCommit, effects[3] claim — plus set_insert. Lowering decoded
      in notes/ledger-abi.org §Implementation "CROSS-CONTRACT CALLS".)
Exit criteria: the xcall/xcontract-events corpus experiments run on MinoCrab
with their differentials green. MET — all differential-green incl. tamper
reject-agreement sweeps (minocrab-contracts tests/xcall_differential.rs,
xcontract_events_differential.rs, xcall_with_payment_differential.rs):
xcall caller localBase/callOnce/callTwice/callBig/callEmit + target
deposit/depositEmit/depositBig; xcontract-events depositViaVault (the
return-value path: result witnesses bound by the comm commitment) + token
deposit; xcall-with-payment caller callOnce/request + target confirmRequest.
NOT here: xcall-with-payment's notify/pay — root-call coin-custody circuits
(receiveShielded), no cross-contract machinery; they belonged to the M4
stdlib coin work and landed 2026-08-14 (M6 session, also green).

* M6 — Baseline benchmark: cross-chain deposit/withdrawal
Benchmark the sig-net cross-chain deposit and withdrawal contracts under
MinoCrab and under Compact. This pass is the BASELINE: our lowering mirrors
compactc's op sequences, so expect parity on constraint/cost numbers —
parity here is evidence the rewrite is correctness-preserving, and it gives
M7's optimisation work its targets. The presentable "better than Compact"
write-up comes after M7, from a refreshed run of this harness.
Confirmed target (dmd, 2026-08-13): the erc20-vault contract
(sig-net/midnight-examples, examples/erc20-vault) — plus its Signet singleton
dependency (sig-net/midnight-integration) as needed for the cross-contract
calls. See notes/signet-corpus.org.
- [X] Both contracts running under both toolchains at the same pinned versions,
      with the M3 correctness harness green on the MinoCrab versions.
      (bench.sh re-runs the erc20-vault + signet-contract differentials
      with preimage dumping before measuring, so every benchmarked
      preimage is corpus-verified first.)
- [X] Head-to-head measurements: RAM, proving time, and constraint/cost-profiler
      breakdowns, reproducible via a single nix-run command.
      (nix run .#bench → target/bench/{results.json,report.md,profiles/};
      median-of-3 proving, per-cell subprocess RSS. Final run 2026-08-14.)
- [X] Baseline write-up: methodology + numbers, and the cost-profile breakdown
      that picks M7's optimisation targets.
      (notes/benchmark.org: full table + findings + the ranked list from
      the region-attributed profiles — regions landed in
      minocrab-contracts with this run; minocrab-bench --profiles
      refreshes attribution without re-proving.)
Exit criteria: reproducible baseline numbers from a clean checkout, and a
ranked list of where optimisation should attack. MET — parity confirmed on
the mirrored circuits (rows ±0.7%), strictly better where compactc
over-serializes (refund -12%, completeSwap -15%, singleton -53..-55% rows;
5 circuits a full k lower → ~half prove time/RAM), and the M7 attack list
is ranked by region-attributed instruction share (notes/benchmark.org).

* M7 — Optimisation + presentable deliverable
Driven by M6's cost profiles; every change re-measured with the M6 harness.
- [X] Const-generic circuit families, monomorphized per size/depth, unrolled by
      rustc.
      (DONE 2026-08-15 in three type-level stages: BytesN<V, N> +
      Serializer::finish::<N> + bytes_limbs() as the one limbing rule
      (minocrab-std/src/v3.rs); the Signet event family
      SignBidirectionalEvent<V, WORDS, LEN_OUT, LEN_RESPOND> +
      EvmCalldata<V, WORDS> + EventRecord with a derived signet::layout
      replacing the hand-counted event_limb table, retyped in the vault
      as VaultEvent<V, 2, 34, 34> / SwapEvent<V, 7, 38, 37> and
      verify_attestation::<LEN_OUTPUT>; MerkleTreePath<V, T, DEPTH>
      (a real [MerkleTreePathEntry; DEPTH], so it is finally a Bundle)
      plus Bundle::WIDTH literals derived from their components.
      Guard: tests/row_snapshot.rs freezes (k, rows) for all 55
      workspace circuits — written first, bit-identical through all
      three stages, differential suites green throughout. Skip
      decisions with reasons (hashing's BytesNDyn, emit_n/call_n_times,
      Serializer typestate, array-backed limbs, B32, ledger values) and
      the dropped serde-unification, in notes/const-generics.org.
      The snapshot doubles as an M8 asset: a toolchain bump that
      changes lowering shows up as a moved row count per circuit.)
- [X] Optimisation passes at the IR layer; measure with the M3 profiler against
      compactc's output (RAM + proving-cost proxies).
      (Pass 1 DONE 2026-08-15 — native-instruction byte plumbing: ZKIR's
      ReverseBytes (~150 rows) replaces the mirrored explode/rebuild
      chains (~4,600); ABI words become algebraic div_mod shifts; the
      notification payload collapses to one add (limb-aligned). All 9
      vault circuits -18..-38% rows; every k=17 circuit crossed to k=16,
      approveRouter 15→14; prove time -49..-76% vs compactc, RSS -43..-51%
      on the k-crossers. All 13 differential suites green unchanged.
      Row-cost method + numbers: notes/benchmark.org "M7 pass 1",
      crates/minocrab-sim/examples/opcost.rs.
      Pass 2 DONE 2026-08-15 — segment-based Serializer re-limbing
      (≤1 div_mod per output-limb straddle instead of full byte
      explosion): singleton circuits -97.5..-97.6% rows vs compactc,
      respond k 15→10 (1,004 rows, 0.11s prove / 44MB vs their
      8.24s / 875MB). All suites green. Remaining vault cost is
      PI-coupled or real crypto — lowering-level work here is done;
      what's left of M7 is const-generic families + the write-up.
      Guard: refund/completeSwap sit ~300-450 rows under 2^16.)
- [X] Refresh the M6 benchmark and produce the presentable write-up
      (methodology, numbers, where the wins come from; honest about any
      losses — baseline parity vs optimised deltas cleanly attributed).
      (BENCHMARK.md at the repo root, from a fresh single-session
      24-cell run 2026-08-15: rows -18..-38% vault / -97.5% singleton,
      8 of 12 circuits at least one k lower, prove -46..-51% on the
      vault k-crossers and -95..-97% singleton, honest same-k rows
      (deposit/withdraw ~0% prove despite -19..-35% rows — cost is the
      padded 2^k) and initialize parity. notes/benchmark.org holds the
      working history; baseline cells in results.jsonl.m6-baseline.)
Exit criteria: a benchmark report you can present, reproducible from a clean
checkout, with measurable wins over compactc and the correctness harness
still green.

* M8 — Churn resilience
- [ ] Documented, low-effort ZKIR/Compact-tooling version-bump workflow
      (update pin → run corpus + harness → fix drift).
Exit criteria: a version bump is a routine chore, not a project.

* M9 — Contract-API ergonomics
Design of record: notes/contract-api.org (agreed with dmd 2026-08-14).
Circuit entry points as plain typed Rust fns (NEAR/dfinity-style
decorator over a non-macro CircuitArg/entry core), typed disclosure
declarations with generated tests, then rusty maps + literals. Standing
principle: real types over macro rewriting — macros only generate
impls/shims writable by hand. Zero circuit overhead throughout; every
phase gated on interface snapshot + row_snapshot + all differential
suites.
- [X] Phase 0: interface-snapshot test — ordered (label, type) of
      inputs/outputs/witnesses for all 55 circuits, frozen at HEAD.
- [X] Phase 1: typed v3 leaves Uint<BITS, V=Private> / Bool / Bytes<N≤31>
      with .field() unwrap; native-Rust values accepted where possible.
- [X] Phase 2: CircuitArg (schema+declare+constrain in one trait),
      ArgPath naming, CircuitArgs, entry(), CircuitOut (Public-only ⇒
      returning is disclosing); hand impls; port sign_bidirectional +
      deposit with zero movement.
- [X] Phase 3: crates/minocrab-macros — #[derive(CircuitArg)]; port
      withdraw + claim (Maybe<Either<..>> flattening).
- [X] Phase 4: #[circuit] attribute (thinness rule: expansion contains
      no Circuit3 call); re-port the phase-2/3 circuits.
- [X] Phase 5: mechanical port of the remaining circuits (order/types
      never move; name column only where mechanical naming lengthens
      hand-abbreviated labels).
      (2026-08-15 — 62 of the 78 snapshot rows now build through the
      typed API, up from 10: the vault's remaining six circuits in all
      THREE forks (18, edited in lockstep because the borsh fork owes
      the opt fork byte-identity on five of them), signet_contract's
      respond pair through the interface crate's own types one level
      flatter than its wrapper struct, and every experiment but one
      family. One new core leaf, Secp256k1Point — the corpus's only
      curve-point argument (7 circuits), one slot whose wire is NOT a
      field element, so push_slots hands back nothing and the provided
      constrain filters Prim::Point out before zipping; five FAB atoms,
      pinned by a serialized-ZKIR test with the point mid-list. New
      derives: SwapRequest ×3, DepositArgs (events/xcall), CoinArg.
      Circuit FAMILIES parameterized by a Rust value (events' emitN,
      xcall's callTwice) use entry() directly, since the attribute makes
      a nullary constructor. LEFT UNPORTED, documented at the code:
      hashing's 16 Bytes<len> circuits, whose width is a runtime sweep
      parameter and whose constraints BytesNDyn already derives from the
      same len — a closed match over today's sizes would buy nothing and
      lose the sweep. Movement: exactly five name-column lines
      (xcall_with_payment::callOnce's coin_ prefix, which its own target
      already used). Zero-movement checked harder than the gates
      require: all 78 circuits' serialized ZKIR dumped at HEAD and
      compared — 68 byte-identical, 9 settle circuits identical up to
      the ORDER of three input constraints (parameter order now, not the
      declarer's), 1 the documented rename. row_snapshot bit-identical,
      cargo test --workspace --release green.)
- [ ] Phase 6: disclosure — v3 Disclosure carries the Identifier, v3
      report(), label!/disclose_as::<L>/Discloses<D, R> +
      Discloses::of, macro-generated set-equality tests with
      prescriptive failure messages + valued-report golden.
- [ ] Phase 7: rusty maps (#[derive(Ledger)] + LedgerMap<K,V>, Compact
      method names, no hidden Impact ops), inline-immediate literals
      (impl Into<Operand>; gated on hand-reviewed instruction deltas —
      row snapshot is blind to Copy removal), and assertion predicates
      (deferred #[must_use] descriptors: c.assert(less_than(0u64,
      amount)) — widths from the operand types, optional message,
      not/and/or + eval() only, no deferred arithmetic; shares the
      Into<Operand> machinery with literals; no macros).
- [ ] Phase 8: showcase twin — a "modern" erc20-vault rewrite using
      every M9 feature to fullest effect, kept BESIDE the zero-movement
      direct ports; corpus-verified by the same differential harness
      (instruction streams are free under PI-equality) + property
      tests, own snapshot entries with deltas reported; README's
      full-featured snippet then points at real code.
Exit criteria: the benchmark contracts read side-by-side with their
Compact sources, disclosure declarations are part of every circuit's
signature and enforced by generated tests, and every instrument (rows,
interfaces, differentials) shows zero unexplained movement — plus the
showcase twin differential-green with its deltas explained.

* M10 — Final: ERC-20 vault, maximally verified and maximally optimized
(dmd, 2026-08-15: "test the shit out of the ERC-20 contract and do
everything in your power to optimise it, then output a report of how
fast the super optimized version is.")
The M7 profiles say the remaining vault cost is PI-COUPLED — pinned by
byte-identical-to-compactc framing, not by the protocol. M10 drops the
compactc-framing constraint where the protocol permits, and re-anchors
correctness on the M3 harness at scale. The direct ports stay intact as
the compatibility reference; the optimized vault is its own artifact
(natural base: M9's showcase twin).
- [X] Verification first — the license to optimize: spec-based property
      harness for all 9 vault circuits (specs in ordinary Rust, every
      branch — claim's four recipient shapes, completeWithdraw's two
      routes, refund's two, completeSwap exact-spend — every guard,
      accepted runs re-validated by the reference VM; PROPTEST_CASES
      scaled to millions in CI), plus adversarial/boundary sweeps
      (2^128-1 amounts, zero addresses, malformed witnesses, tampered
      records) and the existing tamper/reject-agreement suites.
      (PARTIAL 2026-08-15 — harness BUILT and green; see
      notes/vault-optimization.org §"As built — step 1". All nine specs,
      the run_program executor (the ledger-semantics link that existed
      nowhere before), the branch-aware strategies, the shared tamper
      machinery, witness-malleability / wrong-branch / injectivity sweeps
      and completeSwap's named underflow boundary. Three suites green:
      differential 30, spec 9, adversarial 19. Found: the notes' own
      completeWithdraw generation strategy was wrong (a non-canonical
      attested byte is ACCEPTED and refunds, both artifacts agree), and
      the reference model shared the same bug; initialize accepts an
      IDENTITY mpcResponseKey, which authenticates every attestation;
      no counter-overflow guard. DONE 2026-08-15: the millions-of-cases
      gating run EXECUTED — PROPTEST_CASES=1000000 × 9 specs + adversarial,
      9 passed 0 failed, 3,121.83s (~52 min, exit 0), 9,000,000 spec
      cases on the direct ports, harness clean. Re-run as the optimized
      artifact's gate + before the final report.)
- [X] Scope the freed space: what the protocol actually pins (Impact op
      stream ↔ on-chain state machine, cross-contract commitment to the
      Signet singleton, MPC attestation formats, coin commitments) vs
      what only compactc-artifact PI-equality pinned (constrain_bits
      placement/redundancy, transcript framing details, serialization
      shapes). Written analysis before any change.
      (DONE 2026-08-15 — notes/vault-optimization.org. PREMISE
      CORRECTED: the vault is hash-bound, not framing-bound (95-99% of
      rows are SHA/keccak/ECDSA; framing is 1-4%). The freed space is
      the vault's OWN hash constructions (userCommitment, refund
      commitments, domain separators — SPEC-discretionary), refund's
      duplicated branch, changeNonce, state layout. k crossings in
      reach: initialize 13→~10, deposit 15→14; withdraw/swap 16→15
      only via the UNSURE burn restructure. Settle circuits are
      floored above 2^15 by protocol crypto — cuts there are cosmetic;
      "maximally optimized" = minimal k. Do NOT dedup constrain_bits
      (soundness surface; comm doesn't imply canonicity — verified).
      Three UNSUREs to resolve before the risky avenues. Sequencing:
      M10 starts from the direct ports NOW, in parallel with M9 5-8.)
- [X] Optimize everything the analysis frees, re-measured per change:
      k-boundary attacks first (deposit 17,502 is ~1.1k rows over k=14;
      protect nothing — this artifact has no compactc row parity to
      keep), constrain-bits dedup, framing/serialization, then the
      crypto regions if headroom remains. Every change lands with the
      harness green at full scale. (instruments landed: cryptocost
      example, row-attributed profiles, bench third side)
      (IN PROGRESS 2026-08-15 — the artifact is forked and the three
      low-risk rungs have landed; see notes/vault-optimization.org
      §"As built — steps 4 + 5(i-iii)". erc20_vault_opt is a
      byte-identical fork of the nine ports (step 4), gated by a
      DIVERGENCE LEDGER that says per circuit whether compactc's
      PI-equality still covers it or the spec harness has taken over,
      asserted in both directions. Rungs: (i) avenue 7's kernel.self
      dedup, (ii) avenue 5's derived changeNonce, (iii) avenue 2's
      injective domain separator. Measured, port → opt: initialize 0% ·
      deposit −0.07% · approveRouter −0.09% · swap −7.4% · claim /
      completeWithdraw −7.9% · withdraw −9.0% · refund −11.5% ·
      completeSwap −17.3%; −33,883 rows overall (−9.6%). NO k MOVES, so
      no prove-time or RAM change yet — as the analysis predicted for
      the ECDSA/keccak-floored circuits. Two projections corrected:
      avenue 7's kernel.self component is 12 rows per read (144 total),
      an order of magnitude under the ranked table, and the domain
      separator has 8 uses, not 9. Deferred: avenue 7's config→one-Cell
      merge (it moves the ledger declaration and the reference model's
      state layout). Still to come: refund's branch merge, the
      commitments, and the burn restructure — which needs its
      well-formedness test first. Ladder continues, so NOT ticked.
      (step 5(vi) 2026-08-15 — the FIRST prove-time wins:
      §"As built — step 5(vi)". Task A: the burn well-formedness gate
      PASSES against the pinned ledger (a single claimed shielded spend
      of a user-recipient output, empty receive/nullifier sets, no
      contract-owned input, funded by a user input — new test
      erc20_vault_opt_burn_wellformed.rs, dev-deps midnight-ledger +
      midnight-zswap). Task B (avenue 6): withdraw + swap burn via one
      claimed spend, −11,309 each; WITHDRAW 16→15. Task C (avenue 1):
      one-block userCommitment at initialize/deposit/claim, −1,860 each;
      DEPOSIT 15→14 (no Poseidon needed). Two k crossings — the first of
      M10. Correction: initialize does NOT reach k12 (the SHA chip floors
      it at k13, measured); its cut + claim's are cosmetic for prove
      time. swap stays k16 (needs avenues 3+2). Elevated
      PROPTEST_CASES=20000 green on all five touched circuits. Ladder
      continues — refund merge, refund commitments → Poseidon, and swap's
      remaining avenues are the next stage; NOT ticked.))
      (step 5(iv)+5(v) 2026-08-15 — LOWERING LADDER COMPLETE; see
      notes/vault-optimization.org §"As built — steps 5(iv refund
      merge)+5(v Poseidon)". 5(iv) avenue 4: refund's two branches merged
      into ONE commitment hash + ONE cond_selected mint (guards still gate
      the per-route ledger effects; the authorisation is bit-for-bit the
      port's), opt refund 57,721→44,366 (−13,355), k16. 5(v) avenue 3:
      refund commitments → transientHash (Poseidon), internal/short-lived
      so curve-mutability is harmless (Q3/Q4), all five sites in lockstep,
      −3,560 each. THE SWAP MEASUREMENT: 32,819 rows, 51 ABOVE the 32,768
      k15 boundary — the notes' ~348-row projected margin was measurement
      noise, and the "if it lands 32,8xx, say so and stop" branch fired:
      swap STAYS k16, not chased. Map<_,Field> value-typing (Q5) would
      cross it (drops ~143/site) but was DEFERRED (its differential needs
      new shipping-crate ledger builders + a 292-op corpus twin —
      disproportionate for 51 rows on the final rung; the map value stays
      Bytes<32>, no symbolic-layer change). Final k picture: opt beats
      compactc by ≥1 k on eight of nine circuits (only initialize ties);
      the ladder's own crossings over the port are deposit 15→14 and
      withdraw 16→15. Everything else is protocol/crypto-floored (ECDSA
      or the SHA hash-chip); row cuts there (−35%..−58% vs compactc) are
      cosmetic for prove time. All planned avenues landed or explicitly
      stopped, so TICKED; only the report bullet remains.))
- [X] The report: fresh single-session benchmark — super-optimized vault
      vs compactc vs the direct port (three-way, so ergonomics-neutral
      lowering wins and freed-framing wins are attributed separately);
      RAM + prove + k/rows; honest about what could not move and why.
      (DONE 2026-08-15 — BENCHMARK.md refreshed as the three-way capstone
      from a single-session ./bench.sh run. Opt vs compactc: rows
      −35..−58% on every vault circuit, prove −37..−59% and RAM
      −42..−52% where k crosses. HONEST SPLIT: opt's only NEW prove-time
      wins over the already-fast port are deposit (k15→14) and withdraw
      (k16→15); the other seven cut rows but stay k-floored (ECDSA k16 /
      SHA k13), their big compactc deltas inherited from the port's M7
      k-crossings — opt does not claim the port's wins. swap missed k15
      by 51 rows, left deliberately. opt proves its OWN preimage
      (symbolic-effect equality, 9M-case harness) — weaker warrant than
      the port's PI-equality, stated prominently. HTML at
      target/bench/report.html (uncommitted, gitignored tree).)
Exit criteria: report reproducible from a clean checkout; the property
harness green at millions of cases per circuit; the optimized artifact's
every deviation from compactc framing documented and spec-justified.
MET — M10 complete.

* M11 — Borsh (fixed-width subset) serialization
Design of record: =notes/borsh-format.org=. This IS Borsh, restricted
to the fixed-width subset and documented as A SUBSET, NOT A SEPARATE
FORMAT: every byte emitted is valid canonical Borsh for the declared
types, and any Borsh implementation (borsh-js included) parses it from
the same declarations. The subset rule is that every circuit-visible
type has a fixed width, so Maybe ↦ Flagged (bool tag + always-present
payload), never Option, and no Vec/String/data-enums. serde +
bincode-fixint emits byte-identical output for the subset, so spec
types derive both and conformance is a DUAL oracle. One agent per
stage; usual gates + notes as-built per stage. Stages 0 and 8 have
external value and no dependency on 1-7.
- [X] 0. Instruments + oracle, NO circuit changes: borsh/bincode
      dev-deps; spec types for the DEPLOYED payloads (singleton Misc ×3,
      both records, four attested outputs, digest preimages);
      byte-equality against the deployed bytes.
      (DONE 2026-08-15 — notes/borsh-format.org §"As built — stage 0".
      VERDICT: every deployed payload IS canonical Borsh, byte for byte
      — both request records (and so the request ids: requestId ==
      keccak256(borsh(record))), all four attestation digest preimages,
      and all three singleton Misc payloads, the last verified by
      handing the borsh envelope to the PINNED COMPACTC ARTIFACT, which
      accepts it and rejects every single-bit perturbation including in
      the zero pad. Zero divergences. New suite
      tests/serialization_conformance.rs, 15 tests, green at default and
      at PROPTEST_CASES=20000. Corrections to the design: the dual
      oracle alone is NOT the whole subset checker (Option agrees under
      both encoders and is caught by the fixed-width property instead),
      and a Rust fieldless enum is OUTSIDE the dual-oracle subset
      (bincode-fixint writes a 4-byte variant index) so spec-level tags
      are u8. Records measured: vault 404 bytes, swap 571 — the number
      stage 7's estimate is built on.)
- [X] 1. v3::borsh core, hand impls (CircuitBorsh, leaves,
      alignment_of/limbs_of/to_bytes, constrain_canonical, layout).
      ZKIR byte-equality + P1-P4 per leaf.
      (DONE 2026-08-15 — notes/borsh-format.org §"As built — stage 1".
      minocrab_std::v3::borsh, a public module whose docs open with the
      subset framing. The hash path is FREE, as byte-identical ZKIR
      against a hand-written alignment; the packed bytes ARE
      borsh::to_vec of a plain twin and the chip's digest IS
      keccak256(borsh(v)), both through the simulator. constrain_canonical
      is proven equal to CircuitArg::constrain for every leaf that is
      both, with Tag<K>'s `< K` bound the one documented exception.
      Deviations: the CircuitArg supertrait cannot be spelled (Private
      only) so the extension is the blanket CircuitBorshArg bound; `read`
      waits for stage 2 rather than being stubbed; off-width Uint is a
      missing impl rather than a const assert; Flagged is a type alias of
      v3's Maybe. The conformance suite gained an in_circuit section
      tying the library to the DEPLOYED respond payload.)
- [X] 2. BorshReader both modes + cryptocost-style measured per-leaf costs.
      (DONE 2026-08-15 — notes/borsh-format.org §"As built — stage 2".
      A trait with two implementors rather than one struct with a mode
      flag: the modes differ in the visibility they hand back, which is a
      type. Both parse bytes borsh::to_vec produced, in circuit, into the
      right field values; read(to_bytes(v)) == v in both; every witnessed
      leaf is individually pinned to the buffer; both enforce the padding
      rule. MEASURED (minocrab-sim/examples/borshcost.rs): Split ≈ 143
      rows per field boundary interior to a limb, WitnessCheck EXACTLY
      2w+1 rows per leaf (w bytes) plus a div_mod per straddled limb
      boundary — per-leaf 2.0× to 73× cheaper, 1.9× over a whole 204-byte
      record. Used by no circuit, deliberately.)
- [X] 3. #[derive(CircuitBorsh)] + #[borsh(spec)] cross-check test.
      (DONE 2026-08-15 — notes/borsh-format.org §"As built — stage 3".
      One derive emits the CircuitBorsh impl AND the CircuitArg family,
      the latter through the same impl_arg_traits code path the existing
      derive uses (so a type carries one derive, not both). A struct
      generic in a single Vis3 parameter serializes at every visibility —
      log payloads are Public — with the argument impls carrying a
      where-clause instead of a substitution. Two label namespaces:
      camelCase argument labels, verbatim layout paths. Option/Vec/
      String/maps/enums are span errors naming Flagged, [T; K] + count,
      Bytes<N>, Tag<K>. #[borsh(spec = …)] generates the cross-check
      against borsh's own schema; the stage-0 walker was EXTRACTED into
      minocrab_std::v3::borsh::schema behind an off-by-default feature
      (oracle.rs now re-exports it) rather than duplicated. Gate: the
      same record declared derived and by hand, five circuits each,
      byte-identical ZKIR.)
- [X] 4. erc20_vault_borsh byte-identical fork; Art::Borsh; zero
      movement asserted.
      (DONE 2026-08-15 — notes/borsh-format.org §"As built — stage 4".
      A third artifact forked from the OPTIMIZED vault, not the port, so
      the chain is compactc ≡ port ≡ opt ≡ borsh with a ledger and a
      both-ways gate per link: artifact.rs gained Art::Borsh, ARTS: [Art;3]
      and a SECOND ledger borsh_fork_status, and tests/
      erc20_vault_borsh_fork.rs asserts it. Zero movement: the row and
      interface snapshots gained exactly nine entries each, carrying the
      opt numbers verbatim. Deliberately absent is a "still PI-equal to
      compactc" test — M10 diverged all nine circuits, so what the borsh
      fork inherits is the spec harness, not compactc, and the vacuous
      version is not shipped. The harness caught the one real wiring bug:
      two RefundScenario branches asked `art == Art::Opt` where they meant
      "not the port". BENCH DECISION: the borsh artifact is NOT benched
      until stage 7 — at stage 4 a fourth column would repeat the opt one,
      and stage 5 moves no k.)
- [X] 5. Attested outputs → the flat layout (four settle circuits,
      +2..+6 rows, deviation-log D6 for the 0x02 semantics + kind byte).
      (DONE 2026-08-15 — notes/borsh-format.org §"As built — stage 5",
      whose §"THE FORMAT" table IS the spec: kinds 0 claim / 1
      completeWithdraw / 2 completeSwap / 3 refund at byte 0, outputs
      {kind, success: bool} (2 B), {kind, amount_in: u64} (9 B) and
      {kind} (1 B), digest = keccak256(borsh({request_id, output})) at
      34/41/33 bytes. The 5-byte 0xdeadbeef01 sentinel is GONE — a
      response kind says the same thing in the same byte position. The
      0x02 HAZARD IS CLOSED: completeWithdraw's success is a Borsh bool,
      so a non-boolean attestation is unprovable where the port and the
      optimized fork REFUND on it, and the harness pins that intentional
      three-way divergence in both directions. Cross-circuit replay is
      structurally impossible and has its own adversarial property.
      MEASURED, no k moves: claim +6, completeWithdraw +6, completeSwap
      +9, refund −9 (the shorter preimage pays). One documented departure:
      `tag < K` is not emitted, because each circuit asserts equality with
      its own kind, which is strictly stronger.)
- [X] 6. Event payload twins (events_borsh/xcontract_events_borsh)
      beside the pinned originals.
      (DONE 2026-08-15 — notes/borsh-format.org §"As built — stage 6".
      STRONGER THAN THE TARGET: the twins are byte-identical SERIALIZED
      ZKIR to the originals, instruction for instruction, not merely equal
      payloads — so same rows, same interface, same PI vector, and
      compactc's differential covers them verbatim (asserted both ways:
      ZKIR equality, and the twins run against the corpus artifact on the
      preimage carrying the exact 288-byte Misc). Stage 0 proved the
      deployed payload IS canonical Borsh; this proves the API PRODUCES
      it, on real event shapes, for zero rows. Both settings of the
      padding rule are exercised on one shape: a 56-byte DepositEvent
      padded into Bytes<256> and hashed whole, and a Misc envelope whose
      LEN is exactly 288 and so has no pad at all. constrain_canonical is
      deliberately not re-emitted — every leaf is canonical where it is
      produced, and re-emitting would move rows. depositViaVault is not
      twinned: it serializes nothing.)
- [ ] 7. THE RECORD (gated on dmd Q1/Q6): schemas → kind tag; expect
      swap k16→k15; re-run the 9M gate; refresh BENCHMARK.md.
- [X] 8. spec/borsh-subset.md (grammar, leaf table, reject rules,
      padding rule, Maybe↦Flagged, per-record offset tables GENERATED
      from layout()) + spec/vectors/*.json. The artifact the TS/MPC side
      implements against.
      (DONE 2026-08-15 — notes/borsh-format.org §"As built — stage 8".
      Twelve sections of hand-written prose around ONE GENERATED REGION
      (§9's per-type offset tables, walked out of borsh's own schema by
      tests/serialization/spec_doc.rs) plus five committed vector files.
      THE DOCUMENT CANNOT DRIFT: three tests fail if the committed
      artifact stops being the generator's output — the marked region,
      every vector file byte for byte (and the directory holding exactly
      the generated names), and each vector's fields tiling its value
      with no gap or overlap. Vector values are the DEPLOYED constants
      where the protocol fixes them and a visible byte pattern where it
      does not; each carries hex (authoritative), sha256, keccak256 (the
      request id for a record, the signed digest for an attestation
      preimage) and the value leaf by leaf — as an ORDERED ARRAY, since
      the format is ordered and JSON objects are not. The prose carries
      what tables cannot: Maybe↦Flagged with its failure mode, the five
      finite reject classes plus the in-circuit asymmetry, the
      response-kind table and digest rule, the 128-byte V1 notification
      layout (the one non-Borsh block, stated for log readers),
      endianness twice, the two-oracle statement AND explicit permission
      to use a plain DataView, and a STATUS section separating what is
      deployed from what is specified. Deviation: the regenerator is a
      new --ignored test rather than an extension of stage 0's
      print_layout_snapshot — the two write different things.)
- [X] 9. Hash-flavor API (dmd 2026-08-15): expose persistent_hash
      (BORSH flavor, the default — hashes the subset bytes) and
      persistent_hash_compact (FAB binary_repr semantics: field atoms
      reduced mod p, compress atoms via transient_commit, option
      segments — ONLY for digest-agreement with a Compact contract or
      Compact-produced off-chain digests); same split for
      transient_hash (borsh = straightforward LE limbing;
      _compact = FAB field_repr limbing with its reversed-chunk rule).
      NOT REQUIRED elsewhere (dmd asked): ledger maps/state do no
      in-circuit hashing (keys/values travel as typed limbs +
      alignment; storage-level keying is the ledger's own, off
      circuit), and every ledger-protocol preimage that IS pinned
      (tokenType, coin commitment/nullifier, entry-point hash,
      cross-contract comm, merkle interiors) is already encapsulated
      inside its stdlib/ledger function — users never pick a flavor
      for those, and their byte-shaped preimages are borsh-identical
      anyway. Since contracts are never part-Compact/part-MinoCrab,
      the _compact flavors exist solely at interop boundaries; the
      compat-port artifacts keep FAB by definition.
      (DONE 2026-08-15 — notes/borsh-format.org §"As built — stage 9".
      minocrab_std::v3::hash, four functions, always written
      module-qualified so the flavor is visible at the call site.
      NOTHING WAS RENAMED: the borsh pair is new, Circuit3's raw
      instructions keep their names, and the v2 hash module — FAB by
      definition, since its job is compactc's own lowering — is
      untouched but now says so and points here. The _compact flavor
      has three callers immediately (token_type, coin_nullifier_contract,
      coin_commitment route through it at byte-identical ZKIR), which
      makes "the pinned preimages are its real users" greppable.
      Gates: the digest IS Sha256(borsh::to_vec(twin)) through the
      simulator; the two PERSISTENT flavors agree as byte-identical
      ZKIR, from two independent descriptions (push_limbs vs
      CircuitAbi::atoms + CircuitArg::push_slots) — finding #1 as a
      checkable fact; the borsh transient limbing pinned against native
      Poseidon over borsh::to_vec(v).chunks(31); and the two TRANSIENT
      flavors pinned as genuinely different digests, which is where the
      choice is real. sha2 became a dev-dep of minocrab-std: one lock
      LINE, no new package. Deliberately absent: a keccak256 flavor
      pair — the borsh keccak path already exists and is used, and no
      Compact-interop keccak digest exists to disambiguate against.)
- [X] 10. TypeScript parser codegen (dmd 2026-08-15): generate TS
      reader/writer code from CircuitBorsh::layout() (fixed offsets →
      DataView accessors, dependency-free) into spec/ts/, validated
      against spec/vectors/*.json (vector-driven tests; borsh-js
      remains the library alternative). Then M13 may claim native
      parser generation in BOTH languages.
      (DONE 2026-08-15 — notes/borsh-format.org §"As built — stage 10".
      spec/ts/: six files, 28 types, a reader + a writer + §9's offset
      table AS DATA + a codec registry keyed by the spec's own names,
      all walked out of the SAME schema walk that generates §9 and the
      vectors — =getU64(view, 148)= and the table row for
      tx_params.nonce are one walk emitted twice. Dependency-free in
      the strong sense: no npm packages, no node_modules, no
      package.json; borsh-subset.ts imports only primitives.ts and
      primitives.ts imports nothing. THE CODE CANNOT DRIFT EITHER:
      stage 8's regenerator now writes a third thing, and two new tests
      fail if it stops being the committed one —
      the_committed_typescript_is_generated (byte for byte, and the
      directory holding exactly the generated names) and
      every_vector_type_has_a_typescript_codec (the loop-closer: a new
      vector for an uncovered type fails in Rust, before node runs).
      39 node tests green: every vector decoded, its fields checked
      against the codec's offset table one by one, every DECODED leaf
      re-encoded to the vector's hex, byte-equal re-serialization at
      offset 0 and at offset 3 of a bigger buffer, the 288-byte Misc
      pad rule enforced, plus four reject tests (the 0x02 hazard, short
      buffer, out-of-range write, Flagged fixed-width at both tags).
      MUTATION-CHECKED so the suite cannot be a tautology: shifting one
      generated offset fails 2 tests, flipping a getUint16 to
      big-endian fails 3. Deviations: generated from the spec types'
      BORSH SCHEMA rather than CircuitBorsh::layout() — stage 3's
      cross-check already proves the two equal, the vectors come from
      the schema walk, and several published types have no circuit twin
      — and the hand-written files (primitives, tests, tsconfig,
      README, node-builtins.d.ts) are include_str!-copied from
      tests/serialization/ts/ so that "everything in spec/ts is
      generator output" has no exceptions clause. Nix supplies nodejs_22
      + typescript as BINARIES ONLY; node runs the .ts directly by type
      stripping, so there is no build step and no emitted JS.)
Exit criteria: the spec document and vectors exist and the TS/MPC side
can implement against them; every in-circuit encoder is byte-equal to
both oracles at millions of cases; the record change's k movement is
measured and published.
NOTE stage 7 is HELD by dmd (notes/borsh-format.org §"Protocol
status"): designed + measured, not implemented, until the contract is
presented for discussion. Nothing else blocks on it.

* M12 — Interface crates: import a contract's crate to call it
(dmd, 2026-08-15: "import someone else's crate and use it for your
interface while doing a cross contract call, think like how NEAR does
it... this solves a big compact problem which is the lack of a package
manager (it would be also great if you could do things like derive
keys if necessary)")
Cargo IS the package manager: contract interfaces ship as ordinary
Rust crates (crates.io/git/path), semver'd. Grounding: the M5
cross-contract machinery (contract_call + kernel_claim_contract_call,
the full desugar, differential-green in the xcall suites) is the
engine; M9's typed args/derives are the interface language. What NEAR
cannot do and we can: an interface crate is CHECKABLE against the
deployed artifact (zkir v3 carries the typed I/O schema), so drift is
a test failure, not a runtime surprise.
- [X] Design (notes/interface-crates.org): the #[interface] decorator —
      NEAR ext_contract-style trait declaring the callee's circuits with
      typed args/results; expands to typed call builders over the M5
      desugar (result-limb witnesses, cc-rand, in-circuit
      transientCommit, effects claim). Entry-point hashes DERIVED from
      names, comm layout DERIVED from the types — no hand-typed keys
      ("derive keys" bullet); contract address stays a runtime
      parameter. Macro thinness rules per M9.
- [X] Artifact agreement: machinery asserting an interface crate
      matches a callee artifact's typed I/O schema (reuse
      assert_call_compatible / interface-snapshot idioms) — run in the
      interface crate's OWN test suite against a pinned artifact.
- [X] The Compact-interop importer: generate an interface crate from a
      compactc artifact (contract-info.json + .zkir) — ANY deployed
      Midnight contract becomes importable, Compact-authored or not.
      This is the package-manager answer for the existing ecosystem.
- [X] Port the proof cases: the hand-written signet interface module
      (the vault's calls to the Signet singleton) and the xcall
      experiments re-expressed through interface crates, zero movement
      on all snapshots/differentials.
- [X] Versioning/publishing conventions doc: semver semantics for
      interface changes (argument reorder = major, by construction),
      address/verifier-key pinning story, workspace vs published use.
- [X] README showcase section: the interface-crate story described in
      the README with a real snippet (dmd: "the readme is a showcase
      of our features") — same rule for other landed features as they
      finish (Borsh subset, predicates, maps).
      (DONE 2026-08-15 — two new sections, both quoting code that is
      really in the tree: "Cross-contract calls: the interface is a
      crate" and "Serialization: a Borsh subset, not a format of ours".)
Stages (notes/interface-crates.org §Phasing is the detail; standing
gates every stage: both snapshots bit-identical, all suites green):
- [X] 1. The ABI table: Prim + the ported emit-constraints-for +
      CircuitAbi; CircuitArg::constrain becomes a table-driven provided
      method; LimbConstraint replaces contract_call's &[Option<u32>];
      EntryPoint/ep_hash/ep_limbs via upstream, swept over all 312
      corpus artifacts (618 distinct circuit names).
- [X] 2. The call core, no macros: CallArg(s)/CallResult,
      ContractAddress, Callee, call(); hand-written SignetSigner/Token/
      XcallTarget/PaymentTarget handles; ALL call sites re-expressed
      (both vault forks identically); four differential suites switched
      from invented ep bytes to derived name hashes.
- [X] 3. #[interface] + the visibility-generic derive mode (one
      declaration → CircuitAbi everywhere, CircuitArg at Private,
      CallArg/CallResult at Public); the stage-2 handles re-expressed
      through it with NO call site touched; byte-equal-ZKIR gate against
      hand-written twins; 14-item error inventory, unit-tested.
- [X] 4. signet-signer-interface crate + minocrab-abi + pinned
      artifact + mutation test + schema snapshot; the three vault
      artifacts call signBidirectional through the imported crate at
      zero movement (two import lines changed per artifact).
- [X] 5. minocrab-interface-gen (regenerating stage 4's hand-authored
      crate reproduced every declaration byte for byte — only the doc
      wrapping moved; the xcall target, a Compact-only contract with no
      port, imported and its callers re-expressed at zero movement;
      --check wired as a test).
- [X] 6. Callee-side export (signet_contract's signBidirectional takes
      the interface crate's own record type — one declaration, Private
      for the callee and Public for callers) +
      contract_matches_its_interface (the built IrSource vs the trait
      schema, for all three circuits) + the conventions section.
- [X] 7. README showcase + tick M12.
      (DONE 2026-08-15 — notes/interface-crates.org §"As built — stage 7".
      The cross-contract section shows the vault's REAL call site, the
      cargo dependency, the bodyless trait, the generator invocation and
      the honest limit; the drift-test output is the mutation suite's
      ACTUAL text, captured by running it. A second section showcases the
      M11 Borsh subset (subset-not-format, the deployed-protocol-is-
      already-Borsh finding, the free hash path, the kind byte, and the
      spec/ pointer). The M9-era honesty marker was refreshed and had to
      be CORRECTED in both directions: #[circuit] and #[derive(CircuitArg)]
      are landed and used by deposit/withdraw/claim (so the old "today's
      numbers come from the explicit builder form" was wrong), while the
      side-by-side snippet advertised label!/disclose_as/Discloses — an
      API that does not exist (M9 phase 6). The snippet is now the real
      deposit, abridged.)
Approved by dmd 2026-08-15: the approach as designed, including that
contract interfaces are STATIC (address is data, interface is
compile-time) — not a limitation to work around.
Exit criteria: the vault calls the Signet singleton through an
imported interface crate with every gate green; a Compact-only
artifact imports via the generator; the conventions doc exists; the
README showcases it.

* M13 — README overhaul (dmd 2026-08-15: "showcase as many features
as possible... compare vs Compact... keep the vibe coding line")
Structure agreed in session: header + vibe-coding line verbatim →
MinoCrab-vs-Compact feature table (Rust-not-DSL / cargo package
manager / Borsh subset vs FAB / measured perf / native execution in
cargo test / drift-as-test-failure / typed-args-are-range-constraints /
disclosure tracking / profiler / const generics), EVERY ROW LINKING TO
THE TEST OR DOC THAT ENFORCES IT (the differentiator: claims are
verified, not asserted) → side-by-side deposit (marker refreshed:
#[circuit] is real) → PORTING KIT section (corpus 788 artifacts + the
differential harness: you or your LLM port, the harness proves
statement-identity against compactc's artifact) → trust bullets (9M
cases) → benchmarks condensed → layout/deeper. EXCLUDE until landed:
Discloses labels, maps/predicates (M9 6-7). Runs AFTER the closing
agent's README sections land; keep its voice (show-not-tell, no
adjectives, honest markers).
- [X] The overhaul per the structure above.
      (DONE 2026-08-15 — twelve sections, docs-only (no crate touched,
      both snapshots untouched). The feature table is 12 rows and EVERY
      row links the test or document that enforces it; 47 distinct
      repo-relative link targets, all verified to exist. Safety is the
      organizing theme: the table leads with the compile_fail
      disclosure doctest, typed-args-are-constraints and
      drift-as-test-failure, and §"Safety: what actually stops a
      mistake" replaces the old trust bullets with the 9M-case harness,
      the adversarial sweeps and the 0x02 class closed by a bijective
      format. THREE STRUCTURE ADDITIONS, all showcase: §"The macros are
      decorators, not a compiler" (dmd's macros story, quoting the
      thinness test verbatim), and the two M12 stage-7 sections kept
      and extended — cross-contract unchanged, serialization gained the
      Borsh-AND-FAB flavor pair and the both-languages parser-generation
      block (Rust serde+borsh twins, spec/ts, 39 node tests re-run
      green while writing). §"Porting kit" quotes the real
      assert_call_compatible over 788 artifacts / 312 contracts.
      FAIRNESS: five confirmed gaps after auditing eight candidates
      against the code — arbitrary-bound Uint<0..n>, Opaque, the
      List/MerkleTree/Set ledger ADTs, the unported kernel+unshielded
      builtins, and the Agda spec as an assurance gap. THREE REJECTED
      with evidence: enum exhaustiveness (Compact has no `match`; its
      enums are Uint<0..k-1>, so the real gap is the bound row),
      ZKIR 3.x curve breadth (our pinned IrType already carries
      Curve25519 + secp256r1, and the pinned Compact stdlib exposes
      only jubjub/secp256k1 — the rest is in flight upstream, so not
      possible in Compact today either; this CORRECTS the note's
      tentative VALID) and hardfork targeting. Audit written up in
      notes/readme-research.org §"As built — M13 fairness audit".)
Exit criteria: dmd recognizes the README as the feature showcase; every
factual claim carries its enforcement link.

M13 additions (dmd): include the MACROS story (thin decorators over
hand-writable impls, tooling-friendly — bodies moved not rewritten);
FOCUS HEAVILY ON SAFETY (typed args = range constraints, disclosure
tracking, drift-as-test-failure, bijective serialization closing the
0x02 class, the 9M-case harness — safety is the organizing theme, perf
second); mention BOTH Borsh AND FAB support (Borsh default, _compact
flavors + FAB compat ports — we speak both); NATIVE PARSER GENERATION
in Rust (serde+borsh derives from the same types) and TypeScript (see
M11 stage 10); check the COMPACT ROADMAP and emphasize anything big we
already have (research task feeding the M13 agent).

M13 tone rules (dmd): Borsh is presented as a STABLE, WIDELY-SUPPORTED
format (spec'd, canonical, mature parsers across languages) — that IS
the advantage; NO "they plan it, we shipped it" point-scoring — state
our advantages neutrally, roadmap research is background input only,
not a scoreboard; FAIRNESS section: honestly list major Compact
features MinoCrab lacks (e.g. compactc's maturity/official support,
the TS runtime + dapp tooling integration, language-level docs/
tutorials/ecosystem, anything the roadmap research surfaces —
excluding trivial "we're not our own language" items).

M13 fairness refinement (dmd): the we-lack list contains ONLY actual
language/feature gaps — things POSSIBLE in Compact but NOT possible in
MinoCrab today. Not maturity/support (not a feature), not the TS
runtime (planned later via wasm). Honest candidates to audit for the
list: arbitrary-bound Uint<0..n> for non-power-of-two n (compactc
lowers via less_than; our leaves are power-of-two widths only),
Opaque<'ts-type'> bridging, enum exhaustiveness at the language level,
the unported kernel.* built-ins (blockTime*, unshieldedBalance*,
mint/send/receive kernel circuits — M4 REMAINING), ledger MerkleTree
ops if still unported. The M13 agent verifies each against the code
before listing (only true gaps).

* M14 — Bounded integers: Uint<0..n> for arbitrary n
From the README's fairness list (dmd 2026-08-15: promote the gaps to
milestones). Compact's Uint<0..n> takes any bound; our leaf is
Uint<BITS> (power-of-two ranges only). compactc lowers a
non-power-of-two bound via less_than + assert; the constraint already
exists on the interop side (Prim::UintMax → LimbConstraint::Bounded,
crates/minocrab/src/v3/abi.rs) — this milestone makes it DECLARABLE:
a bounded leaf type whose constrain_input emits the same lowering,
through the whole stack (CircuitArg, derives, interface crates,
Borsh width rules). Covers non-power-of-two enums (Compact enums are
Uint<0..k-1>).
- [ ] Design note first (notes/): leaf shape (const-generic bound),
      lowering, Borsh/FAB width interaction, interface-gen round-trip.
- [ ] Implement + corpus differentials on contracts using Uint<0..10>,
      <0..300>, <0..1000>, <0..70000>.
Exit criteria: a corpus contract with a non-power-of-two bound ports
with the usual differential green; the README fairness bullet moves to
the feature list.

* M15 — Opaque<'ts-type'> bridging
Fairness-list gap: Compact contracts hold and thread TS-side values
with no in-circuit representation (ledger cells, witness args,
Set<Opaque<…>>); our ABI reader and interface generator reject Opaque
by design (crates/minocrab-abi/src/info.rs,
crates/minocrab-interface-gen/src/lib.rs).
- [ ] Design note first: what Opaque means at the ZKIR/ledger level in
      compactc's artifacts (survey the corpus uses), and what the Rust
      shape should be (likely an opaque handle type carrying the
      ts-type string, never entering circuits).
- [ ] Implement through ABI reader + interface-gen + ledger layer;
      corpus differential on a contract using Opaque.
Exit criteria: an Opaque-using corpus contract imports and ports;
fairness bullet retired.

* M16 — Ledger ADTs: List, MerkleTree, HistoricMerkleTree, full Set
Fairness-list gap. crates/minocrab-ledger has Cell/Counter/Map +
Set::insert; the corpus declares 25 List, 15 MerkleTree, 7
HistoricMerkleTree, 41 Set. Merkle PATH circuits are ported
(minocrab-std/src/merkle.rs); the ledger-state ops on trees are not.
Subsumes M4's "Map rem, MerkleTree ops" remainder at the ledger-op
layer.
- [ ] Per-ADT: Impact-op lowering via the real Op types (M4's
      ledger-op method), differential per corpus contract that uses it.
- [ ] Rusty API per M9 phase 7's #[derive(Ledger)] conventions.
Exit criteria: corpus contracts using each ADT port with differentials
green; fairness bullet retired.

* M17 — Kernel + token stdlib remainder
Fairness-list gap; this IS M4's REMAINING bullet, promoted: kernel.checkpoint,
the block-time family (blockTimeLessThan/GreaterThan, blockTimeLt/Lte/
Gt/Gte), the unshielded-token family (mintUnshielded, sendUnshielded,
receiveUnshielded, claimUnshieldedCoinSpend, incUnshieldedInputs/
Outputs, unshieldedBalance*), kernel.balance/balanceLessThan/
balanceGreaterThan, sendShielded, mergeCoin/mergeCoinImmediate.
- [ ] Port via the established route: compactc's vm-code lowering →
      minocrab-ledger ops → stdlib circuits, each with a corpus (or
      tiny.compact-style) differential.
Exit criteria: the README fairness bullet shrinks to nothing in this
class; M4's stdlib bullet finally ticks.
