#+title: MinoCrab Milestones

Working method: one milestone at a time, each session starts by reading this
file and any =notes/= file it links. Findings and decisions get written to
=notes/= as they're made, so no session ever re-derives them. Mark tasks DONE
in place.

Closed milestones are kept in SUMMARY form: what they were, the headline
outcome, and where the full as-built record lives (the =notes/*.org= file
each names, plus git history). Open milestones are kept in full.
Open milestones are ordered by PRIORITY, not number (dmd, 2026-09-04):
the quick wins M31-M34 sit ahead of the long-running Lean work (M27),
which is filed last. Session start = the first open milestone in file
order.

* M0 — Foundations, corpus, and layer map
Research and setup, no eDSL code: the pinned toolchain, the corpus, and the
written-down layer map every later milestone reads instead of rediscovering.
- [X] Nix flake + direnv; compactc 0.33.0-rc.2 pinned by hash (mainnet
      stable 0.31.1 noted).
- [X] Corpus in =corpus/=: 8 pinned sources, 478 files (+195 negative),
      312 compile OK including all 20 sig-net contracts
      (--feature-zkir-v3); =./update.sh= bumps pins; failures recorded as
      data in compile-report.tsv.
- [X] Midnight repo survey → =notes/zkir.org=, =notes/midnight-code-reuse.org=.
- [X] Layer map → =notes/architecture.org=.
Exit criteria MET: corpus compiles reproducibly under nix; architecture doc
exists; the Midnight code we reuse is identified.

* M1 — ZKIR bindings layer
- [X] =crates/minocrab-zkir=: re-exports midnight-zkir 2.2.0 pinned to the
      toolchain's own ledger tag, plus the .zkir version-envelope writer
      the crate lacks.
- [X] Round-trip: parse compactc's ZKIR, re-emit, verify equivalence —
      all 788 corpus files round-trip clean (722 v2 + 66 v3).
- [X] ZKIR v3 bindings (=minocrab_zkir::v3= + =read_any=), pinned to the
      flake's ledger rev.
- [X] A hand-emitted trivial circuit is accepted by Midnight's tooling
      (zkir mock-compile).
Exit criteria MET: we read and write ZKIR their toolchain accepts, proven
against the corpus.

* M2 — Core eDSL + native simulator
- [X] Minimal eDSL: =crates/minocrab-ir= (typed builder over all 26 ZKIR v2
      instructions), =crates/minocrab= (Circuit/Wire API).
- [X] Type-level disclosure tracking: =Wire<Private>= cannot reach a public
      output without =disclose(w, label)= — the single, greppable gate,
      carrying an audit label into the report.
- [X] Native simulator (=crates/minocrab-sim=) mirroring midnight-ledger's
      reference VM; every run convertible to a ProofPreimage and
      cross-checked against =IrSource::check=, so the simulator is never
      trusted alone. Report = op counts + witnesses + labelled disclosures.
Exit criteria MET: the age-gate demo lowers to valid ZKIR, zkir
mock-compile accepts it, the reference VM agrees with the simulator.

* M3 — Verification harness + cost profiler
- [X] Spec-vs-circuit proptest harness (spec in ordinary Rust; every
      accepted case re-validated by the reference VM; =PROPTEST_CASES=
      scales to millions). Disclosure typing proven by compile_fail doctest.
- [X] Differential testing against compactc: ABI modeled in
      =notes/ledger-abi.org= (the equivalence criterion: same typed I/O +
      identical =Preprocessed::pis/pi_skips= on a shared ProofPreimage;
      instruction streams free to differ). Eleven differential suites in
      =crates/minocrab-contracts/tests/*_differential.rs=, each with
      reject-agreement and tamper sweeps.
- [X] Cost profiler: =Circuit::region()= spans; =minocrab_sim::profile()= →
      per-region breakdown + Midnight's CircuitModel k/rows.
Exit criteria MET: the "obviously correct" story exists and is cheap in CI.

* M4 — stdlib + corpus rewrites
- [X] Compact stdlib ported by mechanically translating Midnight's own
      implementations (=crates/minocrab-std=): Bundle wire-typing, Maybe/
      Either, merkle paths, persistent/transient hash+commit, coin types,
      jubjubSchnorrVerify, secp256k1 ECDSA + Ethereum address. The kernel/
      token remainder became M17 (closed).
- [X] ZKIR v3 emission + execution: =Builder3= / =Circuit3= (=Wire3<T, Vis>=,
      capability-trait ops) / =minocrab-sim::v3='s =preprocess()= port —
      see =notes/ledger-abi.org= §6.
- [X] Ledger-op layer (=crates/minocrab-ledger=): Impact ops via
      onchain-vm's =Op::field_repr=, value-carrying ops splicing wires into
      the real layout. The ADT remainder became M16 (closed).
- [X] Sig-net corpus contracts rewritten one by one, each a differential +
      property test — every experiment, test-caller, the Signet singleton,
      and all nine erc20-vault circuits with tamper sweeps
      (=notes/signet-corpus.org=).
Exit criteria MET, CLOSED 2026-08-16: the sig-net contracts run on
MinoCrab with the M3 harness green; both pointer bullets resolved by
M16/M17, each instruction-for-instruction against compactc.

* M5 — Cross-contract calling
Pulled ahead of the benchmark: the erc20-vault runtime circuits call the
Signet singleton cross-contract.
- [X] Cross-contract calls on ZKIR v3 — the full desugar (result-limb
      witnesses, cc-rand/ep witnesses, in-circuit transientCommit,
      effects[3] claim), decoded in =notes/ledger-abi.org=.
Exit criteria MET: xcall / xcontract-events / xcall-with-payment all
differential-green including tamper reject-agreement sweeps.

* M6 — Baseline benchmark: cross-chain deposit/withdrawal
The erc20-vault + Signet singleton benchmarked under MinoCrab and Compact
at the same pinned versions. The BASELINE pass: our lowering mirrors
compactc's, so parity is evidence the rewrite is correctness-preserving.
- [X] Both contracts under both toolchains, M3 harness green; every
      benchmarked preimage corpus-verified first (bench.sh).
- [X] Head-to-head RAM / proving time / cost breakdowns, reproducible via
      =nix run .#bench= (median-of-3 proving, per-cell subprocess RSS).
- [X] Write-up: =notes/benchmark.org= — full table, findings, and the
      ranked optimisation-target list from region-attributed profiles.
Exit criteria MET: parity on the mirrored circuits (rows ±0.7%), strictly
better where compactc over-serializes (refund −12%, completeSwap −15%,
singleton −53..−55% rows; 5 circuits a full k lower → ~half prove
time/RAM), and M7's attack list ranked.

* M7 — Optimisation + presentable deliverable
- [X] Const-generic circuit families (=BytesN<V, N>=, the Signet event
      family, =MerkleTreePath<V, T, DEPTH>=), monomorphized per size/depth;
      =tests/row_snapshot.rs= born here as the (k, rows) freeze for every
      workspace circuit. Skip decisions in =notes/const-generics.org=.
- [X] IR-layer optimisation passes, measured per change
      (=notes/benchmark.org= "M7 pass 1/2", =examples/opcost.rs=):
      pass 1 — native byte plumbing (ReverseBytes replaces ~4,600-row
      explode/rebuild chains): all 9 vault circuits −18..−38% rows, every
      k=17 circuit crossed to k=16, prove −49..−76% vs compactc;
      pass 2 — segment-based Serializer re-limbing: singleton circuits
      −97.5% rows vs compactc, respond k 15→10 (0.11s prove / 44MB vs
      their 8.24s / 875MB).
- [X] BENCHMARK.md at the repo root from a fresh 24-cell run — honest
      about same-k rows cuts that don't move prove time.
Exit criteria MET: a presentable, reproducible report with measurable wins
and the correctness harness green.

* M8 — Churn resilience
- [X] Documented, tooled version-bump workflow: =notes/version-bump.org=
      (the four independent pins, the checklist, a DRIFT TAXONOMY of
      twelve instruments — what a firing MEANS and which response is
      right — and eight hazards) + =./bump.sh= (=pins= / =gates= /
      =accept=). Dry run performed and recorded, including a rejected
      upstream divergence and synthetic instrument firings in both
      directions.
Exit criteria MET: a version bump is a routine chore; one honest
remainder — no corpus diff from a genuinely different compiler has been
read yet, because upstream hasn't shipped one since we pinned.

* M9 — Contract-API ergonomics
Design of record: =notes/contract-api.org=. Circuit entry points as plain
typed Rust fns; real types over macro rewriting (macros only generate
impls/shims writable by hand); zero circuit overhead, gated on interface
snapshot + row_snapshot + all differentials at every phase.
- [X] Phase 0: interface-snapshot test — ordered (label, type) for all
      circuits, frozen.
- [X] Phase 1: typed v3 leaves =Uint<BITS, V>= / =Bool= / =Bytes<N≤31>=.
- [X] Phase 2: =CircuitArg= / =entry()= / =CircuitOut= (Public-only ⇒
      returning is disclosing), hand impls.
- [X] Phase 3: =#[derive(CircuitArg)]= (=crates/minocrab-macros=).
- [X] Phase 4: =#[circuit]= attribute (thinness rule: the expansion
      contains no Circuit3 call).
- [X] Phase 5: mechanical port of the remaining circuits — 78 circuits'
      ZKIR compared at HEAD, movement limited to five renamed labels and
      nine reordered input constraints.
- [X] Phase 6: disclosure declarations in signatures — =label!= /
      =Discloses<D, R>= + macro-generated set-equality tests, which FOUND
      undeclared cross-contract disclosures on day one (now
      =minocrab_ledger::{XcallEntryPointHash, XcallCommitment, XcallResult}=).
- [X] Phase 7: rusty maps (=#[derive(Ledger)]= + =LedgerMap/Cell/Counter=),
      inline-immediate literals (47 =copy <immediate>= removed, zero rows),
      assertion predicates (deferred =#[must_use]= descriptors).
- [X] Phase 8: =erc20_vault_modern=, the showcase twin — every M9 feature,
      PI-equal to the borsh fork on all nine circuits, rows and k
      identical; =.widen::<N>()=; compile-errors-over-panics adopted as a
      standing principle (both width rejections are E0080).
- [X] M9 closure: disclosure declarations on every circuit that has a
      signature — 71 of 87 declare with 63 generated tests; the 16 that
      don't are one documented family (hashing's runtime-width sweep rig).
Exit criteria MET (all four): side-by-side readability with Compact
sources, declarations enforced by tests, zero unexplained movement, the
twin green with deltas explained.

* M10 — Final: ERC-20 vault, maximally verified and maximally optimized
Verification first as the license to optimize, then drop the
compactc-framing constraint where the protocol permits. The direct ports
stay intact as the compatibility reference; the optimized vault is its own
artifact. Full record: =notes/vault-optimization.org=.
- [X] Spec-based property harness for all 9 vault circuits, every branch
      and guard, plus adversarial/boundary sweeps; the gating run executed:
      PROPTEST_CASES=1000000 × 9 specs, 9,000,000 cases, all green (~52
      min). Found along the way: a wrong generation strategy shared with
      the reference model, and the identity-mpcResponseKey acceptance.
- [X] Scoped the freed space in writing first: the vault is HASH-bound
      (95-99% of rows are SHA/keccak/ECDSA), so "maximally optimized" =
      minimal k; constrain_bits dedup rejected as a soundness surface.
- [X] The optimization ladder, every rung gated by the harness and a
      per-circuit divergence ledger: kernel.self dedup, derived
      changeNonce, injective domain separator, the burn restructure
      (single claimed spend, −11,309 rows each for withdraw/swap),
      one-block userCommitment, refund branch merge, Poseidon refund
      commitments. Crossings over the port: deposit k15→14, withdraw
      k16→15; swap missed k15 by 51 rows and was left deliberately.
- [X] The report: BENCHMARK.md refreshed as a three-way capstone.
Exit criteria MET: opt vs compactc rows −35..−58% on every vault circuit,
prove −37..−59% and RAM −42..−52% where k crosses; opt beats compactc by
≥1 k on eight of nine circuits; every deviation documented and
spec-justified; the weaker warrant (spec harness, not PI-equality) stated
prominently.

* M11 — Borsh (fixed-width subset) serialization
Design of record: =notes/borsh-format.org=. This IS Borsh, restricted
to the fixed-width subset and documented as A SUBSET, NOT A SEPARATE
FORMAT: every byte emitted is valid canonical Borsh for the declared
types, and any Borsh implementation (borsh-js included) parses it from
the same declarations. The subset rule is that every circuit-visible
type has a fixed width, so Maybe ↦ Flagged (bool tag + always-present
payload), never Option, and no Vec/String/data-enums. serde +
bincode-fixint emits byte-identical output for the subset, so spec
types derive both and conformance is a DUAL oracle.
Stages 0–10 all landed (as-built per stage in =notes/borsh-format.org=):
- [X] 0. Instruments + oracle: every DEPLOYED payload verified to be
      canonical Borsh byte for byte, including against the pinned compactc
      artifact.
- [X] 1. =minocrab_std::v3::borsh= core: the hash path is FREE
      (byte-identical ZKIR) and the chip's digest IS keccak256(borsh(v)).
- [X] 2. BorshReader, both modes, with measured per-leaf costs
      (=examples/borshcost.rs=).
- [X] 3. =#[derive(CircuitBorsh)]= + =#[borsh(spec)]= cross-check.
- [X] 4. =erc20_vault_borsh= byte-identical fork, own divergence ledger.
- [X] 5. Attested outputs → the flat layout; the 0x02 hazard closed
      (success is a Borsh bool, non-boolean attestations unprovable).
- [X] 6. Event payload twins, byte-identical serialized ZKIR to the
      pinned originals.
- [X] 7. The record: in-band schema strings → a kind byte + format-version
      byte; swap k16→k15 (32,819 → 28,625 rows); records 404→338 and
      571→498 bytes; approveRouter gains a request-only kind so an
      approve is structurally unsettleable.
- [X] 8. =spec/borsh-subset.md= + =spec/vectors/*.json= — the artifact the
      TS/MPC side implements against; generated regions drift-tested.
- [X] 9. Hash-flavor API: =persistent_hash= (Borsh, default) /
      =persistent_hash_compact= (FAB semantics, interop only), same split
      for transient.
- [X] 10. TypeScript parser codegen into =spec/ts/= — dependency-free,
      generated from the same schema walk as the spec, 39 node tests,
      mutation-checked.
Exit criteria: the spec document and vectors exist and the TS/MPC side
can implement against them; every in-circuit encoder is byte-equal to
both oracles at millions of cases; the record change's k movement is
measured and published.
NOTE: stage 7 was held by dmd pending discussion; implemented 2026-08-16
and the hold LIFTED EXPLICITLY 2026-08-17 ("Lift the hold on M11 stage 7,
I'm happy with it"). Nothing is deployed on either side. Two items remain
open:
- [X] Stage 7 HARDENING (queued by the 2026-08-17 code review, which
      found the docs claiming checks the circuits do not make — the
      docs now tell the truth): bind `record.kind == output.kind` and
      assert the record's version byte in each settle circuit — two
      wire equalities per circuit, its own gate (rows move, snapshots
      regen, elevated spec run), both twins in lockstep.
      (DONE 2026-08-27 — assert_record_binds over new EventRecordV2
      kind/version accessors, all four settle circuits, borsh+modern
      byte-identical throughout. refund's bind is ROUTE-selected
      (WITHDRAW/SWAP by the pending-marker bit) because its output kind
      is FAILURE — the one circuit where record.kind and output.kind
      legitimately differ, said so at the site. Movement: +6 rows
      claim/completeWithdraw/completeSwap, +9 refund, no k moves;
      taint baseline renumbered only (verified by normalized diff);
      elevated spec 20k×9 green.)
- [X] BENCHMARK.md refresh (the stage-7 tick's honest remainder): a
      fresh bench run now that swap crossed k16→k15.
      (DONE 2026-08-27 — the benchmark gained a FOURTH side: borsh,
      superseding stage 4's not-benched decision (which predated the
      crossing). minocrab-bench Artifacts::Borsh + per-side preimages
      dumped by the borsh fork gate; fresh single-session 42-cell run.
      THE NUMBER: swap k15, prove 5.27s / 788MB — −40.5% / −46% vs opt,
      −67% / −73% vs compactc — the crossing M10 stopped 51 rows short
      of, paid for by the stage-7 kind-for-schemas record change. All
      other borsh cells within noise of opt, as byte-identity predicts;
      the hardened settle circuits are what is priced. BENCHMARK.md
      rewritten as the four-artifact report.)

* M12 — Interface crates: import a contract's crate to call it
(dmd: NEAR-style — "import someone else's crate and use it for your
interface while doing a cross contract call".) Cargo IS the package
manager: contract interfaces ship as ordinary semver'd Rust crates. What
NEAR cannot do and we can: an interface crate is CHECKABLE against the
deployed artifact (zkir v3 carries the typed I/O schema), so drift is a
test failure, not a runtime surprise. Design + as-built:
=notes/interface-crates.org=. Approved by dmd 2026-08-15, including that
interfaces are STATIC (address is data, interface is compile-time).
- [X] The =#[interface]= decorator: a bodyless trait declaring the
      callee's circuits, expanding to typed call builders over the M5
      desugar; entry-point hashes DERIVED from names, comm layout from
      the types.
- [X] Artifact-agreement machinery, run in the interface crate's own test
      suite against a pinned artifact (mutation-tested).
- [X] =minocrab-interface-gen=: generate an interface crate from any
      compactc artifact — regenerating the hand-authored crate reproduced
      every declaration byte for byte; a Compact-only contract (the xcall
      target) imported at zero movement.
- [X] The vault's three artifacts call signBidirectional through the
      imported =signet-signer-interface= crate; callee-side export shares
      the same declaration.
- [X] Versioning/publishing conventions doc.
- [X] README showcase sections (cross-contract + Borsh), quoting real code.
Exit criteria MET: the vault calls the Signet singleton through an
imported interface crate with every gate green; a Compact-only artifact
imports via the generator.

* M13 — README overhaul
(dmd: "showcase as many features as possible... compare vs Compact".)
- [X] Twelve sections, docs-only. The differentiator: the 12-row
      MinoCrab-vs-Compact feature table where EVERY row links the test or
      document that ENFORCES the claim (47 verified link targets). Safety
      is the organizing theme. FAIRNESS section: five confirmed gaps
      audited against the code (each later promoted to its own milestone,
      M14–M17), three candidates rejected with evidence; no point-scoring
      against the Compact roadmap. Audit in =notes/readme-research.org=.
Exit criteria MET: every factual claim carries its enforcement link.

* M14 — Bounded integers: Uint<0..n> for arbitrary n
First of the fairness-list promotions. Design + as-built:
=notes/bounded-integers.org=.
- [X] THE FINDING: Compact's =Uint<0..n>= range end is EXCLUSIVE
      (compactc's own error text and artifacts agree); three of our notes
      said otherwise, nothing compiled was wrong.
- [X] =BoundedUint<const BOUND: u128, V>= through the whole stack
      (CircuitArg, calls, ledger, disclosure, Borsh, interface-gen).
      No corpus contract was portable (the scan is recorded), so an
      eleven-circuit fixture compiled with the pinned compactc covers
      every edge — and the result is serialized ZKIR EQUAL to compactc's
      up to identifier renaming for all eleven.
Exit criteria MET (with the fixture substitution recorded). M14 CLOSED.

* M15 — Opaque<'ts-type'> bridging
Fairness-list gap. Design + as-built: =notes/opaque-bridging.org=.
- [X] The reframing findings: an Opaque DOES enter circuits (one
      unconstrained scalar slot under a =compress= atom, equality decides
      equality of the TS values via =transient_commit=), and compactc
      publishes the two CURVE POINT types AS Opaque — which is why the
      erc20-vault's own =initialize= was not importable through our own
      interface machinery until this milestone (a real bug closed).
- [X] =Opaque<T: TsType, V>= (marker types, so different ts-types don't
      unify — compile_fail-proven) through the whole stack, plus
      =JubjubPoint=. Fourteen-circuit fixture: ZKIR equal to compactc up
      to renaming for eleven, and for the other three up to one known
      zero-row =copy= each, stated in tests rather than hidden.
Exit criteria MET. M15 CLOSED. Follow-up recorded: no fixture sends an
opaque across a real cross-contract boundary (probe-established only).

* M16 — Ledger ADTs: List, MerkleTree, HistoricMerkleTree, full Set
Fairness-list gap; subsumed M4's ledger-ADT remainder. Design + as-built:
=notes/ledger-adts.org=.
- [X] All thirty-one operations lowered via the real Op types — the
      genuinely new surface was seven opcodes, all already encodable.
      Notable findings: ADT control flow is TRANSCRIPT-level branch/jmp
      (fixed circuit cost); a "default value" is all-zero limbs for every
      Compact type; the fixture caught a one-instruction ordering bug in
      =history_append= that only instruction-level differentials would.
- [X] Typed API per M9's conventions (=LedgerList=, =LedgerMerkleTree=,
      =LedgerHistoricMerkleTree=, completed =LedgerSet=/=LedgerMap=); one
      new leaf (=MerkleTreeDigest=); =AnyWire3= gained =immediate=, closing
      the named-immediate gap for hash operands.
Exit criteria MET: ZKIR equal to compactc up to renaming for ALL 31
circuits, no substitution; corpus provenance recovered against
production code (=push_front_matches_the_corpus=). M16 CLOSED.
Explicitly scoped out (→ M22 / M17): the three coin arms, nested ADTs.

* M17 — Kernel + token stdlib remainder
M4's REMAINING bullet promoted. Design + as-built: =notes/kernel-tokens.org=.
- [X] All 24 fixture circuits — the nine compilable kernel primitives,
      the stdlib compositions, the block-time and unshielded families,
      the three shielded compositions — match compactc INSTRUCTION FOR
      INSTRUCTION. =kernel.checkpoint()= is not ported because compactc's
      own v3 backend cannot compile it (a compactc gap, quoted in the
      fixture). The milestone's list turned out to be two layers wearing
      one name: only nine entries have kernel vm-code, and those are
      three instruction shapes, one of which had existed since M4.
Exit criteria MET: the README fairness bullet in this class is GONE;
M4's pointer bullets tick. M17 CLOSED.

* M18 — Ergonomic parity for the optimized vault (dmd, 2026-08-16)
dmd's question, queued behind the ir-passes staging (notes/ir-passes.org §5
steps 3-5): "what optimisations would make the opt version of the erc-20
vault as ergonomic as the non-opt version?"
THE PREMISE NEEDS CORRECTING FIRST, and the correction is measured rather
than argued. The optimized vault is ALREADY the ergonomic one:
`erc20_vault_modern` is the SHORTEST of the four artifacts (1,608 lines vs
the port's 1,651) with zero raw `emit(` calls, and it is forked from borsh
which is forked from opt — every M10 optimisation is underneath it.
What the opt lineage actually costs is a PARALLEL VOCABULARY: it calls
`burn_spend` where the port calls `burn_coin`, `mint_shielded_token_to_key
_with` where the port calls `..._to_key`, `commitment_short`,
`assert_deployer_short`. The optimisation is encoded in the function NAME a
caller must know to reach for — the same shape as the `_under`/`_guarded`
halves staging step 3 retires. So the milestone's real question is which
suffixes can stop being lexical and become AMBIENT.
- [X] Design note first (the method every milestone since M14 has used):
      which of the opt vocabulary is a PROFILE and which is a genuinely
      different operation. First pass, to be checked rather than trusted:
      (a) `_with` (threaded `kernel.self()`) is MEMOIZATION, and its
      soundness paragraph is already written (M10 rung i: read count is
      framing, not protocol — every emitted read still reconciles through
      `process_read`). Under an opt profile `kernel::self_address` returns
      the cached read and every `_with` twin disappears. This is the typed
      CSE notes/ir-passes.org §3 says belongs in a gadget rather than a
      pass.
      (b) `commitment_short` / `assert_deployer_short` are a one-block hash
      shape — instruction selection from the type, which §4 keeps in the
      typed layer: the commitment builder picks the cheapest limbing.
      (c) `burn_spend` vs `burn_coin` is NOT ergonomics. Different protocol
      shape, different well-formedness warrant (tests/erc20_vault_opt_burn
      _wellformed.rs). Two names is right; the names should say what they do.
      (d) The M10 rungs that changed the SPEC (derived changeNonce,
      injective domain separator, Poseidon refund commitments) are not
      candidates at all — they are decisions, not spellings.
- [X] THE PRIZE, and the reason this is a milestone rather than a cleanup:
      if the choice is a profile rather than a spelling, `erc20_vault_opt`
      stops needing to be a separate SOURCE. The recurring cost in this
      repo is not that opt reads worse — it is that FOUR FORKS are edited
      in lockstep every time the API moves (M9 phase 7, M9 phase 8 and
      M17's `token_type` each paid it). Whether the fork can collapse
      depends on (d): a spec divergence cannot be a profile.
THE GOVERNING CONSTRAINT (dmd, 2026-08-16, on hearing the fork-collapse
idea): "correctness is the number one most important thing in this API."
So, stated before anyone reads the prize and reaches for the delete key:
- `erc20_vault` (the COMPAT port) may never collapse into anything. It is
  PI-equal to compactc and it IS the differential warrant — the whole chain
  `compactc ≡ port ≡ opt ≡ borsh ≡ modern` hangs off it. Collapsing it
  would not simplify the code, it would delete the evidence.
- A fork may only collapse where its divergence is LEXICAL. Item (d) above
  is the test: opt changed the spec in three places (derived changeNonce,
  injective domain separator, Poseidon refund commitments), and a spec
  divergence cannot be a profile. If those survive, opt stays a separate
  source and the honest answer to this milestone is "the vocabulary
  unifies, the fork does not".
- Every divergence ledger stays, asserted in BOTH directions. They are what
  makes a circuit leaving its predecessor's coverage an explicit edit; a
  collapse that removed a ledger would be trading a correctness instrument
  for line count, which is the wrong direction on this project's priority
  order.
Exit criteria: the opt artifact is written in the same vocabulary as the
port, with every remaining twin justified by a protocol or spec difference
rather than by cost — and the four-fork lockstep either gone or explicitly
priced. Not one instrument weaker than it is today.
MET 2026-08-27 — notes/ergonomic-parity.org. The classifications checked
(and one corrected: the packed/padded commitments are a per-deployment
VALUE choice, not instruction selection): the threaded kernel.self is the
one true profile, landed as cache_self_address over a new Circuit3 gadget
scratch map — the _with twins deleted, one mint vocabulary for all four
artifacts, dump-gated byte-identical across all 209 circuits. Remaining
twins are value/protocol differences and say so (assert_deployer_packed
completes the naming). The fork does not collapse (spec divergences);
the lockstep is priced: it is now confined to spec-changing edits. One
instrument STRONGER: tests/v3_kernel_cache.rs.

* M19 — API safety survey and the fixes it ranks (dmd, 2026-08-16)
Survey: =notes/api-safety-survey.org=. THE METHOD, since it is reusable:
look where a test on WELL-FORMED INPUT CANNOT SEE THE ERROR — invariants
stated in prose, and compactc's own verified lowering rules
(=notes/builtin-lowering.org= §9), where anything compactc does
automatically and we leave to the author is a divergence whatever the
differentials say.
- [X] The survey: four wrong-code findings, four footguns. The headline:
      COMPACT EMITS =assert(a >= b)= BEFORE EVERY SUBTRACTION and we
      emitted nothing — the balance-underflow bug, easier to write in our
      API than in Compact.
- [X] Fix 1: typed arithmetic carrying Compact's guards — =Uint::sub/
      sub_with= emit exactly compactc's guard (proven against a real
      artifact: the vault's only subtraction ported with a zero-byte ZKIR
      move); =BoundedUint= add/mul/sub/narrow with compile-time bound
      arithmetic, stricter than Compact where §B4 found Compact emits
      nothing.
- [X] Fix 2: constrain-on-construction (=Serializer::constrained()=) made
      free by the opt-in =dedup_range_constraints= pass — byte-identical
      when combined, proven; B3 now preventable (=notes/ir-passes.org= §11).
- [X] Fix 3: =from_field= split into =from_field_unchecked= /
      =from_field_checked= across every v3 leaf (~100 call sites chose
      explicitly).
- [X] Fix 4: Compact's own signature types on the contracts-level helpers
      (=Uint<64>= where Compact declares it).
- [X] Fix 5: the =_with= family takes =ContractAddress<Public>= — only
      =kernel::self_address= produces one, so the memoization key can no
      longer be an arbitrary =B32=.
Exit criteria: every finding either fixed or refused in writing with a
reason; no invariant left as prose that a pass or a type could hold.

* M20 — Handoff: a human team productionizes this (dmd, 2026-08-17)
A team from another company takes over to productionize. The risk is not
missing information — the notes are thorough — it is that the notes are
written for the agent/owner loop (org jargon, M-numbers, assumed history),
and that THE DISCIPLINE THAT KEEPS THIS CODEBASE CORRECT LIVES IN HABITS,
NOT FILES: baseline dump → change → gates → explain every moved byte →
ledger entry when a circuit leaves its predecessor's coverage. If the new
team edits like a normal repo, the instruments decay into noise and the
correctness story dies quietly. Items chosen by dmd from the handoff
survey (his picks: 1, 2, 3, 5, 7).
- [X] CONTRIBUTING.md — the working method written down, because the
      process is load-bearing. How to make a change (the loop above); what
      each instrument MEANS when it fires (point at version-bump.org's
      drift taxonomy, do not duplicate it); when a divergence-ledger entry
      must move and what its both-directions assertion is for; how to
      regenerate snapshots (between-markers, reviewable diff, never a
      paste); the api-safety-survey §0 method for the class tests cannot
      see; the escape-hatch greps as the code-review checklist.
- [X] CI — the gate ladder in GitHub Actions, nix-provisioned. On PR:
      workspace tests, --release row/interface snapshots, the zkir dump
      comparison against the base branch. Nightly: elevated
      PROPTEST_CASES. Weekly: corpus determinism (compile.sh, zero of 788
      artifacts moved). The point for a team that does not yet trust the
      codebase: instruments that run on their PRs without being asked.
- [X] ONBOARDING.md — the day-one reading path (README → VERIFICATION.md →
      ledger-abi.org §6 → architecture.org, then review-queue.org for
      what has had human eyes), a map of the notes/ files with one line
      each of "read this when …", and a GLOSSARY for the first hour's
      jargon: PI-equality, FAB, the four vault artifacts and why four
      forks in lockstep is a feature, zero movement, the fold, k/rows,
      Impact, the M-numbering. Assembly, not writing.
- [X] Issue-ize the known debt — everything deferred lives in notes prose
      where their boards cannot see it: the `_with` caching profile (M18's
      loose end), Q5's map value-typing (51 rows), the 27 guarded sites,
      e2e-on-a-network and the reference client (the expert-audience
      roadmap), M19's open fixes, the review-queue items. One issue each,
      linking back to the note section that holds the reasoning — the
      issue is a pointer, the note stays the record.
- [X] Publish rustdoc now — cargo doc to GitHub Pages from CI, not gated
      on crates.io (which waits on upstream publishing the ledger-9.1
      line; see PUBLISHING.md). The API docs are good; they are locked
      behind a local build.
Exit criteria: a new engineer reaches their first gated, explained,
reviewable change without asking anyone anything — and their PR runs the
instruments without them knowing the instruments exist.
LANDED 2026-08-27, with one honest caveat: CONTRIBUTING.md (the loop,
the taxonomy pointers, the ledger rule, the survey method, the greps),
ONBOARDING.md (the day-one path, all 32 notes mapped, the glossary),
four workflows (.github/workflows/: ci — workspace + release snapshots
+ the base-branch zkir-dump comparison that fails exactly on the
hidden-movement class; nightly elevated PROPTEST; weekly corpus
determinism; docs → Pages, ENABLED at sig-net.github.io/minocrab,
workflow build type, via gh api), the flake made cross-platform for
linux runners (compactc hashes + target mapping for all four systems),
and the known debt filed as issues #1–#9, each a pointer to the note
that holds the reasoning (the M18 _with item was NOT filed — it closed
today; Q5 filed with its superseded-for-borsh framing). THE CAVEAT: the
workflows have not yet RUN — they run on the next push to GitHub, and
their first green is the exit criterion's real test.
CAVEAT RETIRED 2026-08-28: first push day. ci and docs both green on
main (the review-fixes PR also exercised the PR-only zkir-dump job,
which did its job — the branch's runs surfaced real failures before
merge). The gate ladder is live.

* M21 — crates.io readiness (dmd, 2026-08-17: "ready to be deployed")
- [X] Everything on our side prepared so publish day is a `cargo publish`
      sequence: nine publishable crates classified and metadata'd, crate
      rustdoc front doors, per-crate READMEs, docs.rs config (no build.rs
      anywhere), license texts, PUBLISHING.md with the dependency-DAG
      publish order. DECISIONS OF RECORD, dmd's: the unblock path is WAIT
      FOR UPSTREAM to publish the ledger-9.1 line (vendoring REJECTED),
      and NO fake `version` keys on the git deps — a publish resolving
      against the wrong upstream would be the silently-different-build
      failure this project exists to prevent. rust-version = 1.85.
Exit criteria: when upstream publishes, `cargo publish` in PUBLISHING.md's
order with no repo changes beyond removing the git pins.

* M22 — The last two ADT gaps: coin arms + nested ADTs (dmd, 2026-08-17)
The two remaining bullets of the README's fairness list. Design of record
and as-built per stage: =notes/coin-arms-nested-adts.org= (nested design
reviewed by dmd in-session 2026-08-17: =at_key= builds a path and emits
NOTHING, leaf methods emit the ONE Impact op with the path re-encoded per
op, Map-only nesting per compactc's own kind-checker, depth bound
compile-checked).
- [X] Stage A — the three coin arms (=Set.insertCoin=, =Map.insertCoin=,
      =List.pushFrontCoin=): shared =qualify_coin=, fixture byte-equal to
      compactc for all three, 167 pre-existing circuits byte-identical.
- [X] Stage B1 — nested lowering: =LedgerKey= + the general-path encoder
      (=idx_path=), purely additive; 28-circuit fixture byte-equal to
      compactc. Found and fixed en route: a 16-field ledger block made
      every Cell write nested by itself (a real pre-existing bug), and
      ADT-valued =insertDefault= pushes the ADT's INITIAL VALUE, not zeros.
- [X] Stage B2 — the typed surface: path-carrying handles (=FieldPath= /
      =KeyPath=), sealed =LedgerSlot=, =at_key= via a GAT, depth bound as
      a missing impl (probed: an inline-const assert does not fire in dead
      code). All thirty nested circuits lower byte-identically from the
      typed and raw spellings.
Exit criteria MET: the coin bullet is gone; the nested bullet is REDUCED
to nested coin arms (lowering exists, no differential yet); zero movement
(198/198 byte-identical).

* M23 — Review-readiness: instruments and ergonomics before the super review
(dmd, 2026-08-17.) The sequence to the deep review, approved in-session:
R1 → R2 → R3 → R4, then the M11 hardening stage and the BENCHMARK.md
refresh, then the super review. e2e-on-node (the expert-audience roadmap)
deliberately opens the NEXT cycle — new surface should not land days
before a deep review. Each stage: one agent against this spec, supervisor
verifies (independent gate re-run + diff read), zero movement unless the
stage declares movement, notes as-built.
- [X] R1 — small batch, all zero-movement:
      LANDED 2026-08-17, notes/contract-api.org §"As built — M23 R1".
      One deviation, measured and recorded: rustc renders
      `#[diagnostic::on_unimplemented]` on the E0277 path only, so the
      two METHOD-resolution gates (the coin arms, the eleventh `at_key`)
      gain a named trait in their E0599 but not its prose.
      (a) `#[diagnostic::on_unimplemented]` across the rejection traits —
      the missing-impl rejections gain domain-specific messages (the
      Vis3/disclose gate, LedgerRepr's Public-only bound, the
      QualifiedShieldedCoinInfo3 coin-arm bound, CircuitArg's leaves,
      Meet). B2's KeyedPath bound already carries one; that is the
      pattern.
      (b) `#[circuit(max_k = N)]` cost budgets: the attribute generates
      one more test asserting the circuit's k against the declared budget
      via minocrab_sim's cost model; DOCUMENT it (README feature list +
      the attribute's rustdoc). Adopt on the nine vault circuits of the
      borsh lineage with today's k values as the budgets.
      (c) Self-explaining snapshot failures: when row_snapshot fires,
      print the instruction-level diff for the moved circuit by running
      the dump comparison inline (the machinery exists in zkir_dump.rs +
      the cmp idiom; wire it into the failure path).
      (d) `#[track_caller]` on the recorded build-time panics (the
      value-dependent checks listed in notes/contract-api.org §Panics)
      so they blame the user's line.
      (e) The fold's `returned_identifiers` made terminator-exhaustive
      the way `operands_mut` is (notes/formal-verification-options.org
      §10) — a new upstream terminator must break the build, since the
      unsound direction is the fold doing MORE.
- [X] R2 — the newtype sweep (dmd: "use newtypes wherever you can to
      make the API safer"). Survey first, one commit per family,
      dump-gated; families where the ripple exceeds the safety win
      refused in writing.
      (DONE 2026-08-27 — notes/newtype-survey.org, §"As built". Eight
      families adopted and landed (RequestId, SecretKey, UserCommitment,
      RefundCommitment, CoinNonce, TokenDomainSeparator+CoinColor,
      SigningPath+Caip2Id with the Secp256k1SigLimbs sub-case,
      ZswapCoinPublicKey through the recipient plumbing), eight refused
      in writing. Every commit: zkir dumps byte-identical for all 209
      circuits, interface snapshot unchanged, workspace green. The
      sharpest closures: a swapped mint nonce/recipient (a coin minted
      to a key nobody holds) and the event triple whose transposition
      inverted the deposit authorisation model no longer compile.)
- [X] R3 — THE TAINT LINT, a new instrument (dmd approved 2026-08-17):
      ~150-line pass over Vec<Instruction> per circuit — mark every
      range-constrained wire (constrain_bits/boolean directly;
      intrinsically-bounded instruction outputs: div_mod, byte
      decompositions, encode limbs), then every hash-preimage operand
      (keccak256/persistent_hash/transient_hash) whose atom is narrower
      than the field must be constrained-or-constant, failing with
      circuit+instruction+wire named. Run over support::circuits() so
      all 200 present and every future circuit is audited. This
      mechanizes api-safety-survey §B3 — the class no test on honest
      preimages can see. IF IT FIRES ON AN EXISTING CIRCUIT: that goes
      to dmd before any allowlist entry is written; extending the taint
      rules (provably-bounded sources) is preferred over allowlisting.
      (BUILT 2026-08-27 — notes/taint-lint.org. =minocrab_ir::v3::taint=
      with exact 256-bit interval maxima (a bit-count rule falsely fires
      on every full-width packing) and the in-circuit decoder's own
      operand↔atom consumption rule; gate
      =minocrab-contracts/tests/taint_lint.rs= over all 200 circuits.
      One premise correction: the decoder itself range-checks every limb
      it absorbs, so the lint's residual value is the PACKED-SEGMENT
      discipline behind each limb. IT FIRED: 329 findings, frozen
      byte-for-byte in tests/taint_baseline.txt (any NEW finding fails;
      no allowlist), classified for dmd in the note — ZERO free-witness
      findings anywhere; 317 root in public-transcript popeq wires whose
      warrant is the ledger's normalization (compactc's own artifacts
      fire identically: 27 of 84 v3 corpus files, 195 findings, measured
      by =--example taint_corpus=), and 12 are the M7 ABI-word div
      shifts where the chip's absorption check is what enforces the
      slot. AWAITING dmd's ruling on the extension options ranked in
      the note; unticked for that reason only.)
      (RULED 2026-08-28, option 1 — "less noise is better, but let's be
      clear about the limitations of the lint in the current form" —
      and LANDED, notes/taint-lint.org §7: the Impact/popeq warrant for
      immediate-guard reads (mark_popeq; variable guards keep firing,
      syntactically), the module docs gain a "Limitations, stated
      plainly" section naming the one external rule for what it is.
      Baseline 329 → 101, all classified: 74 guarded reads, 20 derived
      from them, 7 write-embedded computed values; class 2's 12
      resolved themselves once the inputs had real bounds. Corpus
      parity re-measured: compactc 27/195 → 4/85, same residue. Four
      new unit tests; zero movement. Bycatch fixed: minocrab-contracts
      now declares its minocrab-sim dev-dep's =unstable= instead of
      leaning on sibling feature unification.)
- [X] R4 — KANI on the passes (dmd approved, conditional on not
      bloating test times — satisfied by construction: #[cfg(kani)]
      harnesses that plain cargo test never compiles, run as the opt-in
      `./kani.sh`, a separate job when CI exists). Targets:
      fold_immediate_copies, dedup_range_constraints, and R3's taint
      pass. Bounds stated per property; measured runtimes; a property
      that cannot close at a useful bound is scoped down honestly.
      (DONE 2026-08-27 — notes/formal-verification-options.org §"As
      built — M23 R4". CLOSED: the taint lint's exact interval
      arithmetic, ten proofs at stated bounds (0.24s..522s — the 16-bit
      multiplier dominates; sum/shift/bits/order against exact u128 and
      closed-form oracles, cross-limb carries at full width). DOES NOT
      CLOSE, recorded with the mechanism named: the two stream-level
      pass properties — CBMC must unwind Vec<Instruction> drop glue,
      which statically reaches the RECURSIVE Alignment type, and a fully
      CONCRETE two-instruction harness exceeds 25 min — so they are
      retained as bounded SPECIMENS whose unbounded discharge is M25's
      ZKIR-pass prong. Byproduct kept: the passes' internal maps are now
      an insertion-ordered AssocMap (RandomState-seeded HashMap is
      opaque to any model checker; BTreeMap defeats symex), the statable
      shape Lean wants too; workspace green, taint baseline unchanged.
      Provisioning per dmd: rustup FROM NIX (flake devshell, ordered
      behind the pinned toolchain) + a gcc→clang alias for CBMC; kani
      via its supported rustup path; two temporary shims for kani-0.67's
      pre-1.95 nightly vs sysinfo 0.39, documented in kani.sh, both dead
      when kani's nightly catches up.)
- [X] NOTED, NOT YET (dmd): cargo-semver-checks on the interface crates
      in CI — waits for CI (M20 item 2). (DONE by M34 B, 2026-09-05:
      the semver job from the flake, proven to fire.)
Exit criteria: R1-R4 landed with their gates; the taint lint green over
all 200 circuits or its findings resolved through dmd; Kani properties
proved at stated bounds with runtimes recorded; then the M11 hardening
stage and BENCHMARK refresh clear the last known-open items and the
super review runs against a tree with no known-open defects and no
stale published claims.
STATUS 2026-08-28: the queue is CLEAR — R1/R2/R3/R4 all ticked (R3's
ruling landed as the option-1 extension, notes/taint-lint.org §7).
M23 CLOSED. The super review itself is OFF this repo's milestones
(dmd, 2026-08-28): an external party is running it for us — the
milestone's job was readiness, and the tree it hands over has no
known-open defects and no stale published claims.

* M24 — MinoCrab as a library: the GHC-API model (dmd, 2026-08-27)
The identity shift, dmd's: "this is primarily a library like the GHC API"
(https://hackage.haskell.org/package/ghc). GHC ships its innards as an
importable library so the whole Haskell tooling ecosystem — HLS, hlint,
plugins — is built WITHOUT forking GHC. MinoCrab should be the same for
ZKIR: expose the innards so third parties write their own super-optimised
ZKIR gadgets and their own optimisation passes ("compiler rules à la
carte") as ordinary crates, never a fork.
MinoCrab starts CLOSER to this than GHC ever was, because it is
library-first by construction — the eDSL IS the API — and the crate split
already mirrors GHC's internal layering: minocrab/minocrab-std are the
surface language + prelude, minocrab-ir is the "Core" layer (IR builder +
passes), minocrab-zkir is codegen/serialization, minocrab-sim is analysis
(cost model, region profiler, verification harness). The work is deciding
which seams become SUPPORTED extension points, not building the library
from scratch.
Motivated by the AA-manager port (2026-08-27, notes/manager-port.org),
whose −41%-rows-vs-compactc came entirely from the lowering levers this
milestone wants to hand to others.

DECISIONS OF RECORD (dmd, 2026-08-27):
- TIER THE SURFACE — a small semver-stable public API and an explicitly
  unstable internals tier, from day one (do NOT flatten everything, which
  is the GHC API's most-criticised trait — impossible to tell stable from
  incidental, breaks every release). KEEP THE STABLE API SMALL for now:
  "keep our hands free while we have very few users." Rust gives the tools
  Haskell lacked at the start (semver + module visibility + feature gates
  + a `minocrab-internals`-style crate or an `unstable` feature); draw the
  line up front rather than retrofit it (GHC's ghc-lib split came years
  in, painfully).
- SOUNDNESS DEFERRED, BUT DESIGN FOR IT. dmd: "I want to move over to
  having lean verification of steps later, I don't have the time for that
  right now, but build with the fact we're doing that later in mind." The
  path is already staged in notes/formal-verification-options.org
  (compile-fail tests → Kani → the Lean prover). CONCRETE DESIGN
  CONSEQUENCE: passes stay pure, total, `IR -> IR`, with the invariant a
  pass preserves STATABLE — no reaching into global mutable state, nothing
  a machine check (Kani now, per M23 R4; Lean eventually) could not
  target. Build the extension surface so the verification obligation can
  be BOLTED ON later without redesign; do not build the verifier now.
- NIX FLAKE CLI as a RELEASE-ONLY distribution pattern, not a dev
  dependency. dmd: "make sure we're not constantly rebuilding using nix,
  it's very slow"; "I don't mind if it stops working on main occasionally
  for now (later we'll add it to CI)"; "I don't like the binary distro
  though, if it's too hard just don't bother." So: the CLI is a plain
  cargo binary in dev (the flake does NOT enter the dev loop); the flake
  gains a release-only app that builds it from source — the ONE scoped
  relaxation of the M0 hard rule "no crane/naersk, never build our crates
  under nix," authorised here for the release CLI only, allowed to break
  on main until CI. NO prebuilt-binary distribution. If the source-build
  flake app is disproportionately hard, SKIP the flake packaging entirely
  — the cargo binary alone still serves.
- AUDIENCE SPLIT. Rust people use MinoCrab's performance tooling like any
  Rust program — criterion / cargo bench against the crate API,
  `minocrab_sim::profile()` in a test, the row snapshot as a regression
  gate — NO bespoke tooling. The CLI is for people NOT using Rust:
  measurement + running published passes over `.zkir` files, never
  authoring (authoring is inherently the Rust API's job), and
  COMPILER-AGNOSTIC — it reads compactc's ZKIR too, so a Compact user who
  never touches MinoCrab still gets the gate-count value.

- [X] DESIGN NOTE FIRST (the house method): notes/library-api.org. Draws
      the tier boundary (a proposed minimal stable set + the =unstable=-
      feature enforcement), the four extension surfaces, the =Pass= trait +
      its contract (seeded by ir-passes.org's accept/reject list), the
      CLI + flake packaging, and the design-for-Kani/Lean rule (pure/total
      IR→IR). ONE REFRAME it forces, from ir-passes.org §4: the perf wins
      are TYPED-LAYER instruction selection, and ZKIR is type-erased — so
      extension point #1 (typed gadget libraries) is the HIGH CEILING and
      #2 (IR passes) is the uniform-transform TAIL, not the speed headline.
      Say so in the public docs. Build order, least-work-first: CLI seed →
      =Pass= trait → tier boundary → gadget cookbook + rustdoc.
The four extension points (à la carte, no fork):
- [X] 1. GADGET / CIRCUIT LIBRARIES (the highest ceiling, Layer 1): a
      third party publishes a crate depending on minocrab/minocrab-std
      with a hand-tuned keccak, a cheaper Merkle path, a smaller ABI
      encoder. Works TODAY over a git dependency; what this milestone owes
      is DECIDING and DOCUMENTING the stable authoring surface (the
      `Circuit3` builder core, the typed leaves, `AnyWire3`, the raw
      instruction emitters, the FAB/alignment machinery, the cost model)
      and marking the rest internal. The lowering levers to document are
      already inventoried in notes/benchmark.org + notes/vault-
      optimization.org + notes/manager-port.org.
      (COOKBOOK LANDED 2026-08-27, step 4 first half: OPTIMIZATION.md at
      the repo root — the ten levers de-jargoned for external gadget
      authors, each grounded in a real API + the measured note behind it,
      linked from the README library section.
      CLOSED 2026-08-27 with tier boundary v1: the stable authoring
      surface is decided and documented — the README's Stability-tiers
      table + a tier statement opening every crate's docs.)
- [X] 2. IR PASSES (Layer 2, "compiler rules à la carte"): promote the
      pass layer from today's free-functions + `dedup_range_constraints`
      builder flag to a stable `Pass` trait (`IR -> IR` + a report) with
      ordinary-Rust composition (a `Vec<Box<dyn Pass>>` or a builder — NO
      GHC-style `-fplugin` dynamic-loading machinery; it's just a crate
      import). Build on notes/ir-passes.org, which already names the three
      to build (guard scoping DONE, constant folding, constraint dedup)
      and — crucially — the TWO IT REJECTS as unsound-here (DCE, CSE): the
      reject list is the seed of the pass CONTRACT third parties must
      honour. Passes designed pure/total/statable per the soundness
      decision above.
      (LANDED 2026-08-27, notes/library-api.org §8: =Pass= in
      minocrab_ir::v3::passes — pure/total =transform= + a PROVIDED =run=
      that always returns a =PassReport= and AUTO-WARNS on any net
      instruction drop (dmd: "make sure they've been warned first"), the
      two reference passes wrapped, =run_pipeline= + =by_name=/=builtin_
      names=. Report OPTIONAL to consume; a VALID dedup still trips the
      drop-warning, as dmd anticipated. Exposed by default (stable-ish);
      the =unstable= internals sweep is step 3, not this. Tests green.)
- [X] 3. VERIFICATION HOOKS: the differential / spec / taint harness as
      public API, so a third-party pass can ship WITH its own proof it
      preserved meaning. NOT built now (soundness is deferred), but the
      extension surface in points 1-2 must not PRECLUDE it — this is the
      "build with it in mind" clause made concrete, and it converges with
      M23 R3 (taint lint) and R4 (Kani on the passes), which this
      milestone pulls forward.
      (SATISFIED BY DESIGN 2026-08-27: R3 and R4 are landed, the taint
      lint IS stable public API (minocrab_ir::v3::taint), passes stayed
      pure/total throughout (the AssocMap refactor made them MORE
      statable), and nothing in the tier boundary precludes exposing the
      differential/spec harness later — the deferred-build clause holds.)
- [X] 4. COST / MEASUREMENT: `cost()` / `profile()` / the calibrated
      primitive-cost tables as stable API, plus the release-only nix flake
      CLI over `.zkir` (measure, region-profile, diff two circuits, run a
      published pass) per the decisions above.
      (SEED LANDED 2026-08-27: a simple `[[bin]]` `minocrab` in
      minocrab-sim (src/bin/minocrab.rs, dmd's "simple [[bin]]" — not its
      own crate) — `rows <file.zkir>...` (k, rows, instr, in/out + total)
      and `diff <a> <b>` (row/k delta). Compiler-agnostic (reads compactc's
      ZKIR). Region-profile deliberately NOT over `.zkir` — it needs the
      annotated Compiled3, so it stays the Rust `profile()` path.
      CLOSED 2026-08-27: cost/profile/assert_max_k/rowcost are the stable
      measurement tier (tier boundary v1); `pass <name> <file.zkir>
      [-o out]` landed (minocrab-ir a regular dep of minocrab-sim; runs a
      published pass over any ZKIR — compactc's included — reporting the
      instruction/k/row delta and the pass's warnings, writing only with
      -o). The FLAKE APP is SKIPPED BY DECISION, per dmd's pre-authorised
      branch: nixpkgs' importCargoLock cannot vendor a workspace whose two
      pinned midnight-ledger git revs export identical crate
      name-versions — recorded in flake.nix at the spot; the CLI stays a
      plain cargo binary.)
- [X] TIER BOUNDARY + DOCS, the foundation for all four: draw the stable
      vs internals line (a proposed MINIMAL stable set: the eDSL surface,
      the `Pass` trait, the cost API — everything else internal, dmd to
      confirm in/out), and publish rustdoc — for a compiler-as-a-library
      the DOCS ARE THE PRODUCT, so M20's "publish rustdoc" item becomes
      central here rather than a handoff chore.
      (LANDED 2026-08-27 as TIER BOUNDARY v1 — notes/library-api.org
      §"As built: the tier boundary". The proposed minimal set is
      IMPLEMENTED: an `unstable` cargo feature gates the named internals
      (minocrab-ir's Builder3/Val layers, minocrab-sim's simulator VMs),
      every crate's docs open with its tier statement, and the README
      carries the Stability-tiers table. The honest mechanics are
      recorded: cargo feature-unification makes the gate a hard wall
      exactly for the à-la-carte pass/lint-crate graph (minocrab-ir
      alone) and a docs/semver declaration elsewhere. The std
      contract-authoring surface (ledger/kernel/borsh/disclosure) is
      explicitly NOT yet promised — the in/out call that remains dmd's.
      `cargo doc --no-deps` builds warning-free on the stable tier;
      PUBLISHING rustdoc to Pages stays with M20's CI item.)
Exit criteria: a third party can, from a git dependency and the published
rustdoc, write both a ZKIR gadget crate and an optimisation-pass crate
against a small stable API — without forking, and without touching the
internals tier — and measure the result through the crate API (Rust) or
the release CLI (non-Rust); the pass surface is shaped so the deferred
Kani/Lean verification of "preserves meaning" bolts on without redesign.

* M25 — Port the Compact repo's Agda proofs to Lean (dmd, 2026-08-27)
THE FINAL milestone, and the machine-checked-spec endgame of
notes/formal-verification-options.org's staging (compile-fail tests →
Kani → Lean). dmd's directive: directly port as many Agda proofs from the
Compact repo to Lean as possible, prioritising those that represent ZKIR
PASSES or anything HIGHER LEVEL. dmd is partial to Lean; any proof here is
LLM-written and LLM-maintained under review, so LLM-writability is the
main axis. Do NOT start until the earlier rungs (M23 R4 Kani, and M24's
pure/total pass surface) are in place — this is deliberately last.

THE HONEST GROUND, from notes/formal-verification-options.org — record it
so the milestone is not scoped on the Agda's reputation:
- The CHECKED Agda is Compact's STATIC + TYPING semantics of Lsrc
  (=Lsrc.lagda.md= 2899 lines, =Lsrc-Typing.lagda.md= 1447,
  =Coverage.agda= 340). That is the "anything higher level" content, and
  it is what is DIRECTLY PORTABLE — the SOURCE-language semantics, not
  ZKIR. Caveat: =Lsrc.lagda.md= carries =--allow-unsolved-metas= and is
  not =--safe=, so a port must decide per hole whether to carry it or
  close it (closing is the higher-value act and the LLM-writability test).
- "Where they represent ZKIR passes": THE AGDA HAS NONE. It says nothing
  whatever about ZKIR, and the one part that could relate — the
  operational/CEK semantics with its witness oracle — is an UNCHECKED
  SKETCH: =Machine.agda= has unfilled holes (=fetch/deref/>>= = {!!}=),
  no module imports it, it may not compile. So the ZKIR-pass half is NOT
  a port; it is NEW Lean, built on the footing M24 laid deliberately: the
  =Pass= trait's =transform= is pure/total /so that/ a Lean model of "the
  output PI/witness stream ≡ the input's" and "only implied constraints
  dropped" can target it. The Agda's value here is a MODELLING PRECEDENT
  (the witness oracle is exactly M19 §A3's guarded-witness bug shape),
  not code to transcribe.
- The Lean BRIDGE is not yet load-bearing (formal-verification-options.org
  §9's named bar). This milestone assumes that bar has cleared; if it has
  not, the milestone's prerequisite is clearing it, not porting.

THE METHOD (dmd, 2026-08-27) — a PER-CONSTRUCT loop, not a bulk port, and
the payoff lands in the TYPE SYSTEM rather than in a Lean file in a drawer:
  1. MAP — find our eDSL syntax construct's counterpart in Compact's
     syntax. The corpus ports already ARE this mapping in practice: each
     MinoCrab circuit is a Compact construct rendered in the eDSL.
  2. FIND — locate the Agda proof(s) about that Compact construct. Because
     the CHECKED Agda is STATIC/TYPING semantics, the constructs that have
     proofs are the source-level ones (well-formedness, typing rules) —
     which is precisely why this method fits the Agda we actually have,
     where a bulk port would not.
  3. PORT + CONSTRAIN — port the proof to Lean (machine-checked), AND
     reflect its invariants into MinoCrab's Rust TYPE SYSTEM so the types
     CONSTRAIN use of the feature to what the proof covers. The Lean
     proof is the WHY; the Rust type is the always-on enforcement at
     every call site. This is CLAUDE.md's "compile errors over panics"
     with a PROOF behind the constraint, landing on M24's type-level
     surface. A typing rule maps onto a trait bound / a distinct type that
     does not unify / an inline-const assert — the same ladder the hard
     rule already ranks.
     THE FIT (dmd, 2026-08-27): the Rust types are NOT required to cover
     EXACTLY the set of values the Lean proof allows. The requirement is
     SOUNDNESS in one direction — the types must admit a SUBSET of what the
     proof allows (never a value the proof does not cover) — and within
     that, the MAXIMAL such subset the type system can express at
     reasonable cost. Sometimes the subset is the whole set and the two
     coincide; often the proof's boundary is value-dependent or
     non-structural and the type-level approximation is properly smaller.
     Both are acceptable; admitting MORE than the proof is not.
     THE BALANCE: pick a reasonable point between usability and typing
     strength, leaning towards CORRECTNESS over usability but WITHOUT
     abandoning usability. A constraint that requires a type-level
     contortion no user could reasonably write (or read) is a worse
     product than a slightly smaller admitted set expressed plainly —
     and where tightening to the proof's edge would make the feature
     unspellable in practice, stop short and record the gap (the shortfall
     is an honest entry for the LIMITATIONS DISCUSSION below, not a
     failure of the loop).
  The deliverable of each turn of the loop is thus TWO things: a checked
  Lean proof, and a type-system invariant that makes only proven-safe
  usage spellable (the maximal usable subset, per THE FIT above).

- [X] Design note first (the house method), extending
      notes/formal-verification-options.org: the construct inventory (the
      step-1 map), which constructs have checked Agda proofs (step 2), and
      for each the target Lean module AND the Rust type invariant it
      becomes (step 3). State the §9 bridge bar and confirm it is cleared
      before any Lean lands.
      (DONE 2026-08-27 — notes/lean-port.org. The eight-row inventory
      (rows 1-3 the portable Agda: numeric hierarchy, Castable,
      visibility; rows 4-6 the named OTHER APPROACHES; rows 7-8 the new
      Lean), each FIT-annotated EXACT/SUBSET. PATH DECIDED: model-first
      (one of the survey's two Aeneas blockers fell to R4's AssocMap;
      the map_operands twin is the prepared upgrade), with the model gap
      stated in every Lean file's header. THE BAR, restated for the
      model path — a nix-provisioned toolchain and one real theorem,
      lake-green — is CLEARED: lean/ at the repo root (lean4 from
      nixpkgs in the devshell, no elan), with the dedup model and FOUR
      sorry-free theorems: sublist, passthrough-verbatim, per-wire
      tightest-bound (hence solution-set) preservation, idempotence —
      the R4 stream specimens' dedup half discharged UNBOUNDED. A lean
      job added to CI.)
- [X] Run the loop over the HIGHER-LEVEL (static/typing) constructs:
      Agda proof → Lean (holes closed where the LLM-writability axis
      allows) → the Rust type-system invariant that constrains the feature
      to (a maximal usable subset of) what the proof allows, per THE FIT.
      The type invariant is the milestone's user-facing product; the Lean
      is its warrant. Record per construct whether the types match the
      proof's set exactly or admit a strict subset, and why.
      (DONE 2026-08-27 — notes/lean-port.org §3 "CORRECTED BY THE PORT"
      + §5. THE STEP-2 CORRECTION, found by reading the Agda at the
      pinned rev: rows 1-3 have NOTHING to port — the bound computation
      is =⊢undeclared= stubs, subtyping is a refl/trans stub with a
      postulate, disclose types through with no visibility judgment
      (bycatch: their =⊢>== smart constructor is a copy-paste of
      =⊢expr-<==). So the warrants are NEW Lean stating compactc's real
      rules: a second lake package =crates/minocrab-std/lean/=
      (MinocrabStdProofs), 23 sorry-free theorems green under Lean 4.30
      and 4.33 — Numeric.lean (add/mul bound asserts sound AND minimal,
      the sub guard makes field-sub integer-sub with the underflow shape
      as the counter-theorem, the guard's comparison width, the free
      retypes) and Visibility.lean (Meet's table is a lattice meet; an
      expression types Public IFF every private leaf is under a
      disclose — both directions). Reflection: the type invariants
      already existed (the const asserts, Meet, Public-only CircuitOut);
      tests/lean_claims.rs embeds both proofs via lean_proof! as the
      drift gate; lean/ ships in the crate; CI's lean job builds both
      packages. FIT recorded per construct in §5: BoundedUint add/mul
      EXACT (the _minimal theorems discharge maximality), widen/to_uint
      EXACT, Uint-family SUBSET by design, visibility SUBSET in the
      strong direction.)
- [X] The ZKIR-PASS prong (new Lean, no Agda to map): the =Pass= trait's
      preserve-meaning proof (PI/witness stream unchanged, only implied
      range constraints dropped — M23 R4 Kani bounds it first, Lean
      discharges it unbounded), reflected as the type-level pass CONTRACT
      (M24 §3) so a third-party pass that violates it does not type-check
      as far as the type system can reach.
      (DONE 2026-08-27, in three landings — notes/lean-port.org §5:
      dedup's four sorry-free theorems; fold's SYNTACTIC half
      (=fold_outputs=/=fold_skeleton=/=foldRun_is_filter_map=); and
      fold's SEMANTIC half — =fold_preserves_observables=, sorry-free,
      under a =WellFormed= that is exactly Builder3's SSA guarantee
      (Nodup dsts + defs-before-uses), via a shared =envStep= so the
      observer and =namedOf= agree by construction. The R4 stream
      specimens are both discharged UNBOUNDED. The REFLECTION landed as
      designed: =ProofRef= + =lean_proof!= (proof file EMBEDDED at
      compile time — a deleted proof is a build error), =VerifiedPass=,
      =run_pipeline_verified= with theorem-drift surfaced as report
      warnings and a builtin-claims test. The lake project MOVED to
      crates/minocrab-ir/lean/ so the proofs ship inside the crate the
      macro embeds them from (Cargo.toml include; CI + flake updated).
      Checked green under both Lean 4.30 (CI devshell) and 4.33.)
- [X] THE LIMITATIONS DISCUSSION (dmd, 2026-08-27): an honest writeup of
      what the Compact repo's spec actually covers and does NOT — static/
      typing only, an unchecked operational SKETCH, nothing on ZKIR,
      =--allow-unsolved-metas= — so PARITY is measured against their real
      coverage, not their reputation. Where a construct has NOTHING to
      port, the milestone does not leave a gap: it names the OTHER
      APPROACH taken (a property test, a Kani harness, or a type invariant
      argued directly from the protocol) so the feature is still
      constrained, just not by a ported proof.
      (DONE 2026-08-27 — notes/lean-port.org §6 is the full account
      (what their spec is, what parity therefore means, what our Lean is
      NOT); VERIFICATION.md §5's first bullet rewritten from "no
      machine-checked semantics" to the honest two-sided statement; the
      README fairness bullet rewritten likewise, and the README pass-
      authoring section's "planned (Kani, then Lean)" updated to the
      landed VerifiedPass reality. The other approaches are named per
      construct in the §3 table rows 4-6.)
THE AIM (dmd, 2026-08-27): PARITY, not exhaustiveness. The Agda is thin;
"as many as possible" means match their formal coverage where it is real
and portable, reach for the other approaches where it is not, and say so.
Exit criteria: PARITY with the Compact repo's formal coverage — every
construct they actually prove has its invariant in our Rust type system
(admitting a subset of the proven-safe values, maximal within the
usability balance of THE FIT, never a superset), Lean-warranted where
portable and argued via the other approaches where not — plus the honest
limitations discussion above; and the M24 pass
surface has a machine-checked "preserves meaning" proof reflected in its
type-level contract. Explicitly OUT: transcribing the unchecked
operational sketch (porting a =  {!!}= proves nothing) and any Lean the
§9 bridge does not yet support.
MET 2026-08-27, with the parity bar RESTATED by what the port found:
what the Compact spec ACTUALLY proves is well-formedness plumbing and
constructor coverage (its bound computation, subtyping and visibility
content are stubs/postulates/transparent — notes/lean-port.org §3
correction + §6) — that coverage is matched (exhaustive-match
instruments, the typed I/O artifact-agreement tests) and then exceeded:
23 sorry-free theorems in MinocrabStdProofs warrant the numeric asserts
(sound AND minimal) and the disclose gate (sound AND complete over the
model), 11 in MinocrabProofs warrant the pass layer, reflected as
VerifiedPass + the lean_claims drift gates. The honest boundary stands
in every file header: model proofs, transcription-matches-Rust by
review + instruments, no extraction. Row 8's unbounded interval twins
deliberately left at Kani (§5, the recorded decision). No proof was
PORTED — the milestone's premise dissolved on contact with the real
Agda, and the loop's deliverable (proof + type invariant per construct)
landed anyway, which is the outcome the FIT rule was written for.

* M26 — External-review follow-ups not taken on the review-fixes branch (dmd, 2026-08-28)
The review-fixes branch (notes/external-review-triage.org, one commit per
finding) closed the external review's correctness items. What it
deliberately left, with the reasoning, so it is a milestone and not a
note in a drawer:
- [X] =when_private= — a private-condition scope for bodies with NO
      on-chain effect (assert / witness only). (DONE 2026-09-05:
      =Circuit3::when_private= in minocrab v3; the on-chain emitters
      refuse inside it at build time with the disclose-first message —
      the ladder's last rung, so the gate is =should_panic= rather than
      =compile_fail=, three of them, plus the byte-identity idiom against
      the hand-threaded private guard; registered as panic 5 in
      notes/contract-api.org §"Panics that could NOT become compile
      errors". Zero uses in the workspace, as expected.) Compact allows
      =if (privateBit) { assert(...) }= without =disclose=; since §4.1's
      =OnChainGuard= bound, MinoCrab does not — a private condition must
      be disclosed to branch on at all. Zero uses in the workspace today,
      which is why it was not built; it is a PARITY gap, not a safety one.
      The shape when needed: =when_private(cond, body)= accepting any
      visibility, pushing a private-scope marker; the on-chain emitters
      (=emit_impact=, =emit_public_input= in =v3/effects.rs=) fail at
      build time inside it with the disclose-first message (the ladder's
      last rung, because the scope stack is dynamic and the type system
      cannot reach it without a type-state =Circuit3=, which ripples
      through every =&mut Circuit3= helper). Gate: the guard-scope suite's
      byte-identity idiom plus a =compile_fail= that an =impact= inside
      =when_private= is rejected.
- [ ] The authoring path the review's §5 found missing: a typed
      contract-testing harness in minocrab-sim (distil the vault's
      6,611-line private support), a from-scratch template/tutorial, and
      the keygen/deploy binding. Explicitly deferred by dmd (2026-08-28):
      not before the super review. NOTE 2026-09-04: M23's closing line
      records that the super review is now run by an external party;
      whether that satisfies the hold is dmd's call — an agent can
      build this item once it is lifted (best-effort shape: distil the
      vault's =tests/support= into a typed harness first, the tutorial
      second, the deploy binding after M29 D/E exist).
- [X] (2026-09-05, 7dc65fd: six files — impact / ops / reads / kernel /
      calls / tests — lib.rs = crate doc + re-exports (103 lines); dump
      byte-identical on 209 circuits, re-run on main at merge;
      notes/edsl-trim.org "M26 — the ledger crate split".)
      =minocrab-ledger/src/lib.rs= at 3,700 lines — a split by concern
      (Impact encoding / ledger ops / calls). Cosmetic; zero-movement by
      construction (the dump gate).
Exit criteria: =when_private= lands with its gates or is refused in writing
with a measured reason; the authoring-path items are scheduled into their
own milestone after the super review.

* M28 — The sig-net protocol refresh: Poseidon hashing, the renamed 17-circuit vault, and the lending flows (dmd, 2026-08-30; rewritten 2026-09-05)
WHAT UPSTREAM SHIPPED (corpus refreshed on branch =sig-refresh=, 2026-09-05:
signet-midnight-integration =1ee4a7f3= → =fff3421c=, signet-midnight-examples
=2c929a1b= → =0d9c1660=; both still language 0.25, compactc 0.33.0-rc.2
compiles them, compile-report unchanged):
- THE PROTOCOL MOVE (integration #85/#87, 2026-08-19..09-03, "Move in
  circuit hashing to transientHash"): =calculateRequestId= and
  =calculateSignetAttestationDigest= are =upgradeFromTransient(transientHash(…))=
  (Poseidon over the FAB field representation; byte 31 of every id is
  zero); =verifyRespondBidirectionalEvent= takes =bigR.x= / =s=
  LITTLE-endian (reversed off-chain, "circuit-input form") — no in-circuit
  =reverseBytes32=; =abiWordToUint128= decomposes the word once (Horner).
  The MPC followed on 2026-09-04 (mpc #1206 "align Compact hashing": a
  =compact-hashing= crate; the reader recomputes the id from the stored
  CELL's field repr; response digest per source chain). The singleton's
  three circuits are byte-unchanged.
- THE VAULT (examples, 2026-08-13..24): 17 circuits under NEW NAMES —
  =initialise=, =startDeposit=/=completeDeposit=, =startWithdraw=/
  =completeWithdraw=/=refundWithdraw=, =startSwap=/=completeSwap=/
  =refundSwap=, =approveRouter=, =approveStata=, =startSupply=/
  =completeSupply=/=refundSupply=, =startRedeem=/=completeRedeem=/
  =refundRedeem= — per-flow refund circuits (the split the =Pending=
  lineage made on its own), the Aave lending flows, 19 ledger fields
  (chunked paths, depth 2), and the commitments / domain separator /
  change nonce moved to =transientHash= (the =_opt= lineage's own
  avenues, adopted upstream). The test caller's =deployerCommitment=
  moved likewise.
DONE 2026-09-05 (=sig-refresh=):
- [X] Corpus pins bumped and recompiled (4d9cf61).
- [X] The Signet module port follows: =signet::request_id_of= and both
      attestation digests are Poseidon + upgrade; =verify_attestation_signature=
      takes little-endian scalars (=Secp256k1SigLimbs= says so); the
      =Limbs::transient_hash= twin. GATED at the artifact level by
      =test_caller_differential= (=initialise= call-compatible with the
      NEW artifact — the deployer commitment through =transientHash=)
      and by the singleton differentials (unchanged). The Poseidon
      request id's artifact gate is the vault re-port below (or a port
      of the test caller's =submitSignatureRequest=, not done).
- [X] =signet-sim= retranslated to mpc =10360c3c=: =hashing.rs=
      (=compute_request_id= over the cell's field repr,
      =compute_response_hash=, the MPC's golden passes), both id rules
      (=IdRule::Transient= / =LegacyKeccak= — the captured fixture is
      valid under the legacy rule and a "rid-mismatch" under the live
      one, as the MPC's own renamed test asserts), =Signature::circuit_input=
      (the LE form). The drift gate (tests/signet_flow.rs) now proves the
      MPC's field preimage IS the circuit's limb list.
- [X] =signet_flow= / =erc20_vault_pending= / the toy and is_even
      callers follow through the shared functions (green; cheaper —
      Poseidon for keccak, no reversal), and the Pending lineage's
      identity commitment follows upstream to Poseidon
      (=common::commitment_transient=): initialize 2,446 → 747 rows,
      deposit 3,476 → 1,781, claim −1,700. The rows-only re-benchmark
      against compactc's 17 new artifacts is in notes/benchmark.org
      (§2026-09-05): every request circuit 0.14-0.35× of compactc's,
      every settle 0.86-0.88×.
NOT DONE — the vault re-port, which is this milestone's body:
- [X] A. The compat lineage (=erc20_vault=) re-ported against the 17
      new artifacts (2026-09-05, branch =m28-vault=, notes/vault-refresh.org
      §1): the 21-field SEGMENTED block (six at =[0,i]=, fifteen at
      =[1,i−6]= — the derive's paths proven against compactc for the
      first time), five =#[derive(LedgerRepr)]= settle views, the lending
      flows, Poseidon commitments everywhere, per-flow refunds. All 17
      PI-equal to compactc's artifacts: =erc20_vault_differential= 57
      tests (matches-corpus + guard agreement + tamper sweeps, zero
      disagreements). Rows, compat vs compactc: =initialise= 891 = 891;
      =startDeposit= 1,834 vs 11,406; =startWithdraw= 23,109 vs 32,751;
      =startSwap= 23,955 vs 43,592; settles 35.6-45.4k vs 40.3-52.5k
      (row_snapshot). Two upstream facts followed exactly:
      =completeWithdraw= witnesses the secret unconditionally (asserted on
      the refund branch only — the success-case sweep is transcript-only);
      =startRedeem= reads =vaultEvmAddress= twice.
      - [X] The =_opt= / =_borsh= / =_modern= forks: RETIRED (2026-09-05,
            branch =m28-vault=, notes/vault-refresh.org §0) — their
            compactc anchor left with the rename, upstream adopted their
            avenues, and the =Pending= lineage carries what remains
            (the kind constants and V2 aliases moved there; the harness's
            =Art= survives as a concretization selector for the
            conformance suite, =ARTS = [Compat]=). Bench back to two
            sides; README's side-by-side now shows =erc20_vault_pending=.
- [X] B. =tests/vault= rewritten around one concretization (=ops.rs=
      segmented op shapes, =model.rs= Env/Req/17 scenarios with DERIVED
      popeq outputs, =exec.rs= segmented state, =spec.rs= concrete
      effects, =gen.rs=): =erc20_vault_spec= 17 properties × five links
      (acceptance, reference VM, compactc comparator, PI closure, ledger
      Effects) green; =erc20_vault_adversarial= 19 tests;
      =disclosure_report= re-pinned to =startDeposit= (7 disclosures);
      =serialization_conformance= keeps the byte claims and DROPS the
      =keccak256(borsh(record)) == id= equalities (the id is Poseidon over
      field limbs now) — spec/borsh-subset.md §5/§6 REWRITTEN around the
      Poseidon rule (dmd, same day): §6a states the field-element rule
      and =upgradeFromTransient=; layout (Borsh bytes) and identity
      (Poseidon over field elements) are two halves now. The two lending
      record instantiations (=SupplyEvent= 407, =RedeemEvent= 439, and
      =RedeemEventV2= 370) TABULATED in §9 and proven byte-equal to the
      model's FAB bytes 2026-09-05 (6d83065).
- [X] C. The bench's target table remapped (17 + 3 targets, two sides)
      and BENCHMARK.md RE-RUN and rewritten (2026-09-05, 40 cells): every
      request circuit ≥1 k level below compactc (2-word requests k14→k11,
      prove −80..−81%; startSwap/startRedeem k16→15, −44..−46%), the nine
      settles ECDSA-floored at k16 (rows −12..−16%, prove ±noise),
      =initialise= row-identical at 891, singleton −95..−97% as before.
      The headline did change materially: the settle-side wins shrank to
      rows-only (Poseidon on both sides now), the request-side wins grew.
      "Optimise" beyond this is the Pending lineage's job (rows-only table
      in notes/benchmark.org); the compat lineage is PI-equal by
      definition.
- [X] D. M27's V-list re-frozen against the 17-circuit vault
      (2026-09-05, notes/zkir-semantics.org §7.1): one artifact,
      seventeen circuits, the draft's C1-C10 re-mapped; V5's Gap 2
      changes materially (=completeWithdraw= witnesses the secret
      unconditionally, asserts it on the refund branch only) and its
      approveRouter pre-check is settled (the three witnesses are the
      xcall's cc-rand + ep limbs); V7 answered by construction (no
      settle reads the record back); V3/V4 restated for Poseidon; V8
      flags the transientHash limb-bound check.
Exit criteria: the 17 circuits ported PI-equal (compat) and the API
lineage green; every suite on the branch green; BENCHMARK.md re-run on
the new protocol; corpus pins on main.
Exit criteria MET 2026-09-05; merged to main the same evening (dmd:
"merge everything"). The Borsh spec's §9 tables for the two lending
record instantiations followed the same day (6d83065); the Pending
lineage's lending flows landed under M35 C (1f81716).

* M29 — Signer artifacts for the MPC publisher; real gates on transaction construction and proving (dmd, 2026-09-02)
Survey and decisions: notes/mpc-publisher.org. The signet contract is
not deployed yet, so the deployment is OUR artifact (respond at k=10 /
1,004 rows vs compactc's k=16 / 40,931; ~46 MB vs ~855 MB to prove).
Felipe's TypeScript sidecar in =sig-net/mpc= (=midnight-publisher-ts=)
never interprets the IR: compactc's JS executor makes the preimage and
whatever sits at =zkir/<circuit>.bzkir= proves it, so the swap needs
no mpc change — only the artifact files, their verifier-key hashes,
and the deploy tooling in signet-midnight-integration. What is NOT yet
shown, anywhere: that a respond intent built the way the ledger builds
it proves under our key and applies to a =LedgerState=. Every existing
gate is PI-level (=IrSource::check=), or has proofs switched off. This
milestone ships the artifacts AND closes that gap, before M30 puts a
Rust publisher on them.
- [ ] DESIGN NOTE: notes/mpc-publisher.org (written 2026-09-02, §3 the
      swap path, §4 the honest test inventory, §6 the pins).
- [X] A. (2026-09-05, 992ce06 — =crates/signet-artifacts=: library +
      =cargo run -p signet-artifacts -- <out-dir>=; managed dir with
      committed =.zkir= + =.verifier= + =MANIFEST.sha256= (all twelve
      files' SHA-256, uncommitted prover/bzkir included) +
      =expectedVk.json=; =generated_equals_committed= runs the real
      =zkir-v3 compile-many= (0.5s for three k=10 circuits, which also
      writes the =.bzkir= siblings); notes/mpc-publisher.org §8.)
      Artifact pipeline: emit =.zkir= for the three signer circuits
      (=signBidirectional=, =respond=, =respondBidirectional=) from
      =signet_contract= via =minocrab_zkir::v3::write_zkir=; run the
      flake's pinned =zkir-v3= (=compile-many=, the same step compactc
      runs) for =.bzkir= + =.prover= + =.verifier=; lay them out as the
      managed dir the sidecar and deploy tooling read
      (=keys/<circuit>.prover|.verifier=, =zkir/<circuit>.bzkir=).
      Commit the =.zkir= and =.verifier= (small, reviewable); a
      generated-equals-committed gate on both, like =spec/ts=; the
      prover key and =.bzkir= regenerate deterministically at package
      time (record their SHA-256 so drift is visible).
- [X] B. (2026-09-05, same commit. NO (=.verifier=, hash) fixture pair
      exists anywhere reachable — =@sig-net/midnight-contract= is not
      vendored in ~/mpc and the signet contract is undeployed — so the
      pin RUNS compact-js's =hashVerifierKey= (sha256-hex over the raw
      file bytes, =ContractKeyLocation.js=) under node on our committed
      =respond.verifier= and asserts the Rust function agrees; skips
      loudly without node or the checkout.) Verifier-key hashes: emit
      the =expectedVk= table (circuit →
      hash) the sidecar's =build= and =prove= paths check against; pin
      the exact hash function to compact-js's =hashVerifierKey= by a
      test on a corpus =.verifier= whose hash the mpc fixtures carry.
- [X] C. (2026-09-05, d8ae7b5 — tests/support/signet_call.rs builds every
      singleton preimage through ContractCallPrototype → Intent →
      construct_proof and reads it back out of the ContractCall;
      tests/signet_construction.rs: call-compatibility on that preimage
      for all three circuits + tamper twins + the alignment-invariance
      claim; signet_contract_differential.rs retired.) Construction gate — the ledger's preimage, not a hand-built one:
      build each signer call through =ContractCallPrototype= and the
      ledger's =construct_proof= (=ContractCallExt for ProofPreimage=,
      construct.rs:515-573), so the preimage under test is byte for byte
      what the sidecar and any Rust publisher produce; assert
      call-compatibility on THAT preimage for all three circuits. The
      differential test keeps its tamper twins; the hand-built
      =preimage()= helper retires.
- [X] D. (2026-09-05, 177e922 — tests/signet_ledger_apply.rs; goldens
      under tests/fixtures/mpc/ whose SHA-256 IS the captured tx hash;
      entry point, Impact program op for op, log version/type, the 288
      Misc bytes all equal; address / comm / proof / binding / gas
      necessarily differ; one measured surprise: compute_time 2.208 ms
      theirs vs 1.303 ms ours — parameters differ, M30 B item.)
      Ledger-apply gate: =ContractDeploy= with OUR verifier keys into a
      =LedgerState=; build the respond =Intent= / =Transaction=;
      =well_formed= then =apply= under the proof-preimage marker; the
      emitted Misc bytes must equal the layout mpc's reader decodes —
      import =chain-midnight/fixtures/respond-tx-161.mn= and
      =respond-bidirectional-tx-181.mn= as goldens and compare the call
      transcript to theirs.
- [X] E. (2026-09-05, 9e7ac09 — tests/signet_real_proof.rs: keygen ==
      the committed managed key byte for byte, Transaction::prove
      through an in-process V3ProvingProvider (35-line transcription of
      test_utilities' v3 arm), well_formed with verify_contract_proofs
      ON in Real mode, apply; permuted-key negative control rejects all
      three. Prove 0.18 / 0.12 / 0.12 s; whole-run peak RSS 83 MB for
      all three signer circuits in one process. The first real proof in
      the test suite.) Real-proving gate (=--release=, ignored by default, with
      =row_snapshot=): keygen from our IR, =Transaction::prove= through an
      in-process =ProvingProvider= (=LocalProvingProvider= +
      =MidnightDataProvider=, params pre-warmed), then =well_formed= with
      =verify_contract_proofs= ON against the deployed key, all three
      circuits. Record prove time and peak RSS beside BENCHMARK.md's
      numbers. This is the first real proof in the test suite.
- [-] F. (2026-09-05: FACTS recorded, notes/mpc-publisher.org §9 —
      every shared crate is at the same semver in both repos, only the
      git object differs; mpc has no zkir-v3 and acquires one from the
      swap; the .mn goldens deserialize under our pin unchanged. The
      CHOICE is dmd's; notes/zkir.org's pin table deliberately
      unedited.) Pin reconciliation: this repo (rev 04c9c5d) vs mpc
      (crate-ledger-9.1.0.0-rc.3 / transient-crypto-3.0.0-rc.2) —
      choose one, build =minocrab-contracts= against it, record the
      decision in notes/zkir.org's pin table.
Exit criteria: the managed-dir artifacts and =expectedVk= table
generated and gated; C, D, E green (E under =--release=); the pin
settled. Deploying the package and running mpc's real-stack test
against it is mpc's work, tracked there (dmd, 2026-09-02: these
milestones track this repo only).

* M30 — A Rust publisher for the MPC: retire the TypeScript sidecar (dmd, 2026-09-02)
Scope decided 2026-09-02: the publisher's pure core lives HERE as a
library crate that mpc's =chain-midnight= consumes behind its existing
=IntentClient= seam; the node I/O (subxt extrinsic, event ingestion),
the swap-in, the sidecar's deletion and the real-stack test are mpc's
work and are tracked in the mpc repo, not here. Proving is a Rust function call — no proof server, no
child process; the only network touch (KZG params) is removed by
shipping the params in the image (notes/mpc-publisher.org §2). The
gap is the DUST fee wallet: every primitive exists in
=midnight_ledger::dust= but no Rust wallet has been written around
them. Estimate ≈1,800 production lines in mpc, ≈4,500 deleted (§7).
- [ ] A. Public surface here: one function per respond circuit taking
      =(request_id, signature)= and returning the =ContractCallPrototype=
      inputs (args as =AlignedValue=, transcript ops, key location) —
      the M29 C path made a library; disclosure manifest unchanged;
      =Discloses= gate still generated.
- [ ] B. =minocrab-publisher= crate (name open): build → =Intent= with
      TTL; prove → in-process =ProvingProvider= with a disk =Resolver=
      for our keys and =DustResolver= for the fee proof; sign
      (=Intent::sign=) and =seal=. Pure over (contract state, ledger
      parameters, funding keys, dust state); no node I/O, so every step
      is unit-testable on the M29 D fixtures.
- [ ] C. DUST wallet: =DustLocalState= replayed from ledger =Event=s,
      persisted and REBUILDABLE from genesis (untrusted source, §5);
      =Transaction::balance= → dust spend → =DustActions=;
      =process_ttls=; single-UTXO gate; readiness = sync time past the
      pinned block. Tests on event fixtures captured from the mpc real
      stack (add the capture to the real-stack driver).
Out of scope here, for the mpc side (recorded so the seam is
explicit): the extrinsic the wallet SDK posts (verify the pallet call
against midnight-node first); error classification preserving the
sidecar README's four codes (=wallet_unsynced=, =proving_timeout=,
=state_conflict=, =ambiguous_submit=), which =publisher.rs='s retry
logic keys on; the in-process =IntentClient= replacing =intent_gen.rs=;
the reader's dropped =BlockProofSeed= (indexer.rs:120-132), the MPC's
real exposure to a lying endpoint (notes/mpc-publisher.org §5.1),
which the publisher port does not address.
Exit criteria: A-C published from this workspace with unit gates on
the captured fixtures; a respond intent goes build → prove → sign →
seal → =well_formed= (proofs verified) in one process, inside the M29 E
time and RSS budget, with no network access when the params are on
disk; the DUST wallet rebuilds its state from an event log and spends
against a =LedgerState= in a test.

* M31 — The first REAL compactc bump: 0.33.0-rc.2 → 0.34.0 (language 0.26.0) (dmd, 2026-09-04)
M8 built the churn-resilience workflow and left one honest remainder:
"no corpus diff from a genuinely different compiler has been read yet,
because upstream hasn't shipped one since we pinned." Upstream has:
compactc 0.34.0 shipped 2026-08-25 as a FULL release (language 0.26.0).
notes/compact-findings.org already probed it (2026-08-28) and
established the one fact that bounds the blast radius — 0.34.0's own
flake.lock pins zkir-v3 at =04c9c5d9bceb=, EXACTLY our Cargo pin, and
our bindings round-trip its output — so this is a compactc-only bump:
the ledger pins do not move (version-bump.org "The four pins").
Nothing deployed is affected either way (nothing is deployed).
This is the milestone that tests whether the drift taxonomy is real:
twelve instruments whose meanings were written down against a DRY RUN
and synthetic firings, now read against a compiler that changed for
its own reasons. Follow notes/version-bump.org "The checklist" step by
step; =./bump.sh pins= / =gates= / =accept= is the runnable half.
- [ ] 0. Look before touching: =./bump.sh pins= output recorded; the
      0.34.0 release notes / language 0.26.0 changelog read and the
      items that could plausibly move a corpus artifact listed BEFORE
      the recompile (so the reading in step 4 is a prediction checked,
      not a story fitted afterwards). Hazard 1 (version string vs rev)
      and hazard 2 (=--version= drops the suffix) re-read.
- [ ] 1. Move the pin: =compactcVersion= in flake.nix, release hashes
      for ALL FOUR systems (the M20 cross-platform flake — a linux
      hash left stale breaks CI, not the laptop), =notes/zkir.org='s
      pin table.
- [ ] 2. Recompile the corpus (=corpus/compile.sh=) and READ
      =compile-report.tsv='s diff first: language 0.26.0 may reject
      sources that compiled under 0.33 — a source that stops compiling
      is DATA (recorded, not patched), but a sig-net contract that
      stops compiling is a finding for its owners, said so in
      notes/corpus-sources.org.
- [ ] 3. Gates in taxonomy order (=./bump.sh gates=); every failing
      instrument classified by notes/version-bump.org "Drift taxonomy"
      1-12 BEFORE anything is regenerated.
- [ ] 4. The reading, written up as version-bump.org "Bump 1 —
      2026-09-xx" beside the dry run: per moved artifact, which
      taxonomy class, and the evidence (the new
      =cargo run -p minocrab-ir --example zkir_text= disassembler is
      how to read one). The three answers each firing can have:
      (a) upstream lowering changed, statement preserved → our port
      keeps PI-equality, the instruction-level fixtures (adts, kernel,
      opaque, bounded, coins, nested, wide) may legitimately move and
      say so per circuit; (b) upstream FIXED something we mirrored →
      the mirror moves with it, ledger entry; (c) a wire-contract
      change → stop, dmd decides.
- [ ] 5. Accept only what step 4 justified (=./bump.sh accept= is
      EVERYTHING — the reviewed diff is the between-markers regen, per
      CONTRIBUTING.md, never a blanket accept). Snapshot diffs read.
- [ ] 6. Numbers: the bump-time bench compact-findings.org deferred
      here — the Signet singleton and the vault's nine under 0.34.0,
      one row-comparison table; BENCHMARK.md's compactc column
      re-stamped only if rows moved.
- [ ] 7. Commit shape per version-bump.org §7: the pin commit, the
      corpus commit, the accepted-baseline commit, the write-up —
      separately reviewable. Then tick M8's honest remainder in place.
BEST-EFFORT COMPROMISE, stated up front: if step 3 fires broadly (a
lowering change across hundreds of artifacts), the milestone's
deliverable becomes the CLASSIFIED READING plus a decision list for
dmd, with the pin bump left on a branch — not a forced accept. A
half-read bump accepted on main is hazard 8 (a diff that is not the
bump) at scale.
ORDERING CONSTRAINT: land on main BEFORE M28's =vault-refresh= branch
rebases — that branch already moves one pin (midnight-examples), and
two pins moving in one diff is the thing the taxonomy cannot separate.
After M31, M28 recompiles its corpus under 0.34.0.
Exit criteria: the pin is 0.34.0 on main with CI green on all
platforms; every instrument that fired has a taxonomy class and a
sentence of evidence in the write-up; M8's remainder ticked; or, on
the compromise path, the classified reading and decision list exist
and the branch waits on dmd.

* M32 — Circuit-set completeness: =#[contract]= everywhere, and a test that the set is complete (dmd, 2026-09-04)
VERIFICATION.md §5 lists it as a known hole: =support::circuits()= is
hand-written (200 entries) and feeds row_snapshot, interface_snapshot,
the zkir dump and the adversarial suite; a circuit added and not
listed is covered by NOTHING and nothing fires. The fix in progress is
=#[contract]= deriving the set from the impl block (review-queue.org);
adoption today is 57 of 198 =#[circuit]= attributes (kernel_tokens,
coins, nested). Two halves, both zero-movement by construction under
the dump gate.
- [X] A. (DONE 2026-09-05, c6f5123 — 16 more files under =#[contract] impl=; =support::circuits()= = the derived unions + the =entry()=-built remainder (events, events_borsh, the hashing width sweep) + two contracts kept as individual rows to preserve the frozen snapshot order; dump byte-identical on 209 circuits, both snapshots unchanged, circuit_closure green.) Adoption: every remaining =#[circuit]= in
      =crates/minocrab-contracts/src= moves under a =#[contract]= block;
      =support::circuits()= becomes the union of the derived sets plus
      ONE explicit remainder — the =entry()=-built families (the hashing
      width sweep rig) that carry no attribute — each remainder entry
      with a comment saying why it cannot be derived. Gate per file:
      dump byte-identical, both snapshots unchanged.
- [X] B. (already built as tests/circuit_closure.rs, c5caf15, review §7.4 — the source scan; it fired tonight on M35's ten new circuits) The completeness assertion, the half that closes the hole:
      a test asserting that every =#[circuit]= attribute in the
      workspace SOURCE appears in the derived union. The honest cheap
      version is a source scan (walk =crates/*/src=, collect the fn
      names under =#[circuit]=, compare) — crude, but it is the same
      class of instrument as the escape-hatch greps CONTRIBUTING.md
      already relies on, and it runs in plain =cargo test=. If the scan
      proves brittle (cfg'd-out circuits, doc examples), the upgrade is
      link-time registration (=linkme= distributed slice populated by
      the =#[circuit]= expansion; no build.rs, so docs.rs stays clean)
      — decide in writing, do not do both.
- [X] C. (2026-09-05: `attest::mutant_unlisted` injected → `every_circuit_attribute_is_listed` FAILED naming it; reverted) Mutation check, the way every instrument here is born: add a
      =#[circuit]= to a test-only module and leave it unlisted; the test
      must fail. Record the run.
- [X] D. (2026-09-05) VERIFICATION.md §5's bullet rewritten from "known hole" to
      the instrument, §3's failure-class map gains the row; the
      review-queue entry closed.
BEST-EFFORT COMPROMISE: A is mechanical but touches every contracts
file; if the weekend runs short, B + C over the CURRENT partial
adoption already closes the hole (the scan compares against the union
PLUS the hand list), and A becomes cleanup. B is the milestone; A is
the tidy.
Exit criteria: the mutation in C fails the build; the dump and both
snapshots are byte-identical to the pre-milestone baseline;
VERIFICATION.md no longer lists the hole.

* M33 — The trusted-computing-base inventory: what a human must read, in what order (dmd, 2026-09-04)
Preparation for the human audit that no agent can do. Most of the tree
is warranted by the differentials against compactc; the part that is
not is small and nameable, but no document names it — VERIFICATION.md
§5 lists the limits, architecture.org lists the layers, and neither
says "these files, this many lines, this order". Reviewer time from
people who know the ledger's =preprocess= and Impact internals is the
scarce resource in the whole production plan; this document is what
makes an hour of it count.
- [X] A. (2026-09-05: TRUST.md generated from tests/trust_base.rs; 25,332 lines, 10,501 READING in whole or part) =notes/trust-base.org= (design of record) + a short
      =TRUST.md= at the root for the reviewer: per crate and per module
      within the big ones (=minocrab-ledger/src/lib.rs= at 3,700 lines,
      =minocrab-std= at 10k, =minocrab-macros= at 3.2k), three columns —
      lines; what warrants it (PI-equal differential / instruction-level
      differential / spec+property / Lean model / NOTHING BUT READING);
      and the failure that would hide there if it were wrong. The
      "nothing but reading" rows ARE the trusted computing base.
      First-pass expectation, to be checked not trusted: the IR builder
      and passes, =Circuit3= with the disclosure gate and guard scopes,
      the Impact/ledger-op encoding, the macros' generated code beyond
      shape, and the =minocrab-ir=/ZKIR boundary — the four things §5
      already says the safety survey never covered.
- [X] B. (2026-09-05, TRUST.md §2: nine steps, ~3 days) The read order with a time budget: which files first, what
      question each answers, what to compare it against in the ledger
      source (file and function, with the pinned rev), and the
      escape-hatch greps as the checklist. VERIFICATION.md §6's one-day
      audit path is the seed; this is the multi-day version for
      someone who will sign their name.
- [X] C. (2026-09-05: closure + currency tests, MINOCRAB_TRUST_BASE=1 regenerates) Numbers generated, not typed: the line counts and the
      warrant column are regenerated between markers (the same
      reviewable-diff rule as the snapshots) from a script, so the
      document cannot rot silently; a test asserts the generated
      region is current.
- [-] D. (2026-09-05: VERIFICATION §5 and ONBOARDING DONE; the M20 issue needs the push — dmd) Cross-link: VERIFICATION.md §5 points at it; ONBOARDING.md's
      reading path gains it after VERIFICATION.md; the M20 issue list
      gains one issue pointing at it for the reviewer's board.
BEST-EFFORT COMPROMISE: the warrant classification for the two big
crates can be done at module granularity rather than per function;
a wrong "differential-warranted" claim is the one error the document
must not contain, so where unsure the row says READING and the
reviewer downgrades it, never the reverse.
Exit criteria: a reviewer with the pinned ledger checkout and this
document can start reading with no questions asked; every row's
warrant claim names the test or proof that backs it, or says READING;
the generated region has a currency test.

* M34 — CI closure: clippy and semver checks on the interface crates (dmd, 2026-09-04)
Two small gaps in the gate ladder the external review and M23 both
noted and nothing scheduled.
- [X] A. (2026-09-05, e651c01: 0 findings under -D warnings; two lints decided once in [workspace.lints]; the clippy CI job) Clippy in CI: =cargo clippy --workspace --all-targets --
      -D warnings= as a ci.yml job. First re-measure the review's
      "3 clippy warnings" remark (never re-measured, triage §"Process
      findings"); fix each finding zero-movement (dump gate), or
      =allow= it at the site with a one-line reason — no crate-wide
      allows. Lints that would fight the API's deliberate shapes
      (=too_many_arguments= on circuit entry points, say) get one
      workspace-level decision in Cargo.toml's =[workspace.lints]=,
      recorded in CONTRIBUTING.md.
- [X] B. (2026-09-05: tool from the flake; config-level patches needed and recorded; proven to fire on a method rename — notes/interface-crates.org) =cargo-semver-checks= on =signet-signer-interface= and
      =xcall-target-interface= (issue #6): the binary from the flake
      devshell (a tool, not packaging — the nix-light-touch rule), run
      on PRs with =--baseline-rev= the base branch, since nothing is on
      crates.io yet (issue #9). An interface crate's surface is
      governed by the callee's deployed artifact, so a break there is a
      wire-compat event; the job says so in its name. Prove it fires:
      one PR-shaped run against a branch that renames a circuit method,
      recorded in notes/interface-crates.org.
- [-] C. (2026-09-05: CONTRIBUTING rows DONE; closing issue #6 needs the push — for dmd) Close issue #6 with the workflow link; CONTRIBUTING.md's
      instrument list gains both rows, pointing at what a firing MEANS
      (clippy: style, fix or allow with reason; semver: wire-compat,
      stop and read the artifact-agreement test).
Exit criteria: both jobs green on main and shown to fire on a
synthetic break; issue #6 closed.

* M35 — The Signet round trip as a typed future: =Pending<Env, Resp>=, and a Sig simulator (dmd, 2026-09-05)
Design of record: notes/signet-async.org (§3 why not =.await=, §4 the
sketch, §6 simulating Sig, §7 before/after, §8 the safety catalogue).
THE AIM, in dmd's words: make this interface AS IDIOT-PROOF AS
POSSIBLE, because it is a complicated one. Every vault operation is
one future split across two transactions — a request circuit that
files a record and cross-calls the singleton, and a settle circuit
that verifies the MPC's attestation and consumes the record — and
today the suspension is hand-rolled four times over with eight
invariants kept in sync by eye (§7's table: kind bytes, record
version, notification path bytes, map/env/nonce triples, calldata
re-parsing, the verify→kind→lookup→remove sequence). The principle:
TYPES CONSTRAIN THE AUTHOR, ASSERTS CONSTRAIN THE PROVER — nothing
here removes an in-circuit check on untrusted input; the API makes
the check impossible to forget and the wrong pairing impossible to
compile. Ranked per the house rule: missing impl / non-unifying types
first, inline-const asserts for the const-known (duplicate kinds,
path depth), build-time panics for nothing.
Decisions of record (dmd, 2026-09-05):
- NO TIMEOUT. The vault has no cancel path and that is deliberate;
  refund only on an ATTESTED failure. "Pending forever" is a legal
  terminal state and must hold every invariant.
- Two explicit halves, not an =.await= split. Rust's async captures
  the stack implicitly; here anything that crosses is public ledger
  state, so capture must be explicit and typed (§3).
- The EVM is an ORACLE in tests, never simulated.
- The singleton's three circuits (=signBidirectional=, =respond=,
  =respondBidirectional=) are UNCHANGED — M29's artifacts are not
  blocked. What moves is the CALLER side, before the vault deploys.
- [X] A. (2026-09-05, f0d1935; notes/signet-async.org §9) The types, with a =compile_fail= gate for each §8.A/§8.C item:
      =#[derive(Response)]= (an associated =KIND=, requires
      =CircuitBorsh= so =Bool= is 0/1 by construction); =Env:
      LedgerRepr= with every field =Public= (a =Private= wire fails to
      unify); =Pending<Env, Resp>= as a =ledger!= slot owning request
      map, env map, nonce and the =MpcResponseKey= newtype cell;
      =Settle<Env, Resp>= as ONE =CircuitArg= ticket in frozen wire
      order; =Requested<Env>= / =Settled<Env>= as the complete label
      sets (the label machinery's extension). Inline-const assert: two
      =Response= kinds equal in one block; =Pending= slot path deeper
      than the notification's four bytes (compactc chunks past 15
      fields).
- [X] B. (2026-09-05; §9 records item 14 STRUCK and the per-slot refund deviation) =request= and =settle=: the only constructors of a request id
      and of a typed =Resp=; atomic step order (freshness, nonce,
      insert record + env, derived notification, xcall; disclose id,
      verify keccak(id ‖ borsh(resp)), kind, version, member, lookup,
      remove); ticket consumed by value, no =peek=; outcome returned
      as a value the circuit must match on, =expect_success(c)= the
      explicit assert; sender and caip2 read from context inside
      =request=; =RECORD_FORMAT_VERSION= owned by the API; =hash(Env)=
      in the record's unused 64-byte =params= so the MPC reader's own
      id-recompute covers the continuation state — CONFIRM FIRST
      against =chain-midnight/src/reader.rs= that the recompute spans
      =params= and the width check accepts it (§8 item 14's caveat);
      =Commit<T>= with =open(c, &witness)=, domain-separated by the
      slot, for what survives privately (the three "not the
      depositor/withdrawer/swapper" gates become one op). Notification
      path derived from the slot AND pinned by an artifact-agreement
      test against the compiled contract-info's field path (a wrong
      path is a silent "record not found" on the MPC).
- [X] C. (2026-09-05: circuits DONE, cost gate restated PER PAIR from measurement — §9 table; lending extension DONE — notes/signet-async.org "Rung C, lending extension"; spec harness DONE, f8fe44c — tests/vault_pending/ + tests/erc20_vault_pending_spec.rs, 18 properties × four links (no compactc comparator, by construction), green at PROPTEST_CASES=2000; two harness bugs the .zkir dumps caught are in the note) The vault re-expressed: all four request/settle pairs on
      =Pending= (deposit/claim, withdraw/completeWithdraw,
      swap/completeSwap, approveRouter/refund), as a new lineage the
      spec harness runs like =Art::Opt= — the shared spec stays the
      oracle, its model extended for the env map and the =params=
      binding; every guard in =spec::Guard= still enumerated. Row cost
      measured (=row_snapshot=) and NOT above the current optimised
      lineage per circuit — the calls do what the circuits do today,
      so any excess is a bug in the API, not a price of it. EXTENDED
      with upstream's lending flows (=approveStata=, supply/redeem via
      the stataUSDC wrapper): seventeen circuits on a twenty-two-field
      block, each new circuit costed against =erc20_vault='s (compat
      port) twin rather than modern, which never had them.
- [-] D. (2026-09-05: the responder half DONE as =crates/signet-sim=; the state-driving half BLOCKED on the v3 transcript executor dmd holds in M26 — §9) =SigNetSim=, at the PROTOCOL boundary: the =Responder= trait
      (singleton event, caller ledger state, =EvmOutcome=) →
      attestation; ONE root key with the real =derive_epsilon_midnight=
      so =initialize='s response key is derived, not arbitrary; the
      read side runs the MPC's OWN reader logic (path walk by the
      notification's bytes, decode, id recompute) — the
      dependency-direction decision (factor an I/O-free crate, or move
      =reader/records/request_id= here with M30) recorded in the note;
      failure output built exactly as =node/respond_bidirectional.rs=
      does; adversarial responders (wrong key, wrong kind, replay,
      double response, unfiled id, non-boolean success). State driven
      two ways: the Impact VM on a real =StateValue= chained request →
      post → respond → settle under plain =cargo test=; real proofs +
      =LedgerState= + =Transaction::apply= (the cross-contract call and
      the singleton's event for real) under =--release=, on M29 D/E.
- [ ] E. (BLOCKED 2026-09-05 on the same executor as D; the drift gate and the adversarial refusals exist at the attestation level) Tests that come with the type: for every =Pending= slot in a
      contract, a GENERATED round trip (request → sim → settle accepts)
      plus the adversarial suite (each responder → the expected guard)
      plus an unread-field sweep over the ticket (every field read by
      verification or declared unread — =bigR.y=, =recoveryId= today),
      generic over =Env=/=Resp=, wired for the vault's four.
- [X] F. (2026-09-05; tests/is_even_caller.rs carries the mistake log — §9) The idiot-proofing pass, made measurable: write one NEW flow
      from the public API and docs alone (mpc's =submit_is_even=
      caller is the candidate — the shape its real-stack test drives),
      with a log of every point a mistake was POSSIBLE; close each as a
      compile error or an inline-const assert, or record it for dmd as
      a build-time panic with a prescriptive message. Target: the
      hand-maintained invariants per flow drop from §7's eight to
      authorization-with-a-fresh-witness and the business guards, and
      nothing else. The M30 publisher implements =Responder= too and
      passes E's suite byte-for-byte — tracked in M30, noted here so
      the seam is explicit.
Exit criteria: the vault's four pairs on =Pending= at row cost not
above today's, spec harness green on the new lineage; a =compile_fail=
test for every §8.A and §8.C item; =SigNetSim= round trip and
adversarial suite green for all four flows, two speeds; the
notification-path artifact-agreement gate; §8 item 14 confirmed or
struck against the mpc reader; the new flow of rung F written with
zero hand-synced constants, its mistake log closed or handed to dmd.
Ordering: filed after M34 and before M27 as the default; dmd to move
it up if the vault's deployment date approaches, since the caller
record format freezes when the vault deploys, not when the singleton
does.

* M36 — The eDSL trim for human handover: retire ZKIR v2, cut cruft, read as one system (dmd, 2026-09-05)
dmd (2026-09-05): "I want to hand this over to human reviewers, so
making it terser, clearer and removing some cruft is a big plus" —
scope is THE eDSL ONLY (=minocrab=, =minocrab-std=, =minocrab-macros=,
=minocrab-ledger=, =minocrab-zkir=, =minocrab-sim='s stable tier), not
the contracts, harnesses or corpus. Zero movement throughout: every
differential, snapshot and row gate stays green and byte-identical;
nothing here changes an artifact.
- [X] 0. (2026-09-05, notes/edsl-trim.org §0) Inventory before touching: every v2 surface (=minocrab::Compiled=
      and the v2 builder, =minocrab_sim::simulate=, =minocrab_zkir='s v2
      reader/writer, the v2 corpus artifacts and the tests that read
      them) with WHAT DEPENDS ON IT — a table, gate by gate, saying
      which v2 pieces are load-bearing for a v3 claim (a v2 artifact
      that a v3 gate compares against stays) and which exist only to
      test themselves. Same table for duplicated forms (a helper with
      =_under= / =_guarded= / plain triples that nothing calls in one
      of its forms), stale re-exports, and doc comments that narrate
      milestone history ("M9 phase 8, candidate 2") instead of stating
      the rule. Written to notes/edsl-trim.org.
- [X] A. (2026-09-05, four commits on m36-trim; the crates.io patch block stays for midnight-zswap's sake — §A) Retire v2 where the table says it is self-supporting: module,
      tests and corpus readers together, one commit per crate, so a
      reviewer sees each deletion whole. What a v3 gate still needs is
      kept and SAID to be kept, in one place.
- [-] B. (2026-09-05: warning-free build+doc DONE; pure-history narration rewritten in the five densest files, provenance tags kept; the one-spelling audit NOT done — §B) De-cruft the rest: dead forms, stale re-exports, history
      narration in docs reduced to the rule plus a pointer to the
      note that holds the history; one spelling per idea (the
      =Wire3=/=Operand=/=impl Into= forms audited for a single
      recommended one at each site). Terser, not different: public
      signatures on the stable tier do not change — =cargo
      semver-checks= (M34 B) is the gate that says so.
- [-] C. (2026-09-05: READMEs one screen and re-pointed, rustdoc warning-free; M33's inventory pending M33) The reviewer's path: each eDSL crate's README is the entry
      point a human reads first, ≤ one screen, pointing at the three
      files that matter; =cargo doc --no-deps= warning-free; M33's
      inventory updated to the trimmed tree.
Exit criteria: v2 gone or explicitly load-bearing per the table; all
gates green and byte-identical; semver-checks clean on the stable
tier; the READMEs and M33 inventory reflect the trimmed tree.

* M27 — ZKIR semantics in Lean, and the vault's leakage ground layer
(dmd, 2026-08-28.) Reopens the ladder M25 called final — deliberately:
(Numbered M27 at the push-day merge: the review-fixes branch had
already claimed M26 for the review follow-ups above.)
extraction (Aeneas) was reviewed and DEFERRED for proof ergonomics
(notes/lean-port.org §1, dmd's confirmation recorded), and this is the
other route to proofs about the running system: do not extract the Rust
— give the ARTIFACT a semantics, and check facts of the actual =.zkir=
directly. Two prongs that pay for each other: a machine-checked,
corpus-validated semantics of ZKIR in Lean (the layer NOTHING upstream
covers — Compact's Agda says nothing about ZKIR, M25's honest ground),
and its first real consumer — the per-circuit LEAKAGE INVENTORY of the
erc20 vault that the SigNet unlinkability analysis stands on.

THE MOTIVATING DOCUMENT: "Unlinkability of SigNet Cross-Chain System"
(=Midnight_UnlinkabilityAnalysis-4.pdf=, draft read 2026-08-28; held
OUTSIDE the repo like the external review report — it is someone
else's working draft). It proves withdrawer/swapper unlinkability by a
hybrid argument whose reductions land on multi-user PRF security,
commitment hiding, and ZK simulation. THE SPLIT THAT DEFINES THIS
MILESTONE'S SCOPE: that game-level layer is probabilistic relational
reasoning (EasyCrypt/SSProve territory; no mature Lean framework, and
the paper itself defers its crypto assumptions to Midnight's proofs) —
explicitly OUT. But the proof's GROUND LAYER — the §4.12 τ-tables
inventorying, per circuit, every published transcript component and
its classification (identical =f(p⃗)= / challenger randomness / hiding
/ depends on b) — is a set of DETERMINISTIC claims about our own
artifacts, hand-maintained in the draft and dotted with red "check
this" annotations. Machine-checking that layer, drift-gated, is
exactly this repo's kind of work: the hybrid argument stays a paper
proof, and every per-circuit fact it cites becomes a checked artifact.

DECISIONS OF RECORD (dmd, 2026-08-28):
- SCHEDULING: dmd runs this milestone on SATURDAYS AND SUNDAYS with
  the week's leftover tokens. Sessions should therefore leave clean
  per-rung stopping points; nothing in this milestone may block
  weekday work.
- SHELVABLE BY CONSTRUCTION: every rung below has standalone value and
  a hard gate; stopping after any rung is a valid end state, recorded
  as such, not a failure.
- The game-based hybrid proof is OUT (above). No PPT adversaries, no
  probability, no crypto-assumption discharge in Lean.
- Byte-exactness is CORPUS-GATED FOREVER: a Lean parser/printer that
  disagrees with the 788-artifact corpus on one byte is red — the
  reimplemented-serializer hazard is the same one M21 refused to risk
  with vendoring, and the corpus is the instrument that contains it.
- =Fr= is =ZMod p= in the semantics; Montgomery form is a
  SERIALIZATION detail and stays on the parsing side. Intrinsics
  (hashes, curve ops) enter as uninterpreted functions at rung 3 and
  become concrete only at rung 4, ported mechanically, validated
  differentially — never re-derived.
- If the Lean side becomes load-bearing CI, lake/lean enter the M8
  churn perimeter (version-bump surface) — priced then, not now.

ESTIMATE, recorded 2026-08-28 (Fable sessions, this repo's measured
velocity): rungs 1-2 ~1 day, rung 3 ~2 days, rung 4 ~3-4 days with the
intrinsics as the variance driver; the vault prong overlaps rung 3
substantially. Weekend-sized chunks exist at every boundary.

- [X] DESIGN NOTE FIRST (the house method): =notes/zkir-semantics.org=
      — the instruction inventory with per-instruction operand/output
      arity and the consumption rules already decoded (taint.rs, the
      FAB notes), the two semantic readings (evaluation vs constraint)
      and which theorems need which, the intrinsic-function boundary,
      the preimage-dump interface for rung 4's differential, and the
      vault-prong claim list frozen from the analysis draft (so the
      milestone's targets do not drift with the draft).
      (DONE 2026-08-30 — notes/zkir-semantics.org. Decisions recorded
      there: v2 SKIPPED, gate is the 84 v3 corpus files (the "788/66"
      above predates the last corpus refresh: 806 = 722 v2 + 84 v3);
      byte-exactness is against compactc's OWN printer (json.ss
      =print-json-compact= + print-zkir-v3.ss key orders), which forces
      the syntax tree to keep SIGNED immediate literals (=-0x01= occurs
      in 36 files; the Rust writer never emits a minus); =Fr= spelled
      =Fin p= (= =ZMod p=, mathlib-free like the other two packages);
      third lake package at crates/minocrab-zkir/lean/; nine frozen
      vault claims V1-V9 with the rung that discharges each, and two
      grep-level pre-checks flagged for the inventory to settle.)
- [X] RUNG 1 — SYNTAX: the real-IR inductive + parse/print for the
      =.zkir= wire format in Lean. GATE: byte-exact round-trip over
      all 788 corpus artifacts (722 v2 may be SKIPPED by decision —
      v3 is the target; if so, say so and gate on the 84+ v3 files).
      Standalone value: a machine-checked ZKIR grammar with a
      corpus-proven parser — more formal ZKIR coverage than exists
      anywhere upstream.
      (DONE 2026-08-30 — crates/minocrab-zkir/lean/ (=MinocrabZkir=:
      Syntax, Print, Parse + the =zkir-roundtrip= exe). v2 SKIPPED by
      decision; gate = the 84 v3 files, BYTE-EXACT against compactc's
      own printer (json.ss + print-zkir-v3.ss transcribed): "84 ok, 0
      failed, 722 v2 skipped". Mutation-checked (key order, one space,
      =-0x00=, =0X=, trailing spaces all red). Driven from
      =cargo test= (tests/lean_roundtrip.rs, count asserted) and from
      CI's lean job over the tracked corpus + a hand fixture for the
      five corpus-absent ops. notes/zkir-semantics.org §9.)
- [X] RUNG 2 — DATAFLOW: total operands/defines functions over the
      inductive (the Lean twin of =operands_mut= /
      =returned_identifiers=, kernel-checked exhaustive). GATE: agree
      with the Rust functions on every corpus instruction.
      (DONE 2026-08-30 — =MinocrabZkir.Dataflow= (operands / returned /
      reads / defines / guardOf, wildcard-free where it counts) vs the
      new read-only Rust twins in passes.rs, diffed as text over all
      16,815 corpus instructions by minocrab-ir/tests/lean_dataflow.rs;
      green. notes/zkir-semantics.org §9.)
- [X] RUNG 3 — OBSERVABLE SEMANTICS: the environment-passing walk over
      the real IR with intrinsics uninterpreted; observables = the
      PI/witness/output streams. Re-prove the eight pass theorems
      (dedup 4, fold 3+1) OVER THE REAL IR TYPE — closing the pass
      prong's model gap without extraction — and state the
      constraint-side reading (solution sets) beside the
      evaluation-side one, with the completeness lemma (honest
      evaluation satisfies constraints) for the instructions that
      carry both. GATE: lake-green sorry-free; the =VerifiedPass=
      citations move to the real-IR theorems.
      (IN PROGRESS 2026-08-30 — the evaluation reading is transcribed
      and compiles (=MinocrabZkir.Semantics=: =Fr := Fin p=, opaque
      carriers, the =Model= of uninterpreted intrinsics, =step=/=run=
      for all 33 instructions with the four stream cursors and the
      epilogue checks), with seven build-time =#guard= smoke checks
      (=Smoke.lean=) and the corpus proved SSA-well-formed by
      =zkir-wellformed= (84/84, 16,815 instructions). NO theorems yet;
      the constraint reading, completeness lemma and the eight pass
      theorems over the real IR are the remaining gate — of which
      DEDUP's four are DONE the same day (=DedupIr.lean=, the M25
      proofs unchanged on the real =Instr=; =DEDUP_PROOF= re-cited,
      claims test green), and FOLD's three syntactic ones too
      (=FoldIr.lean=: outputs verbatim, skeleton preserved, exactly
      filter-then-substitute), and the SEMANTIC one as well
      (=fold_preserves_observables= over =Instr=, the model's proof
      with the generic instruction packed into a 33-arm
      =mapOperands_congr=; =FOLD_PROOF= now cites =FoldIr.lean=, the
      claims/drift tests green). ALL EIGHT pass theorems are over the
      real IR. DONE 2026-08-30 (night): =Sat= (=Constraint.lean=,
      transcribed from the in-circuit walk) and =completeness=
      (=Completeness.lean=, sorry-free, under =SSA= + the new
      =ImpactsNative= hypothesis — §10 I5); =SatCommitment= stated,
      its completeness deferred to rung 4's Model round-trip. Two of
      the three inferred arms settled against upstream source; one
      (=field= atom width) and the two gadget widths left for rung 4 —
      notes/zkir-semantics.org §9. GATE GREEN; shelve point 3.)
- [X] (2026-09-05, four commits 322b9ac..e4b557e, merged ff7c484 —
      notes/zkir-rung4.org: Poseidon / SHA-256 / Keccak-256 / secp256k1
      concrete under =MinocrabZkir/Intrinsics/=, each validated against
      the Rust digests FIRST (73 known-answer vectors); Jubjub, r1,
      curve25519 left uninterpreted and REFUSED by =Concrete.unsupported=
      (no record exercises them); 40 run records under
      =lean/differential/= (17 vault + 3 singleton + 8 manager honest,
      12 tampered / 11 rejecting) referencing compactc's corpus
      artifacts by path; =lake exe zkir-run= + =tests/lean_run.rs= the
      gate, in CI; green on the first run of the records. Disagreements
      the port surfaced: D1 a bit-reversed scalar caught only by the
      KAT; D2 the Lean TRUNCATED an over-width FAB limb where both
      upstream readings reject — fixed and gated; D3 settles I3; D4
      completeWithdraw's guarded-off witness perturbable with the run
      still accepting — Gap 2 as an executable observation.) RUNG 4 —
      EXECUTABLE SEMANTICS: intrinsics made concrete (=ZMod
      p= arithmetic; keccak/persistent/transient hash and jubjub ops
      ported mechanically), giving a Lean reference interpreter.
      GATE: differential agreement with =IrSource::check= /
      minocrab-sim on the corpus-verified preimage dumps (the bench
      pipeline already produces them) — PI streams equal, accept/
      reject equal. The variance rung; shelve-point before it is
      explicitly fine.
- [-] (2026-09-05: BUILT — tests/leakage_inventory.rs, a static
      dataflow walk over the ONE compat artifact, frozen (1,300 lines,
      17 circuits), three gates green: table frozen, no unlabelled
      witness-dependent public component, corpus twin same surface.
      V1/V2/V3/V5/V7/V9 read off the artifact — notes/zkir-semantics.org
      §7.2. OPEN: the hand-back to the draft's authors (dmd); reqId
      one-time use stays the model's claim, packing stays the lint's.)
      THE VAULT PRONG — the leakage inventory, machine-checked
      against the artifacts of all four vault forks (port as the
      differential anchor; borsh lineage as deployed-shape). From the
      analysis draft's §4/§4.12 tables, the claims to discharge per
      circuit (deposit, completeDeposit, withdraw,
      refundSurrenderedValue, completeWithdraw, refund, swap,
      completeSwap, approveRouter):
      - COMPLETENESS of the inventory: the public statement =x= is
        EXACTLY the listed components — the PI stream contains what
        the table says and NOTHING else (the claim their tables
        assert by hand; ours derives from rung 3's semantics, or
        rung 2's dataflow as the weaker pre-rung-3 form).
      - DEPENDENCY CLASSIFICATION: which emitted components are
        functions of which inputs — in particular, the withdraw
        flow's ONLY vault-emitted =userSk=-dependent component is
        the =refundComm(userSk, reqId)= marker (their τ_mark row),
        with the coin-layer components keyed by =coinSk=/nonces
        (Midnight's layer, blue in the draft) separated out
        mechanically.
      - =keyPath= CONSTANCY: withdraw/swap/approveRouter write the
        LITERAL "vault"; deposit alone writes =userComm(userSk)= —
        the fact their §4.2 table states and the pseudonym analysis
        rides on.
      - =reqId= ONE-TIME USE: =reqCounter= strictly increments per
        request (a ledger state-machine invariant over the op
        semantics), so =refundComm='s input is fresh per request —
        the freshness premise of their Hybrid 2.
      - WITNESS INVENTORIES per circuit, mechanically derived — which
        makes their §5.3 Gap 2 ("a successful withdrawal can be
        settled by ANYONE": completeWithdraw's success branch takes
        no secret witness) a surfaced fact rather than a footnote;
        Gap 1's three-value-event swap shape likewise read off the
        transcript inventory.
      - INJECTIVE PACKING under every commitment/digest in the flow
        (userComm, refundComm, coinCommit, coinNull, attDigest,
        domSep): already mechanized by the taint lint + the M10
        injective-domain-separator work — CITE, do not rebuild; the
        prong's new work is the inventory, not the packing.
      GATE: the inventory is GENERATED from the artifacts and frozen
      like every snapshot (between-markers regen, reviewable diff);
      a circuit edit that changes the leakage surface is a gate
      failure naming the moved component. Deliverable doubles as the
      drift-proof replacement for the draft's hand-maintained tables,
      handed back to its authors.
- [-] OPEN INCONSISTENCIES I1-I4 (notes/zkir-semantics.org §10) each
      closed or discharged: I1 witness classification in V5's
      inventory (DONE 2026-09-05: the =witness= lines of
      tests/leakage_inventory.rs), I2 in the writeup (+ upstream note), I3 at rung 4 (SETTLED
      2026-09-05 in the Lean's favour — notes/zkir-rung4.org D3), I4
      confirmed mechanically. None affects the vault (checked
      2026-08-30 against all four forks' artifacts).
- [X] (2026-09-05: VERIFICATION.md §5, first bullet — "The boundary of
      that layer, stated plainly": ground layer machine-checked; rung 4,
      hiding/hybrids and the running Rust not; authority = corpus
      round-trip + differential agreement with the pinned ir_vm.rs, a
      transcription error the corpus does not exercise invisible to
      both. Cites, does not re-prove — (e) below stays open for the
      upstream PRs.) THE HONEST-BOUNDARY WRITEUP, published where the claims are:
      what is machine-checked (the ground layer), what is not (the
      hybrids, the crypto assumptions), and what the semantics'
      authority rests on (corpus round-trip + differential agreement
      — the same external-warrant epistemics as the popeq rule and
      the M3 harness, stated with the same bluntness).
- [ ] UPSTREAM SPEC WATCH (dmd, 2026-09-04) — two OPEN PRs on
      =midnightntwrk/midnight-zkir= by jmchapman, both opened
      2026-09-04: #16 (a 1,529-line markdown spec of ZKIR v3) and #17
      (a 21,513-line Agda mechanisation: =--safe=, no postulates, the
      trust base one =Assumptions= record; faithfulness both
      directions, statement soundness, extraction uniqueness, and
      DECIDABLE producer obligations O1-O4). THE PIN FACT, checked:
      their pinned =ir_vm.rs= is the SAME git blob (=8ae67349=) as the
      one at our rev =04c9c5d9= and at compactc 0.34.0's — they
      specify exactly the 34-instruction surface our Lean models.
      Full reading: notes/zkir-semantics.org §11. NOT MERGED; the
      items below are CONDITIONAL on merge and are not to be acted on
      before then. CHECK BACK ~2026-09-18 (a week or two, dmd): if
      merged, tick the items in; if still open or changed, re-read §11
      against the new diff before doing anything.
      (a) SPEC DIFF: our 34 =step= arms against their §6.3 instruction
          table and =Semantics.agda=, one row per instruction, deltas
          recorded in §9. A second independent transcription of the
          same Rust; worth more than any port. (Their count is 34;
          our notes say 33 in places — reconcile the count first.)
      (b) CLOSE BY CITATION: I2 (their §4.1 states the Bytes32 decode
          PANIC and calls the intended semantics failure — the
          upstream note we owed is written by upstream); I4 (their
          §6.6(d) + O4 list the same four unenforced premises as our
          §4.3, same =less_than= width formula, same guard
          classification). I5 → an O2 checker (value typing makes
          =ImpactsNative= a corpus fact, not a hypothesis). I3 and I1
          get nothing from them; they stay where they are.
      (c) PRODUCER-OBLIGATION GATE: O2 (value typing) and O3 (bit
          bounds) as checkers over our =Instr=, beside the existing
          =zkir-wellformed= (= their O1), run over the corpus AND all
          200 workspace circuits — "MinoCrab output discharges O1-O3"
          becomes a cargo-test gate. O4 is a witness-shape condition
          and belongs to rung 4's differential, not here.
      (d) RUNG 4 REFRAMED: their §9.1 says comparing the spec to the
          pinned Rust is OUT OF SCOPE by design — our rungs 1-2 are
          the corpus validation their development lacks, and the
          rung-4 differential would validate BOTH. Offer the
          corpus-verified preimage dumps upstream when it lands.
      (e) HONEST-BOUNDARY WRITEUP cites, does not re-prove: their P5
          for the constraint side, statement soundness for "the PI
          stream IS the statement" (the theorem the vault inventory
          rests on). Our =Sat= + =completeness= stay as the MINIMAL
          thing the pass theorems need; not extended.
      (f) STALE-CLAIM SWEEP, on merge: every "nothing upstream covers
          ZKIR" sentence (VERIFICATION.md §5, README, lean-port.org,
          this milestone's preamble) becomes false the day #17 merges.
          The M13 rule applies: a stale published claim is a finding.
      UNAFFECTED either way: the eight pass theorems (nothing in
      either PR is IR-to-IR), the taint lint, the disclosure gate,
      the guard scopes — all above their circuit-layer perimeter.
      HAZARD noted regardless of merge (notes/version-bump.org, pin 5):
      the =zkir= crate now lives in its own repo and their tip already
      carries a NEW instruction batch (=LoadConstant=, =And=/=Or=/=Xor=
      + =Bool=, =Byte=, =Bytes(n)= + =Nth=/=Concat=/=Slice=, =Sha512=,
      stricter decode) that no compactc release uses yet — when one
      does, our bindings, our 34-arm Lean and their Agda all move.
Exit criteria: the ladder climbed to a recorded stopping rung with
every climbed rung's gate green; the pass theorems re-anchored to the
real IR if rung 3 is reached; the vault leakage inventory generated,
frozen, and drift-gated against the four forks' artifacts with every
draft-table claim above either DISCHARGED, corrected (a wrong claim in
the draft is a finding, reported to its authors), or recorded as
blocked with the missing rung named; and the honest-boundary writeup
published. Explicitly OUT: the game-level hybrid argument, any
crypto-assumption discharge, and any new packing analysis duplicating
the taint lint.

* M37 — Typed EVM calls: the call as a type, the slot infers the rest (dmd, 2026-09-05)
dmd: "Higher level constructs that make the ERC-20 transfer more terse …
more type safety … more inference"; against the signature-string macro:
"I'd rather that we have a type somewhere in our system that represents
the input and output, e.g. a tuple and a bool, then that request type is
inferred … macros that don't completely break tooling"; "make it so".
Design of record: notes/evm-calls.org (§1 ABI types, §2 =EvmCall=, §3
the slot, §4 =Owned=, §5 what stays explicit, §6 tooling, §8 gates).
Zero movement throughout: the 17 Pending-lineage circuits byte-identical.
- [ ] A. The ABI type layer + =EvmCall= (notes §1–2): =AbiType= for
      Address / Bool / Bytes32 / U64 / U128 / U256, =AbiTuple= to eight,
      the existing word encoders moved under the trait, the selector
      hashed at build time with =sha3=; the fixture: selectors and words
      of the vault's five calls pinned against compactc's calldata.
- [ ] B. =Pending<Call, Env, WORDS>= (§3): response and kind from the
      call, WORDS checked by inline const, the =Signet= block located by
      the Ledger derive (one per block or a compile error) and threaded
      into each slot, =Contract<Abi>= callee newtype, =Succeeded<Call>= /
      =Failed<Call>= tickets (§3: =complete= asserts success, =refund=
      takes never-executed OR executed-false — closes the spurious
      completion dmd asked about), =complete= / =refund=.
- [ ] C. =Owned<Env>= and the typed commitment tag (§4): the owner
      commitment built on request and opened only on =refund_to_owner=;
      =Commit<T, Tag>=; =compile_fail= gates per §8.
- [ ] D. The vault's Pending lineage re-expressed on A–C: request
      circuits, initialize, approves and claim byte-identical; the eight
      withdraw/swap/supply/redeem settles move DELIBERATELY to the
      Succeeded/Failed split (spec harness re-derived, leakage inventory
      shows no settle consuming an unchecked secret); =FixedGas= / =erc20_call= / the hand
      selectors and word calls retired; the README example compiled as
      a test contract and shown verbatim.
- [ ] E. Tooling audit (§6): =#[circuit]= / =#[contract]= keep the item
      textually intact — a test on the expansion.
Exit criteria: A–D green and byte-identical; the README's cross-chain
section is compiled code; notes/evm-calls.org as-built per rung.

