// This file is part of Compact.
// Copyright (C) 2025 Midnight Foundation
// SPDX-License-Identifier: Apache-2.0
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//  	http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// test1: const with anonymous function
export circuit test1(b: Boolean): Field {
  const p = (b) => { if(b) { return 1; } return 2; }(true), d = () => { return 2; }();

  return p as Field;
}

// test2: const in empty loop
export ledger var_2a: Field;

export circuit test2(): Field {
  for (const a of 0..0) {
    const b = (a as Uint<16>) * (a as Uint<16>), c = (() => { return a; }());
    var_2a = (b * b) as Field;
  }

  return 1 as Field;
}

// test3: for with vector size 0
export ledger var_3a: Field;

export circuit test3(): Field {

  for (const a of default<Vector<0, Field>>) {
    // casting unknown
    const b = (a as Uint<16>) * (a as Uint<16>), c = (() => { return a; }());
    var_3a = (b * b) as Field;

    const [] = [];
  }

  return (1 as Boolean || () => { const a = 1, b = 2; return true; }()) as Field;
}

// test4: struct with ifs
struct var_4 {
  size: Uint<16>
}

export circuit test4(): Field {
  const b = var_4 { size: 1 };

  if (b.size >= 0) { const a = 1, b = 2, c = 3; }
  else { const a = 1, b = 2, c = 3; }

  return 1 as Field;
}

// test5: single const in code block
export circuit test5(): [] {
    {
        const a = 100 as Field;
    }
}

// test6: single const in for
export circuit test6(): [] {
    for (const i of 1..10) {
        const temp = i * 2 as Field;
    }
}

// test7: const with anonymous function in shorthand if
export circuit test7(b: Boolean): [] {
  const a = (disclose(b) ? () => { const b = 1; return b; }() : () => { const c = 2; return c; }());
}
