#+title: Borsh (fixed-width subset): serialization design of record

FRAMING SETTLED by dmd 2026-08-15: this IS BORSH — restricted to the
fixed-width subset — and must be documented as A SUBSET, NOT A
SEPARATE FORMAT, in the code and the spec. Every byte emitted is
valid canonical Borsh for the declared types; any Borsh
implementation (borsh-js on the TS side included) parses it with the
same type declarations; nothing is redefined. The subset rule:
every circuit-visible type has a fixed width (fixed-width ints,
[u8;N], structs, Tag<K> fieldless enums; Flagged<T> — a bool tag +
always-present payload, an ordinary Borsh struct — where optionality
is needed; no Vec/String/Option/data-enums in circuit-visible
positions). Rationale kept from the discussion: the always-max-size
cost of Flagged is CIRCUIT physics (both arms always cost rows —
measured on refund), so wire elision (native Option) would only save
off-chain bytes while making offsets data-dependent; formats with
native fixed-offset optionals (Cap'n Proto, FIDL) pay word-alignment
padding + pointer machinery = keccak rows. For this subset,
serde+bincode-fixint emits byte-identical output, so spec types
derive BOTH borsh and serde: dmd gets serde-generated native ser/de,
and the conformance property is a DUAL oracle (in_circuit ==
borsh::to_vec == bincode-fixint). The generated offset tables also
permit a dependency-free DataView reader.
MPC status: source now available at ~/mpc (cloned 2026-08-15); dmd:
"we can change anything if we want, we just need to document it" —
the open questions below are being answered from source rather than
treated as external unknowns; MPC-side changes are in scope with
documentation as the requirement.

dmd directive 2026-08-15 (FAB concerns documented in
~/Documents/Midnight/FAB{,-issues,-findings}.md, filed upstream as
compact#709): move event payloads + attestation parsing to a terse
format with minimal branching in ZKIR — Borsh — reusing the borsh
ecosystem, serde-like derives. MPC update is in scope (Sig.Network owns
it). Option MAY branch, minimally. Design surveyed by Opus agent
2026-08-15; refinements from dmd folded in.

* The three findings that reshape the plan
1. FOR THE FIXED-WIDTH SUBSET, MIDNIGHT'S HASHED FAB *IS* BORSH,
   byte-for-byte. persistentHash/keccak256 hash binary_repr, which for
   bytes<n> atoms is raw bytes zero-padded to n — a sequence of
   bytes<w_i> atoms hashes as the plain concatenation of fixed-width LE
   fields = Borsh's struct rule. The vault record's atoms are ALL
   bytes<n> (no field/compress/option segments; Compact Maybe is a
   struct with an always-present payload). So keccak256(record) TODAY
   already equals keccak256(borsh::to_vec(record)) for the matching
   Rust struct; same for the attestation digest (= borsh of
   {[u8;32], [u8;N]}) and the singleton's Misc payloads. The
   deliverable is mostly A SPECIFICATION AND A TEST ORACLE — shippable
   for the already-deployed pinned interfaces too.
2. THE IN-CIRCUIT ENCODER IS FREE ON THE HASH PATH. ZKIR's
   Keccak256/PersistentHash do the byte packing in-chip (~0.25
   rows/byte), and their per-limb byte decomposition ENFORCES the
   range constraints that make the encoding injective. Choosing atom
   widths = Borsh widths gets Borsh encoding for zero extra rows, with
   the soundness backbone free.
3. THE STACK CANNOT BRANCH ANYWAY: in-circuit decoding of FAB option
   segments is a hard unimplemented!() error in zkir-v3 (ir_vm.rs:
   114-119). The circuit-safe subset is forced, not chosen.
VERDICT on "small performance hit acceptable": there is no hit.
Attested-output changes cost +2..+6 rows per settle circuit; the
(future-work) record change is −4,218 rows and crosses swap to k15 —
the boundary M10 missed by 51 rows.

* The circuit-safe Borsh subset
Included (every offset a compile-time constant; LEN an associated
const; instruction stream value-independent — checkable via
borsh::object_length(&v) == T::LEN ∀v):
u8..u128 → Uint<8..128> (LE, assert_bits 8w) · bool → Bool
(assert_boolean — NEW check vs today) · [u8;N≤31] → Bytes<N> ·
[u8;32] → B32 · [u8;N>31] → BytesN · [T;K] element-wise · fieldless
enum(K) → Tag<K> (1 byte, assert tag<K) · struct = declaration-order
concatenation.
Excluded, with replacements:
- Vec/String/maps: u32 length prefix = value-dependent layout. Use
  [T;K] + separate count field (the record already does: noWords).
- Option<T>: Borsh omits the payload on None. Use
  struct Flagged<T> { is_some: bool, value: T } — always-present
  payload, which IS what Compact's Maybe compiles to. Maybe ↦ Flagged,
  NOT Option — the single most important line in the spec for the TS
  side. (dmd: minimal branching OK — Flagged is one constrained tag +
  selects, offsets stay static, vanilla-borsh compatible.)
- Data-carrying enums: distinct record types per kind (VaultEvent vs
  SwapEvent already are), or {tag, widest-payload} if ever needed.
- Uint<BITS> for BITS ∉ {8,16,32,64,128}: width must be a Borsh
  primitive; narrower RANGE is a range constraint. Compile error via
  inline const assert.
- Trailing zero padding is NOT Borsh: where a fixed envelope demands
  it (288-byte Misc), the spec states "bytes 0..LEN Borsh, LEN..N MUST
  be zero" — checked in-circuit (test_eq per pad limb) and by the
  decoder rule (slice to LEN, assert tail).
Protocol additions recommended: a 1-byte response-kind discriminant at
offset 0 of every attested output (makes cross-circuit attestation
replay structurally impossible; today claim/completeWithdraw share a
digest shape split only by which map holds the id); replace the two
in-band ABI-JSON schema strings on the record (68/75 bytes) with that
kind tag — this is what buys swap's k15.

* Trait + derive (crates/minocrab-std/src/v3/borsh.rs + minocrab-macros)
CircuitBorsh<V>: CircuitArg — strict extension; one derive yields
circuit args, hash-preimage alignment, packed serialization,
deserialization, range constraints, exported layout:
  const LEN; push_limbs (→ alignment-aware hash instrs, the ZERO-COST
  path); push_segments (→ Serializer, the packed path for logs);
  constrain_canonical (booleanity, tag<K, pad==0 — the checks Borsh's
  decoder makes that Compact's does not); read(c, &mut BorshReader);
  layout(&mut LayoutPath, &mut Vec<FieldSpec>) — drives spec doc +
  vectors + schema cross-check, never codegen.
Free fns: alignment_of / limbs_of (0 rows) / to_bytes (packing cost).
Derive #[derive(CircuitBorsh)] + #[borsh(spec = path)]: the spec type
is a SECOND plain struct with borsh's own derives (the oracle must be
independent of the circuit layout logic or it tests itself); the
attribute only generates a test walking borsh's BorshSchemaContainer
into FieldSpecs and asserting equality with layout(). M9 thinness
gates apply verbatim.

* The deserializer — designed, deliberately off the critical path
BorshReader<V> two modes: Split (M7 pass-2 backwards, one div_mod
(143 rows) per field boundary, fields stay Public) and WitnessCheck
(witness each leaf + constrain_bits + re-pack + assert limb equality;
~2w rows per leaf, 4-70× cheaper; leaves are Private → ledger use
needs disclose). BUT the vault doesn't need either: the attested
output arrives as typed circuit ARGUMENTS and the MPC signs
keccak(requestId ‖ bytes) — declaring fields + running the SERIALIZER
forwards is a complete sound deserialization. The four-line theorem:
packing injective on range-constrained inputs (disjoint powers of
256); MPC signed the digest; circuit constrains bytes = serialize
(fields), fields in range; ∴ fields = what the MPC encoded. Range
constraints enforced twice (constrain_input + the hash chip's own
decomposition). Reader gets built as a tested library component
(round-trip property; future packed state), not a vault dependency.
Deferred: witnessed-straddle writer optimization (only pays for
Private serializations; log payloads are Public).

* Site classification
(a) NOW — attested outputs (new erc20_vault_borsh artifact, forked
byte-identically from erc20_vault_opt; compat ports stay FAB and
differential-green; M10's measured ladder not reopened):
claim/completeWithdraw: Bytes<1> → {kind: Tag<4>, success: bool}, +4
rows each; refund: 5-byte sentinel → kind tag or constant compare, 0;
completeSwap: its 8-byte output is ALREADY a Borsh u64 — add kind
byte, +2. THE 0x02 HAZARD CLOSES: today any byte ≠ 1 routes
completeWithdraw to the refund branch (re-mint on a SUCCESSFUL
withdrawal — the M10 harness finding); Borsh bool is 0|1 and nothing
else; assert_boolean is the same rule. Parsing correctness made
concrete, 2 rows.
(a) NOW — event payloads: ZERO circuit change. The ledger never parses
a Misc payload (Log carries an opaque StateValue), and the deployed
singleton's payloads are already subset-conformant. Deliverable = spec
+ golden vectors for the TS side, immediately.
(b) PINNED, untouched: Impact/popeq framing, comm commitment,
tokenType, coin commitments, the singleton's calldata FAB framing
(payload bytes inside it are already conformant), all compat ports.
(c) FUTURE WORK WITH A NUMBER: the request record. Drop the two
in-band schema strings → kind tag: swap record 571→497 bytes, 5→4
keccak blocks, −4,218 rows → swap (opt) 32,819 → ~28,601 = K15 with
4,167 rows of margin. deposit/approveRouter/withdraw shrink but don't
cross. RULE: state stays FAB (per-field alignment = free array reads
in settle circuits — a packed state buffer would cost ~1,700
rows/settle to read back); only the HASH PREIMAGE + log payload are
Borsh (free, since hashed-FAB ≡ Borsh here). Better: emit the record
as a Misc log payload = the same bytes as the requestId preimage; the
MPC never parses FAB at all.

* borsh ecosystem reuse
borsh 1.6.1 (+derive, unstable__schema) as DEV-dependency of
minocrab-std + minocrab-contracts only (same posture as
midnight-ledger dev-deps; nothing shipping links it). Buys: P1 oracle
(in-circuit bytes == borsh::to_vec, via simulate on per-type encode
circuits — tests the CIRCUIT, not a Rust re-derivation); independence
(spec types use borsh's own derives); BorshSchema drift alarm
(generated test per type); object_length as the mechanical no-branching
proof; golden vectors (spec/vectors/*.json) shared by borsh-js/go/
Rust-MPC. borsh-derive does NOT drive our codegen (io::Write-shaped,
can't see wires); two definitions + generated cross-check is the
honest shape.

* Correctness
Art::Borsh third artifact; ARTS: [Art;3]; concretization arms in
prims.rs are the executable deviation log; 9M-case gate becomes ~78
min. Properties P1-P6 (oracle equality, fixed-length, schema match,
bijectivity, reject-agreement, digest match). Malformed classes are
FINITE: wrong length (off-chain reject; in-circuit it's a different
preimage → signature fails — documented asymmetry), out-of-range leaf
(assert_bits + hash chip), bool ∉ {0,1}, tag ≥ K, non-zero pad.
Duplicate encodings: NONE EXIST in the subset (vs FAB-findings §4's
live canonicity gaps). The port↔borsh injectivity sweep will FAIL on
0x02 and force the deviation-log entry — the harness working as
designed.

* Alternatives (settled — do not re-litigate)
postcard/bincode-default: varints = value-dependent layout, out.
Protobuf: varints+tags, out. SSZ: close, but variable-size head/tail
offset tables + merkleization baggage + consensus-flavoured Rust
ecosystem. EVM ABI static: fixed offsets + native for the MPC, but
~3× bytes and bytes are keccak rows. BORSH CONFIRMED: bijective by
construction, subset byte-identical to what our Serializer emits AND
what binary_repr hashes, clean Rust impl + schema, TS/JS/Go/Python
parsers.

* Phasing (one agent per stage; usual gates + notes as-built per stage)
0. Instruments + oracle, NO circuit changes: borsh dev-dep; spec types
   for the DEPLOYED payloads (singleton Misc ×3, both records, four
   attested outputs, digest preimage); byte-equality tests vs corpus
   artifacts. Deliverable: "today's protocol is already Borsh", with a
   test behind it. Shippable to Sig.Network alone.
1. v3::borsh core, hand impls (CircuitBorsh, leaves, alignment_of/
   limbs_of/to_bytes, constrain_canonical, layout). ZKIR byte-equality
   + P1-P4 per leaf.
2. BorshReader both modes + cryptocost-style measured per-leaf costs.
3. #[derive(CircuitBorsh)] + #[borsh(spec)] cross-check test.
4. erc20_vault_borsh byte-identical fork; Art::Borsh; zero movement
   asserted.
5. Attested outputs → Borsh (four settle circuits, +2..+6 rows,
   deviation-log D6 for the 0x02 semantics + kind byte).
6. Borsh event payload twins (events_borsh/xcontract_events_borsh)
   beside the pinned originals.
7. THE RECORD (gated on dmd Q1/Q6): schemas → kind tag; expect swap
   k16→k15; re-run 9M gate; refresh BENCHMARK.md (a k moves).
8. spec/borsh-subset.md (grammar, leaf table, reject rules, padding
   rule, Maybe↦Flagged, per-record offset tables GENERATED from
   layout()) + spec/vectors/*.json via --ignored regenerator. The
   artifact the TS/MPC side implements against.
Stages 0+8 have external value and no dependency on 1-7 — front-run
them if the TS side is on the critical path.

* As built — stage 0 (2026-08-15)
"Today's protocol is already this layout", with a test behind it. NO
circuit changes, nothing under src/ touched; row_snapshot and
interface_snapshot untouched and green.

** THE HEADLINE: every deployed payload is byte-identical, zero divergences
| deployed payload            | shape                | verified against                          |
|-----------------------------+----------------------+-------------------------------------------|
| vault record <2,34,34>      | 404 B                | FAB binary_repr under the SHIPPING atoms  |
| swap record <7,38,37>       | 571 B                | ditto (571 = the number stage 7 assumes)  |
| requestId (both records)    | keccak of the above  | model's request_id(), M10-differential-pinned |
| claim digest preimage       | 32+1 B               | calculateSignetAttestationDigest's alignment |
| completeWithdraw preimage   | 32+1 B               | ditto                                     |
| refund preimage             | 32+5 B               | ditto                                     |
| completeSwap preimage       | 32+8 B (a borsh u64) | ditto                                     |
| signBidirectional Misc      | 1+32+128 B ‖ pad     | THE CORPUS ARTIFACT accepts/rejects       |
| respond Misc                | 4×32+1 B ‖ pad       | ditto                                     |
| respondBidirectional Misc   | ditto                | ditto                                     |
For the records the equality is asserted TWICE per case, once against
borsh::to_vec and once against bincode-fixint, and then closed with
=keccak256(borsh(record)) == the reference model's request id= — i.e.
the request id the deployed vault stores IS the keccak of the flat
layout. For the singleton the bytes are not compared against a second
Rust construction at all: the borsh envelope is handed to the pinned
compactc artifact as the logged transcript, which ACCEPTS it, and
rejects it as soon as any one of the 288 bytes moves — including a
byte of the zero pad, so the padding rule is already enforced
in-circuit today. Nothing was papered over: no case, at any case
count, produced a mismatch.

** Files
- =crates/minocrab-contracts/tests/serialization/spec_types.rs= — the
  declarations (deriving borsh's traits AND serde's), the leaf table as
  code (=FixedLen=), =Flagged=, =ByteArray<N>=.
- =.../serialization/oracle.rs= — the two encoders, and the
  BorshSchemaContainer walk into (path, kind, offset, width) rows.
- =.../serialization/deployed.rs= — the deployed bytes, built with the
  protocol's own code (parse_field_repr + binary_repr, the Misc
  envelope, the singleton's proof preimage).
- =.../serialization/records.rs= — model scenario ↦ spec value, field
  by field. Deliberately a SECOND statement of each record: neither
  side is computed from the other.
- =crates/minocrab-contracts/tests/serialization_conformance.rs= — the
  15 tests + the frozen 73-row layout table and its
  =print_layout_snapshot= regenerator.

** Dependencies: dev-only, 8 new lock entries, none shipping
borsh 1.6.1 (+derive, +unstable__schema) and bincode 2 are
dev-dependencies of minocrab-contracts only. =cargo tree -i borsh=
reports exactly one path, marked [dev-dependencies] → minocrab-contracts.
Lock diff (8 packages, all NEW, all reachable only from borsh):
borsh 1.6.1, borsh-derive 1.6.1, and borsh-derive's proc-macro chain
proc-macro-crate 3.5.0 → toml_edit 0.25.13 → toml_datetime, toml_parser,
winnow 1.0.4, indexmap 2.14.0. Only =borsh= itself is linked into the
test binary; the other seven are build-time.
BINCODE 2, NOT 1.3: bincode 2.0.1 is ALREADY in the lock — the pinned
midnight-zk-stdlib depends on it, with the =serde= feature already
enabled — so the second oracle costs ZERO new lock entries and enables
no new feature, where bincode 1.3 would have added a whole second major
version of the crate. (Honest note: this means bincode is already
linked by shipping code, via Midnight's own stack; our dev-dep adds no
linkage that was not there before. borsh is the one that is strictly
dev-only.) The fixint mode is spelled out at the call site rather than
taken from =config::legacy()=:
=standard().with_fixed_int_encoding().with_little_endian().with_no_limit()=.

** Two corrections to the design of record
1. THE DUAL ORACLE IS NOT, BY ITSELF, THE SUBSET CHECKER. It catches a
   fieldless enum (borsh: 1-byte discriminant; bincode-fixint: 4-byte
   u32 variant index) and Vec/String (borsh: u32 length prefix;
   bincode-fixint: u64). It does NOT catch Option: both encoders write
   a 1-byte tag and then the payload, so they AGREE byte for byte on
   =Some(9u8)= and on =None=. Option is caught by the FIXED-WIDTH
   property (object_length 2 vs 1) instead. Both properties are
   therefore load-bearing; the three shapes are pinned as negative
   controls in =mod subset_boundary= so the suite can never be vacuous.
2. THE LEAF TABLE'S "fieldless enum(K) → Tag<K>" DOES NOT SURVIVE INTO
   THE SPEC TYPES. A Rust enum is outside the dual-oracle subset by (1),
   so every deployed tag (algo, dest, txParamType) is declared =u8=,
   which is what Compact's Tag already is on the wire. The circuit side
   may still call it Tag<K> and range-check tag < K; the SPEC type says
   u8.

** Smaller findings
- serde's blanket =[T; N]= impls stop at N = 32, and borsh's derived
  BorshSchema drops const-generic parameters from the declaration (two
  =ByteArray<N>= of different N in one container would collide on the
  name and trip borsh's own redefinition assert). Hence
  =ByteArray<N>=: BorshSerialize/Deserialize derived, Serialize written
  as serde's own array impl verbatim (serialize_tuple(N) + N elements),
  BorshSchema delegated to =[u8; N]=. Transparency is pinned by
  =byte_array_wrapper_is_transparent= at N ∈ {1,2,5,20,31,32}, where
  the native impls exist to compare against.
- The deployed claim/completeWithdraw attested output is a =u8=, not a
  bool — any byte is accepted today (the 0x02 hazard). The spec type
  says u8 deliberately; stage 5 is what makes it a bool.
- =Alignment::bin_len= of the shipping atom lists equals each spec
  type's LEN, so the constants are the deployed shapes' own widths, not
  our arithmetic: vault 404, swap 571.
- Each record's FAB limb count is asserted against the shipping
  =SignBidirectionalEvent::LIMBS= (33 and 43) per case, so a shape
  change cannot slip past by moving both sides.

** Case counts and timing
Default =PROPTEST_CASES= is vault::gen's 48 (the suite reuses M10's
generation strategies and config, so one knob scales both). 15 tests,
1.15 s at the default. Elevated gate: PROPTEST_CASES=20000 → 20,000
cases per property (records ×3 shapes/case, attested outputs ×4
circuits/case, singleton Misc ×288-position tamper sweep, both
artifacts simulated per case) in 244 s. =cargo test --workspace
--release= fully green.

** Seed of stage 8
=LAYOUT_SNAPSHOT= is 73 frozen (type, path, kind, offset, width) rows
walked out of =borsh::schema_container_of=, covering all twelve spec
types — the generated offset tables stage 8 publishes, already
machine-readable. Notable offsets for the TS side: vault record
tx_params.to at 196, calldata.is_some at 232, words[0] at 239,
caip2_id at 304, schemas at 336/370; swap record words[0..7] at
239..463, schemas at 496/534.

* ANSWERED from MPC source (2026-08-15, ~/mpc; supersedes the open
questions below — kept for history)
Ingest path: chain-signatures/chain-midnight/ (PR #1059) — full
read-only Midnight integration; hand-written FAB atom cursor over
midnight_contractState RPC via the real ledger crates. NO publish
path: MidnightPublisher::publish_signature bails (cli/mod.rs:537-542)
— nothing has ever settled on Midnight end-to-end.
- Q1 MPC VERIFIES requestId == keccak256(record): YES —
  resolve_verified_record (reader.rs:177-211) recomputes and DROPS on
  mismatch ("rid-mismatch"). Stage 7 = coordinated MPC release. Their
  binary_repr (request_id.rs:16-33) is ALREADY the flat-LE subset
  (le_bytes ints, Maybe as bool+always-present payload, zero-filled
  slots hashed) — independent confirmation of finding #1.
- Q2 rollout: response FORMAT is keyed per source Chain, NOT per
  contract (primitives/src/chain.rs:66-74: Midnight => Abi, with a
  TODO saying it may move). But the response KEY is already
  per-caller-address (sender_string = hex(caller addr) → epsilon), so
  per-deployment format follows the same seam if ever needed. LIKELY
  MOOT: since the publisher bails, no old vault ever settled — stage 5
  is GREENFIELD, flip Midnight => Borsh unconditionally (confirm with
  dmd that no Midnight deployment is live-settling).
- Q3 0x02 emission: closed for "bool" schemas (every path launders
  through a Rust bool; borsh bool writes 0|1; ABI writes canonical
  word). Would reopen if a schema said bytes1. assert_boolean is
  defence-in-depth. THREE LIVE HAZARDS FOUND INSTEAD:
  (a) Chain::Midnight => Abi contradicts the vault's Bytes<1>/<5>/<8>
  outputs — ABI bool = 32-byte word; nothing would settle. Latent
  only because the publisher doesn't exist.
  (b) Failure sentinel: Borsh mode emits 0xdeadbeef01 (matches
  refund's Bytes<5>); ABI mode emits 36 bytes (never verifies).
  (c) serialize_dynsol WIDTH BUG (respond_bidirectional.rs:215-252):
  width taken from the DECODED value not the respond schema, and
  alloy's size is BITS treated as bytes — uint256-decoded swap amount
  → 32 bytes where the circuit demands 8 LE; in today's ABI mode the
  type-check fails outright → every successful swap resolves as
  failure/refund. write_u256/write_i256 have ZERO test coverage. A
  real bug in their tree regardless of minocrab.
- Q7 digest vs requestId paths: fully independent (different crates,
  no shared code) — stages 5 and 7 ship in either order.
- Q4 (version byte): their decoder fails closed on unknown shapes
  (rejects leftover atoms) but silently ("record-undecodable") — a
  format_version byte turns that into a named drop. Recommend yes.
- Key derivation: path = hex::encode(record.path), full-width, never
  trimmed; requester = the READ address (asserted == record.sender).
  Confirms the Q4-userCommitment stranding hazard and the M10
  structural argument. Replay: indexer = parent-block key diff, no
  request-id dedup (SignetMapKey.count makes re-notify a fresh key) —
  request-id uniqueness is protocol-load-bearing.
- Security TODO in their tree: notification caller_address is
  producer-supplied and unchecked vs the actual xcall initiator
  (indexer.rs:344-353) — third-party triggering, not forgery.
MPC-side work orders (documented per dmd's "change anything, document
it"): STAGE 5 = implement MidnightPublisher (blocking, needed
regardless of format) → flip Midnight=>Borsh (1 line) → fix
serialize_dynsol width from the RESPOND schema (+tests) → add the
kind byte. STAGE 7 = records.rs kind field; reader.rs constants
22→21/3→2 + uint<u8> read; request_id.rs tail; convert.rs kind→
(ABI types, response shape) lookup table (keeps chain-ethereum
untouched); recapture fixtures per their fixtures/README.md
discipline; format_version byte recommended.

* Open questions for dmd (gate stages 5/7)
1. Does the MPC verify requestId == keccak256(record)? (M10 UNSURE Q2;
   now decisive: yes → stage 7 needs a coordinated MPC release; no →
   unilateral. Worth 4,218 rows + swap k15.)
2. Rollout: confirm the MPC keys response format per target contract
   (borsh vault is a separate deployment; old vault keeps old format).
3. Can anything emit a response byte ∉ {0,1} today? After stage 5 it
   makes the tx unprovable instead of silently refunding — intended,
   but must not surprise a running service.
4. Format-version byte? Recommend: yes in record/log payload (1 byte,
   0 rows), no in attested outputs (kind tag suffices; signed digest).
5. TS decoder MUST use fixed arrays ([[u8;32]; N] + noWords: u16),
   never Vec (u32 prefix would break byte equality).
6. Logs or state for the indexer? Recommend both: FAB in state for the
   chain, Borsh in the log for the indexer, log bytes = requestId
   preimage.
7. Confirm the attestation digest (stage 5) and the requestId (stage
   7) preimages can move independently in the MPC's code; if coupled,
   re-cut the phasing.
