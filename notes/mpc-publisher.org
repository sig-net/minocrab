#+TITLE: The MPC's Midnight publisher: what the Rust stack replaces, and what it must prove first
#+DATE: 2026-09-02
#+STARTUP: overview

Survey taken 2026-09-02 against =sig-net/mpc= (worktree of
=origin/dry/midnight-publisher-ts= at 1f99fd78, 2026-08-29, which merges
develop) and this repo at fff97c0. Feeds M29 and M30. dmd's framing:
the signet contract is NOT deployed yet, so the deployment is the
minocrab-optimised artifact, and the engineers do not want a "proof
server" process on the MPC.

* 1. What exists on the MPC side

No single Midnight branch merged; the work has landed on =develop=
through PRs since #1059 (#1075 Chain::Midnight, #1129 the stdio
publisher, #1113 the Rust response publisher, #1142 the intent client,
#1189 FAB responses). Unmerged: =feat/chain-midnight=,
=feat/midnight-publisher=, =feat/midnight-respond-intent-seam= (all
July), and =dry/midnight-publisher-ts= (a refactor over develop).

Rust, =chain-signatures/chain-midnight= (8.4k lines): indexer, reader
(decodes signBidirectional records and response events from block
bodies over subxt HTTP RPC — =midnight_block=, =midnight_block_body=,
=midnight_block_events=), EVM tx assembly, the publisher
(=publisher.rs=, 256 non-test lines, talks to an =IntentClient= trait),
and =intent_gen.rs= (643 non-test lines), the NDJSON-over-stdio client
of the sidecar.

TypeScript, =chain-signatures/midnight-publisher-ts= (920 src, 1343
test, 486 devtools lines). Node 22 child of the Rust node; three ops:
- =ready=: protocol handshake, starts the funding-wallet sync.
- =build=: runs compactc's generated JS executor
  (=@sig-net/midnight-contract-deploy='s =signetContractCompiledContract=)
  for =respond= / =respondBidirectional= on the contract state Rust
  pinned at a finalized hash; checks the deployed verifier key's hash
  against the package's =expectedVk=; wraps the call in
  =ContractCallPrototype= (ledger-v9 WASM) with
  =encodeContractKeyLocation({address, circuitId, verifierKeyHash})=;
  returns =Intent.new(ttl).addCall(..).serialize()=.
- =submit=: =Transaction.fromParts(networkId, none, none, intent)=;
  =transaction.prove(provider, initialCostModel)= where the provider
  POSTs =/check= and =/prove= to the proof server with
  =keys/<circuit>.prover=, =keys/<circuit>.verifier=,
  =zkir/<circuit>.bzkir= read from the package's managed dir; then the
  wallet-SDK facade =balanceUnboundTransaction(tx, {tokenKindsToBalance:
  ["dust"]})=, =signRecipe=, =finalizeRecipe=, =submitTransaction=. The
  facade needs the INDEXER (dust UTXOs are derived by replaying ledger
  events). One submit at a time (single DUST UTXO); 360 s absolute
  budget; four retry classes (=wallet_unsynced=, =proving_timeout=,
  =state_conflict=, =ambiguous_submit=; duplicates declared acceptable).

Its own justification (=intent.ts= header): "the one step that cannot
move to Rust: the compiled contract's bindings and executor are
JavaScript." That is the claim this repo makes false.

* 2. Piece by piece against the Rust stack

| sidecar step        | Rust today                                                          | status   |
|---------------------+---------------------------------------------------------------------+----------|
| transcript          | =signet_contract::respond= etc.; call-compatible with the corpus      | covered  |
|                     | artifacts (tests/signet_contract_differential.rs, 6 tests, re-run     |          |
|                     | green 2026-09-02); emit-only, no witnesses, no state reads            |          |
| intent / tx         | =midnight_ledger::construct::{PreTranscript, partition_transcripts,   | covered  |
|                     | ContractCallPrototype}=, =Intent::new=, =Transaction::new= — the      |          |
|                     | burn well-formedness test does exactly this                          |          |
| proving             | =Zkir::prove= on =IrSource= (minocrab-bench keygen+prove+verify       | covered  |
|                     | in-process); ledger =test_utilities= has =LocalProvingProvider=-based |          |
|                     | =CombinedProofProvider= and an HTTP =ProofServerProvider=            |          |
| DUST fee balancing  | =ledger::dust::DustLocalState= {replay_events, spend, process_ttls,   | GAP      |
|                     | wallet_balance}, =Transaction::balance=, =seal=; test_utilities'      |          |
|                     | =balance_tx= shows the calls. No wallet around it anywhere in Rust    |          |
| sign                | =Intent::sign= / =add_signature= (construct.rs), =data_to_sign=        | covered  |
| submit              | subxt extrinsic; mpc already holds an =OnlineClient=. Pallet call     | to write |
|                     | name to be verified against midnight-node                             |          |

Proving is a plain async Rust function. zkir-v3, transient-crypto and
base-crypto never touch =std::process=; parallelism is rayon threads in
the caller's process. Midnight's "proof server" is an HTTP wrapper
(=proof-server= crate, tokio worker pool) around the same function,
for JS/browser clients. The one external touch is the KZG params
(=MidnightDataProvider=: fetched from =srs.midnight.network= on first
use, SHA-256 checked against compiled-in hashes, cached at =$MIDNIGHT_PP=
/ =$XDG_CACHE_HOME/midnight/zk-params= / =~/.cache/midnight/zk-params=;
=MIDNIGHT_PARAM_SOURCE= overrides the host). Ship the params in the
image and no network access is needed.

Memory is k-driven (BENCHMARK.md, single process, median of 3):
| respond           |  k |   rows | prove  | peak RSS |
| compactc artifact | 16 | 40,931 | ~3.5 s | ~855 MB  |
| minocrab artifact | 10 |  1,004 | ~0.12 s|  ~46 MB  |

* 3. Using the minocrab ZKIR with the sidecar UNCHANGED

Possible, and it is the tested combination. The sidecar never
interprets the IR: compactc's JS executor produces args + transcript,
the ledger's =construct_proof= (=ledger/src/construct.rs:515-573=)
turns them into the =ProofPreimage= (=inputs= = field vec of the
arguments, empty =private_transcript= for a witness-free circuit,
=public_transcript_inputs= = op field_repr, comm_comm from the
prototype's randomness, =binding_input= 0), and whatever IR sits at
=zkir/<circuit>.bzkir= proves it. Call-compatibility (ledger-abi.org §6:
same preimage → identical =pis= and =pi_skips= on both IRs,
=IrSource::check= accepts both, tamper twins reject) is exactly "our IR
accepts the executor's preimage".

Changes, none in the mpc repo:
1. Artifacts: =minocrab_zkir::v3::write_zkir= → =.zkir= JSON in compactc's
   on-disk form → the pinned =zkir-v3= binary (the flake's, the same
   tool compactc invokes; =compile-many= does keygen) → =.bzkir= +
   prover/verifier keys.
2. =@sig-net/midnight-contract= ships them under =keys/= and =zkir/=
   with the circuit names; regenerate =expectedVk=.
3. Deploy tooling reads the verifier keys from the same managed dir, so
   a fresh deployment picks them up. Nothing to migrate.

* 4. How well tested, honestly

- Transcript: 6 differential tests (3 circuits match the corpus; event
  name swap, signature tamper, event tamper rejected). PI-level, plus
  =IrSource::check= on both IRs. NO real proof in =cargo test=; only the
  bench harness proves, and it proves each side's OWN artifact. "Our
  preimage proves under our key" is inferred from =check=, not shown.
- Intent/tx assembly: 2 tests, a different call shape (burn spend
  claim), proofs/signatures/balancing/limits all OFF. Nothing here has
  built a respond intent, proven it, or applied it to a =LedgerState=.
- DUST: zero code here. Upstream: 4 tests in =ledger/tests/dust.rs= and
  the test_utilities balancer (feature flag warns it relaxes some
  well-formedness).
- mpc: 41 vitest cases, submit/wallet almost entirely mocked (13/16
  mocks); 9 Rust publisher tests over stub reads + stub client; ONE
  real-stack integration test (=midnight_stream::
  midnight_to_ethereum_to_midnight_consumes_caller_response=, ignored,
  run in CI on Docker: node, indexer, proof server, Anvil, MPC cluster).
  That single test is the only end-to-end coverage of prove + balance +
  submit in either repo.

Captured goldens in mpc worth importing: =chain-midnight/fixtures/
{notify-tx-156, respond-tx-161, respond-bidirectional-tx-181}.mn= (real
transactions), =caller-post-state-156.mn=, =golden-state-caller-156.json=.

* 5. Trust: a stateless, untrusted DA layer

The MPC keeps no Midnight state; everything comes from an RPC endpoint
it does not verify. =indexer.rs:120-132= gathers a =BlockProofSeed=
(header, body, system events) and =drop()=s it: "no verifier or
persistence policy yet".
1. READER is the exposure: a lying endpoint can fabricate a
   signBidirectional request and the MPC signs an attacker-chosen EVM
   tx. Independent of TS vs Rust. Fix: verify the seed (extrinsics root
   vs header, header vs finality) or quorum over endpoints.
2. Publisher reads fail safe for respond: transcript independent of
   contract state; a lie yields a rejected tx or a vk mismatch (keep
   the =expectedVk= gate). A lie about ledger parameters skews the fee,
   bounded by the wallet balance.
3. A Rust DUST wallet replayed from untrusted events: no theft without
   the key; phantom UTXOs → repeated invalid submits, hidden UTXOs →
   stall, stale Merkle root → spend fails. Persisted state must be
   rebuildable from genesis. The funding seed is a hot key either way.
4. Submission: silent drops, "invalid" after inclusion → retries;
   duplicates are by design acceptable; stranded DUST on the 5-min
   recipe TTL is the cost.
5. Version drift between the pinned ledger crate and the live network
   → silent rejections. Both repos are on rc tags.
The port removes the proof server as a network dependency and stops
executing an npm package's JS as trusted code on the signing path.

* 6. Version pins (the reconciliation M29 F must settle)

| crate                      | this repo (rev 04c9c5d, 2026-07-31) | mpc                                    |
| midnight-ledger-v9         | 1.0.0 at that rev                    | tag crate-ledger-9.1.0.0-rc.3 (85e769a) |
| midnight-transient-crypto  | 3.0.0                                | tag transient-crypto-3.0.0-rc.2 (29f5c5d), plus 2.2.0-rc.1 for zkir v2 |
| midnight-zkir-v3           | 04c9c5d                              | zkir-2.2.0-rc.3 (v2 only)              |
| sidecar npm                | —                                    | @midnightntwrk/ledger-v9 1.0.0-rc.3, compact-runtime 0.18.0-rc.1, compactc 0.33 toolkit |

* 7. Size of the switch (estimate, mpc repo)

Delete ≈4,500 lines (sidecar src/tests/devtools/config ≈3,000;
=intent_gen.rs= 1,378; publisher env plumbing ≈60). Add ≈1,800
production Rust (intent build 250, proving 200, DUST wallet 700-1,000,
sign+seal 80, submit 200, funding bootstrap 200, Cargo 50) and
≈1,500-2,000 tests. The =IntentClient= seam keeps =publisher.rs= and
its tests. Outside mpc: deploy tooling in signet-midnight-integration
carries our artifacts (or a Rust =ContractDeploy= path, ≈150 lines);
this repo publishes a ≈100-150-line respond-call surface (M30 A). The
DUST number is the one to distrust.

* 8. As built — M29 A+B (2026-09-05)

** The crate

=crates/signet-artifacts= (workspace member, not a =[workspace.
dependencies]= entry — nothing else depends on it, and that table is
only what =workspace_versions.rs= checks): a library
(=signet_artifacts::{circuits, generate, hash_verifier_key,
zkir_v3_binary, zkir_v3_available}=) plus a thin binary,
=cargo run -p signet-artifacts -- <out-dir>=, that calls =generate=.
=circuits()= lists the three signer circuits under their COMPACT
names (=signBidirectional=, =respond=, =respondBidirectional=) mapped
to =signet_contract::{sign_bidirectional, respond,
respond_bidirectional}= — the sidecar's =expectedVk=/
=RESPOND_CIRCUITS= key by the Compact name, not the Rust one.

** The exact =zkir-v3= invocation

Confirmed by running =zkir-v3 --help= in the devshell: four
subcommands (=mock-compile=, =mock-compile-many=, =compile=,
=compile-many=), matching the note's guess. The pipeline:

1. =minocrab_zkir::v3::write_zkir= writes =<out>/zkir/<circuit>.zkir=
   for each of the three circuits (compactc's on-disk JSON form).
2. =zkir-v3 compile-many <out>/zkir <out>/keys= (confirmed CLI:
   =compile-many <IR_DIR> <KEY_DIR>=). Reading the binary's own source
   (=~/.cargo/git/checkouts/midnight-ledger-*/04c9c5d/zkir-v3/src/
   main.rs=) matters here: =compile-many= does keygen (writes
   =<KEY_DIR>/<circuit>.{prover,verifier}=) but, EXACTLY LIKE
   =compactc=, also writes each circuit's =.bzkir= AS A SIDE EFFECT of
   loading a bare =.zkir=, INTO THE SAME =IR_DIR= the =.zkir= came
   from (=main.rs='s =maybe_bzkir=: on a =.zkir= extension it loads
   the =IrSource= and =tagged_serialize=s a =.bzkir= sibling before
   returning). So there is no separate "make me a =.bzkir=" step —
   =<out>/zkir/<circuit>.bzkir= appears next to the =.zkir= once
   =compile-many= runs, matching the managed layout named in
   milestones.org M29 A without any extra copying in this crate.

Located via =$ZKIR_V3= else =zkir-v3= on =PATH= (a bare name with no
=/= makes =std::process::Command= search =PATH= itself — no =which=
dependency needed).

** The managed layout (committed under =crates/signet-artifacts/
managed/=)

#+begin_example
managed/
  zkir/{signBidirectional,respond,respondBidirectional}.zkir   COMMITTED (~2.9 KB each)
  keys/{signBidirectional,respond,respondBidirectional}.verifier COMMITTED (1,353 B each)
  MANIFEST.sha256   sha256 of all twelve generated files, incl. the two
                    uncommitted kinds (.bzkir, .prover) — drift in keygen's
                    OWN output is visible even though nothing regenerates it
                    by hand
  expectedVk.json   {circuit -> hashVerifierKey(<circuit>.verifier)}
#+end_example

=.bzkir= (~16-20 KB) and =.prover= (~56 MB) are NOT committed — the
milestone's call, since both regenerate byte-identically from the
committed =.zkir= (=zkir-v3='s keygen is deterministic: rerunning
=compile-many= on an unchanged =.zkir= reproduces the same =.bzkir=/
=.prover=/=.verifier= bytes, checked by
=tests/generated_equals_committed.rs= below). Whole =managed/= is 32 KB.

** The regeneration gate

=crates/signet-artifacts/tests/generated_equals_committed.rs=: calls
=signet_artifacts::generate= into a fresh temp dir and asserts the
COMMITTED =.zkir=/=.verifier= files, =MANIFEST.sha256= and
=expectedVk.json= are byte-identical to the regenerated copies (the
manifest comparison catches drift in the uncommitted =.bzkir=/
=.prover= too, since their hashes are IN it). Skips loudly
(=eprintln!=, like =corpus_roundtrip= without a corpus) if =zkir-v3=
is not found — it IS found in this devshell, so the gate ran for
real: keygen for all three circuits (k=10 each, BENCHMARK.md's
figure) took *0.5 s total* on this machine, next to nothing beside
compactc's own artifacts (the corpus's compactc-compiled =respond.
zkir= at k=16/40,931 rows took *≈30 s* for the same three-circuit
=compile-many= in the same environment, confirming the k=10 vs k=16
gap BENCHMARK.md already reports also shows up at keygen time, not
just proving time). No separate "if slow, run once and report" step
was needed — every test run pays the 0.5 s.

** The hash function: provenance and the pin

=hashVerifierKey= lives at
=@midnight-ntwrk/compact-js/dist/cjs/ContractKeyLocation.js= (checked
out with dependencies at =~/midnight-examples/node_modules/
@midnight-ntwrk/compact-js=, version 2.5.5-rc.7), lines 18-20:

#+begin_src javascript
function hashVerifierKey(bytes) {
    return (0, node_crypto_1.createHash)('sha256').update(bytes).digest('hex');
}
#+end_src

=sig-net/mpc='s =chain-signatures/midnight-publisher-ts/src/prover.
ts= (=keyMaterial='s =readFile=/=Uint8Array.from= around line 42,
checked with =hashVerifierKey(verifierKey) !== verifierKeyHash= at
line 59) calls it on the RAW bytes of =keys/<circuit>.verifier= read
straight off disk — no framing, no version byte, no length prefix. So the Rust pin is exactly =sha256(file bytes)= as
lowercase hex — =signet_artifacts::hash_verifier_key=, backed by the
=sha2= crate already in =[workspace.dependencies]=. Manually
confirmed once outside the test suite too: =shasum -a 256
keys/respond.verifier= and =node -e '...hashVerifierKey(fs.
readFileSync(...))...'= against the same file printed the identical
64-hex-char string.

*** What could NOT be pinned against mpc's own fixtures

No committed =(.verifier file, expected hash)= pair exists anywhere
reachable. =@sig-net/midnight-contract= — the package that carries
the real =expectedVk= table — is a version-pinned dependency
(=0.20.0-rc.1=) of =midnight-publisher-ts/package.json= but is NOT
vendored under =~/mpc= (no =node_modules= is installed there at all,
so its =dist= is not on disk to read), and grepping =~/mpc= for
=expectedVk=/=hashVerifierKey= only turns up the CALL SITES
(=prover.ts=, =intent.ts=, =prover.test.ts=), never a literal hash
value — consistent with mpc-publisher.org's own framing that the
signet contract is not deployed yet, so no real =expectedVk= table
has ever been generated. =tests/hash_verifier_key_pin.rs= therefore
pins the function the other way the milestone allows: it shells out
to =node= and REQUIRES the real
=.../compact-js/dist/cjs/ContractKeyLocation.js= (path from
=$MIDNIGHT_EXAMPLES_NODE_MODULES=, defaulting to
=~/midnight-examples/node_modules=) on our own committed
=managed/keys/respond.verifier=, and asserts the Rust function agrees
— then separately checks the committed =expectedVk.json='s
=respond= entry against the same two oracles. Both are present in
this devshell (node 22.23.2; the real compact-js checkout), so the
test runs for real rather than skipping; it skips loudly only if
either is absent elsewhere. If =@sig-net/midnight-contract= is ever
vendored with real =expectedVk= data, that would be a STRONGER,
fixture-only pin worth adding alongside this one, not instead of it —
the node-execution pin only proves "matches the source code today",
not "matches what got deployed".

** Timings

- Keygen (all three circuits, =zkir-v3 compile-many=, k=10 each):
  *0.5 s* total, this machine, cold KZG-params cache already warm
  under =~/.cache/midnight/zk-params=.
- Same three-circuit =compile-many= on compactc's OWN v3 artifacts
  (k=16, corpus copies, used only to confirm the CLI's behaviour
  before wiring the crate): *≈30 s* total.

** Unresolved / left for later rungs

- Rung C/D/E (the ledger's own preimage, ledger-apply, real proving
  under =--release=) are untouched here — this note only records A/B.
- =TRUST.md= was NOT regenerated: =signet-artifacts= is not one of
  the seven eDSL crates =trust_base.rs= closes over (=CRATES= in that
  file), so it is out of scope for that gate by design, not by
  oversight.
- The =expectedVk.json= pin is "matches compact-js's source read
  today", not "matches a real, mpc-published, deployed-contract
  =expectedVk=" — see above.

* 9. As built — M29 C+D+E, and F's pin facts (2026-09-05)

Rungs C, D and E in one session, on top of §8's A+B. The through-line: §4
said every existing gate was PI-level or had proofs switched off, and that
"our preimage proves under our key" was inferred from =check=, not shown.
It is shown now.

** C. The construction gate — the ledger's preimage, not a hand-built one

*** What was wrong with the old differential

=tests/signet_contract_differential.rs= built its =ProofPreimage= by hand:
=inputs= a =Vec<Fr>= the test wrote out, =public_transcript_inputs= an
=op.field_repr= over ops the test wrote out, =communications_commitment= a
=transient_commit(&inputs, rand)= the test computed. Every one of those is
a SECOND READING of a rule =ledger/src/construct.rs= already states, and a
differential run against a second reading tells you nothing about the
first. Two artifacts agreeing on a preimage neither of them will ever see
is not evidence.

*** What replaced it

=crates/minocrab-contracts/tests/support/signet_call.rs= — the one place
in the suite that builds a singleton preimage, through the production
path:

#+begin_example
Impact program ──partition_transcripts──▶ Transcript (gas, effects, version)
                                                 │
arguments as an AlignedValue ────────────────────┤
                                                 ▼
                                      ContractCallPrototype
                                                 │
            Intent::new ──▶ add_call ──▶ communication_commitment
                                         ContractCallExt::construct_proof
                                         (construct.rs:515-573)
                                                 ▼
                                          ProofPreimage
#+end_example

The preimage is READ BACK OUT of the =ContractCall= the intent carries,
not reconstructed from what =construct_proof= would have said — so the
bytes under test are the bytes a real intent holds. §3's claim ("the
sidecar never interprets the IR; =construct_proof= makes the preimage and
whatever sits at =zkir/<circuit>.bzkir= proves it") is now the code path
the tests take, not a description of one.

=tests/signet_construction.rs= replaces the old file: the same
call-compatibility gate on all three circuits (=assert_call_compatible= —
same input/output schema, same =pis= and =pi_skips= under our simulator,
=IrSource::check= accepting on both sides) and the same three tamper twins
(tampered event byte, tampered signature byte, swapped event name), all on
that preimage. =partition_transcripts= is content-blind — it runs whatever
program it is handed — so the tampers survive the move onto the production
path unchanged.

*** Three facts that were prose and are now assertions

1. =the_ledgers_inputs_are_the_arguments_field_repr=: =construct_proof=
   fills =inputs= from =ValueReprAlignedValue(call.input).field_vec()=,
   i.e. the argument value's =value_only_field_repr=. The hand-written
   limb lists the suite used before were right, and this says why.
2. =the_alignment_does_not_reach_the_preimage=: a typed =[Bytes<32>,
   Uint<8>, Bytes<128>]= argument value and a flat =[Field; n]= one over
   the same limbs give BYTE-IDENTICAL preimages (tagged-serialized and
   compared). The ledger reads an argument value only through its field
   repr; the alignment never enters the transaction.
3. =input_schema_matches_the_corpus_artifact=: the corpus artifacts
   declare 8 / 9 / 9 =Scalar<BLS12-381>= inputs, and the ledger-built
   preimages carry exactly that many — so the differential is comparing
   two circuits that read the same argument list, not two that happen to
   agree on a prefix.

*** The hand-built helper: REDUCED TO A WRAPPER, not deleted, and why

The milestone allowed either. =serialization/deployed.rs='s
=misc_preimage= survives as

#+begin_src rust
pub fn misc_preimage(inputs: Vec<Fr>, misc_bytes: &[u8]) -> ProofPreimage {
    call_preimage("respond", scalar_input(&inputs), misc_bytes)
}
#+end_src

because its caller is =serialization_conformance='s proptest over
thousands of generated payloads, which has raw limbs and no typed argument
value to hand. Fact 2 above is exactly the licence for that: the flat
alignment and the typed one produce the same bytes, so this wrapper is
not a second construction — there is now ONE definition of a singleton
preimage in the workspace, and this is a call to it. The
=signet_contract_differential.rs= copy was deleted outright.

=bench.sh= dumps its preimages from =--test signet_construction= now, so
BENCHMARK.md's singleton rows price the preimage production builds.

** D. The ledger-apply gate, against mpc's captured transactions

*** The goldens, imported

=crates/minocrab-contracts/tests/fixtures/mpc/= holds
=respond-tx-161.mn= and =respond-bidirectional-tx-181.mn=, copied
=cmp=-identical from =~/mpc/chain-signatures/chain-midnight/fixtures/=,
with a =README.md= carrying the capture record (chain
=c171225731f5ca07=, =@sig-net/midnight-contract@0.20.0-rc.1=, compactc
0.33.0-rc.2, singleton =b116cd04…=, request id =1cd10eb1…=, blocks 161 and
181, extrinsic 4, =TxApplied=). A pleasant accident worth pinning: each
file's SHA-256 IS the ledger transaction hash the capture recorded
(=9444aa63…= and =5291b70c…=), so one assertion is both the integrity
check and the provenance pin, and the test makes it before decoding
anything.

Not imported: =notify-tx-156.mn=, =caller-post-state-156.mn=,
=golden-state-caller-156.json=. Those belong to the READER side, which
=crates/signet-sim= pins for itself; M29 D is about the two respond calls
a publisher makes.

*** The gate

=tests/signet_ledger_apply.rs=, per respond circuit:

1. =ContractDeploy= a singleton whose three operations carry the COMMITTED
   managed verifier keys (§8's =managed/keys/*.verifier=) into a fresh
   =LedgerState=; =well_formed=; =apply=.
2. Decode the golden's singleton call through a mechanical translation of
   =chain-midnight/src/emissions.rs:96-148= — run the guaranteed
   transcript under the ledger VM on an empty =QueryContext=, take the one
   =VersionedLogItem=, check version 1 / type =Misc= / one =Bytes<288>=
   cell, split =name(32) ‖ payload(256)= — and pull the request id and
   signature OUT of that payload at the singleton's declared offsets.
3. Build OUR call on those same values through C's path, =well_formed= it
   against the deployed state, =apply= it.
4. Compare the emission the LEDGER RECORDED while applying ours (an
   =EventDetails::ContractLog= off =TransactionResult::Success=, not a
   re-run of our own program) with the golden's.

*** The comparison, field by field

COMPARED and required equal:
| field | why it must match |
| =ContractCall::entry_point= | the sidecar addresses the circuit by this name |
| the guaranteed transcript's Impact program, OP FOR OP | this is the emission: =Push= of =[Bytes<4> version, Bytes<1> tag, Bytes<288> data]=, then =Log= |
| =fallible_transcript= absent on both | mpc's reader REFUSES a singleton call that has one (=UnsupportedFallibleCall=) |
| =Transcript::version= present | =well_formed= rejects a call against a v3-keyed operation whose transcript declares none |
| =VersionedLogItem::version= (1) and =event_type= (=Misc=) | =emission_from_log_item= checks both before reading a byte |
| the logged cell's alignment: one =Bytes<288>= atom | ditto |
| all 288 emitted bytes: =pad(32, name)= ‖ payload(256) | the name selects mpc's =EmissionKind=; the payload is what the MPC signs over |

NECESSARILY DIFFERENT:
| field | why |
| =ContractCall::address= | theirs is the capture chain's deployment =b116cd04…=; ours is a fresh =ContractDeploy= whose address derives from its own nonce and initial state. Unmatchable without replaying their deployment, which is impossible by construction — their operations carry compactc's verifier keys and ours carry MinoCrab's, and that difference IS the swap |
| =communication_commitment= | =transient_commit(input ‖ output, rand)= over a per-call =rand=; the sidecar sampled theirs, this suite pins ours so dumped preimages stay reproducible |
| =ContractCall::proof= | theirs a real =ProofVersioned::V3=; ours a =ProofPreimageVersioned::V2=. This gate runs under the proof-preimage marker, where =ProofKind::proof_verify= is a NO-OP — which is precisely why rung E exists |
| intent =binding_commitment= / =ttl=, tx =binding_randomness= / =network_id= | per transaction, per chain |
| =Transcript::gas= | see below |

The gas figure is the one measured surprise, and it is worth writing down.
Both sides declare =bytes_written= 678, =bytes_deleted= 678 and
=read_time= 0 (the singleton reads nothing). But the golden declares
=compute_time= *2.208 ms* where ours declares *1.303 ms* — theirs
partitioned by the sidecar against the capture chain's
=crate-ledger-9.1.0.0-rc.3= parameters, ours against =INITIAL_PARAMETERS=
at rev =04c9c5d=. Printed by the test, not asserted; our transaction
applies at our figure, which is what =apply= checks. If a publisher ever
declares gas against the WRONG parameter set the failure is a rejected
transaction on chain, not a test failure here — that is a live item for
M30 B (fetch ledger parameters from the node, do not assume
=INITIAL_PARAMETERS=).

The payload VALUES match only because step 2 feeds the golden's own
request id and signature back in. That is the design: a field the two
toolchains laid out differently would surface as a payload mismatch at a
fixed offset, and the assertion covers all 288 bytes including the zero
pad.

*** Two supporting facts, asserted separately

- =the_deployed_operations_are_our_committed_verifier_keys=: without it,
  the apply gates would pass equally against a contract declaring no keys
  at all, since the proof-preimage marker never looks at one.
- =log_ops_is_the_program_the_captured_transactions_carry=: the Impact
  program the suite writes out (the one thing in C's path NOT taken from
  the ledger — in production it comes from compactc's generated JS
  executor) is op-for-op the program a real chain carried. That closes the
  last hand-written link in the chain.

** E. The real-proving gate — the first real proof in the test suite

=tests/signet_real_proof.rs=, =#[ignore]=d like =row_snapshot=:

#+begin_src sh
cargo test --release -p minocrab-contracts --test signet_real_proof -- --ignored --nocapture
#+end_src

keygen from our IR → assert the verifier key is BYTE-IDENTICAL to the
committed =managed/keys/<circuit>.verifier= → =ContractDeploy= under those
keys → build the call C's way → =Transaction::prove= through an in-process
=ProvingProvider= → =seal= → =well_formed= with =verify_contract_proofs=
ON in =ProofVerificationMode::Real= → =apply=. All three circuits, one
process, sequentially.

*** The negative control, in the same loop

A gate that only ever says yes is not a gate. Each proof is ALSO offered
to a second singleton deployed with the keys PERMUTED (respond's operation
carrying signBidirectional's key, and so on) and =well_formed= must
reject it. All three reject. Without this, the positive half passing would
be consistent with nothing being verified at all — and note that the D
gate genuinely is in that position, by design.

*** The provider: what the pinned ledger does and does not offer

There IS an in-process =ProvingProvider= upstream, but not a reachable
one: =midnight_ledger::test_utilities::CombinedProofProvider= is a private
struct behind the ledger's =test-utilities= + =proving= features, and its
resolver is =ledger::prove::Resolver=, which owns a =ZswapResolver= and a
=DustResolver= — network-fetched parameter sets this gate has no use for,
since the transaction carries no zswap offer and no dust action. And
=zkir::LocalProvingProvider= (the public one) is the ZKIR V2 crate's: it
loads =zkir_v2::IrSource=, which will not read our
=ir-source[v3-generic]=.

So =V3ProvingProvider= is a mechanical translation of
=CombinedProofProvider='s v3 arm only
(=ledger/src/test_utilities.rs:749-818=): resolve the key material, load
the =IrSource= from its =ir_source= bytes, hand the preimage to
=ProofPreimage::prove::<zkir_v3::IrSource>=. About 35 lines. The
milestone's fallback ("prove the ledger-constructed preimage with =Zkir=
directly, the bench harness's path") was NOT needed — the full
=Transaction::prove= path is reachable at the pinned rev.

That matters beyond this test: it is §2's claim about the port, exercised.
Proving is a plain async Rust call — no proof server, no child process, no
network once the KZG parameters are on disk.

*** Numbers (this machine, =--release=, params warm under
=~/.cache/midnight/zk-params=; single-shot, NOT a median)

| circuit              |  k |  rows | keygen | pk size | prove   | well_formed(verify) | peak RSS after |
| signBidirectional    | 11 | 1,205 | 0.112s |  1.8 MB | *0.182s* |             0.015s |        76.6 MB |
| respond              | 10 | 1,004 | 0.071s |  0.9 MB | *0.121s* |             0.004s |        80.7 MB |
| respondBidirectional | 10 | 1,004 | 0.070s |  0.9 MB | *0.116s* |             0.004s |        82.7 MB |

Process baseline before any keygen: *18.7 MB*. Whole-run peak: *82.8 MB*.
The whole test — three keygens, six proofs (three honest, three for the
negative control), six =well_formed=s — takes *1.20 s*.

Run-to-run spread, from a second identical run: prove 0.187 / 0.121 /
0.119 s, per-circuit peaks 78.0 / 81.0 / 82.7 MB, whole-run peak the same
*82.8 MB*, total 1.22 s. So the prove figures are stable to ~3% and the
whole-run peak is reproducible to the tenth of a megabyte; the PER-CIRCUIT
deltas are the noisy part (+59.3 / +3.0 / +1.7 against +57.9 / +4.1 / +2.0),
which is what a monotone process-wide high-water mark does when allocation
order shifts. Quote the whole-run peak, not the deltas.

Read against BENCHMARK.md's minocrab rows (which this note does NOT edit):

| circuit              | BENCHMARK.md prove | here   | BENCHMARK.md RSS | here (cumulative) |
| signBidirectional    |              0.14s | 0.182s |            51 MB |           76.6 MB |
| respond              |              0.09s | 0.121s |            41 MB |           80.7 MB |
| respondBidirectional |              0.09s | 0.116s |            42 MB |           82.7 MB |

The two are measuring different things and the differences are explained,
not anomalies:
- PROVE is ~30% slower here because BENCHMARK.md reports the MEDIAN OF 3
  (=MINOCRAB_PROVE_ITERS=) on a warmed process while this is a single
  cold shot, and because this path goes through =Transaction::prove= —
  =ProvingProvider::check=, the noop-insertion pass and the binding-input
  overwrite — where the harness calls =IrSource::prove= directly.
- RSS is NOT comparable cell by cell: the benchmark spawns one subprocess
  per measurement precisely so each figure is that circuit alone, whereas
  this figure is monotone across a single process holding all three
  prover keys at once. The interesting number is therefore the LAST one:
  *a publisher process serving all three signer circuits peaks at ~83 MB*,
  against ~900 MB for a single compactc =respond= proof (BENCHMARK.md).
  That is the sizing fact M30 needs, and it is why the deltas are printed
  beside the mark: +57.9 MB for the first circuit (params + first prover
  key), then +4.1 and +2.0 for the two k=10 circuits.
- VERIFY is larger than the harness's 0.003s because =well_formed= checks
  the whole transaction, not only the proof.

*** What E does NOT prove

No fee: =enforce_balancing= and =enforce_limits= are off, and there is
still no DUST wallet anywhere in this workspace (§2's one remaining GAP,
M30 C). Nothing is submitted. And the deployed key is ours by
construction — that a real chain carries it is deploy tooling's job,
outside this repo.

** F. The pin reconciliation, facts only

Read off both =Cargo.lock=s on 2026-09-05. The headline is that §6's table
UNDERSTATES the agreement: every shared crate is at the SAME SEMVER
version in both repos. What differs is which git object supplies it —
this repo takes all of them from one rev; mpc takes each from its own
release tag, the way midnight-node pins them.

| crate                     | version | this repo (one rev)      | sig-net/mpc (per-crate tags)           |
| midnight-ledger-v9        |   1.0.0 | rev =04c9c5d9=           | tag =crate-ledger-9.1.0.0-rc.3= (=85e769a0=) |
| midnight-transient-crypto |   3.0.0 | rev =04c9c5d9=           | tag =transient-crypto-3.0.0-rc.2= (=29f5c5de=) |
| midnight-transient-crypto |   2.2.0 | tag =transient-crypto-2.2.0-rc.1= (=a01a1ea0=) | the SAME tag (=a01a1ea0=) |
| midnight-base-crypto      |   1.1.0 | rev =04c9c5d9=           | tag =base-crypto-1.1.0-rc.2= (=00cfcb09=) |
| midnight-onchain-runtime  |   4.0.0 | rev =04c9c5d9=           | tag =onchain-runtime-4.0.0-rc.2= (=37337e70=) |
| midnight-onchain-state    |   4.0.0 | rev =04c9c5d9=           | tag =onchain-state-4.0.0-rc.3= (=a2a4aa50=) |
| midnight-onchain-vm       |   4.0.0 | rev =04c9c5d9=           | tag =onchain-vm-4.0.0-rc.2= (=a2c3c5d5=) |
| midnight-coin-structure   |   3.0.0 | rev =04c9c5d9=           | tag =coin-structure-3.0.0-rc.1= (=8c27955d=) |
| midnight-serialize        |   1.2.0 | rev =04c9c5d9=           | tag =serialize-1.2.0-rc.1= (=7267bca4=) |
| midnight-zswap            |   9.0.0 | rev =04c9c5d9=           | tag =zswap-9.0.0-rc.3= |
| midnight-storage          |   2.0.1 | rev =04c9c5d9=           | crates.io registry |
| midnight-zkir (v2)        |   2.2.0 | rev =04c9c5d9=           | tag =zkir-2.2.0-rc.3= (=5350e0ff=) |
| midnight-zkir-v3          | 3.0.0-rc.2 | rev =04c9c5d9=        | ABSENT — mpc has no v3 dependency |

Two asymmetries worth naming:
- =midnight-transient-crypto 2.2.0= is at the IDENTICAL commit in both
  (=a01a1ea0=), because both repos mirror the ledger's own back-compat
  patch. That is one crate where the pins already coincide exactly.
- mpc carries NO =midnight-zkir-v3=. It never needs one: the sidecar's
  proof server does the proving, and mpc's Rust side only READS
  transactions. A Rust publisher (M30) is the thing that will need it, so
  that row is a dependency mpc will ACQUIRE from the swap, not a conflict
  to resolve.

*** Do the =.mn= goldens deserialize under our pin? YES

This is itself the reconciliation evidence, and D's gate carries it:
=respond-tx-161.mn= and =respond-bidirectional-tx-181.mn= were
tagged-serialized by mpc's =crate-ledger-9.1.0.0-rc.3= and
=tagged_deserialize= into
=Transaction<Signature, ProofMarker, PureGeneratorPedersen, InMemoryDB>=
here at rev =04c9c5d= with no adaptation — same tag
=midnight:transaction[v12](signature[v2],proof,pedersen-schnorr[v1])=,
same =pedersen-schnorr[v1]=. The transaction wire format, the transcript
op encoding, the FAB alignment encoding and the =ProofVersioned=
discriminants are all shared between the two pins, because every proven
transaction in those files round-trips through them. That is a stronger
statement than "the version numbers match", and it is now checked on every
=cargo test= run.

*** The decision this leaves

M29 F's task was to CHOOSE one pin. The facts above say the choice is not
forced: the two pins agree on every shared crate's semver and on the wire
format, so nothing here has to move for the swap. The remaining
difference is provenance discipline — one rev versus per-crate release
tags — and the argument for adopting mpc's shape is that release tags are
what midnight-node itself pins, so drifting from them is drifting from
what the network runs. Against it: this repo's single rev is also what
the official compact flake pins for the =zkir-v3= binaries our compactc
ships with (Cargo.toml's comment on the =midnight-zkir-v3= entry), and
=zkir-v3= has no release tag to move to. dmd's call; recorded here rather
than taken unilaterally, and =notes/zkir.org='s pin table is left
untouched until it is made.

** Files

| file | rung |
| =crates/minocrab-contracts/tests/support/signet_call.rs= | C (built), D (deploy/apply helpers) |
| =crates/minocrab-contracts/tests/signet_construction.rs= | C — replaces =signet_contract_differential.rs= |
| =crates/minocrab-contracts/tests/signet_ledger_apply.rs= | D |
| =crates/minocrab-contracts/tests/fixtures/mpc/{README.md,respond-tx-161.mn,respond-bidirectional-tx-181.mn}= | D |
| =crates/minocrab-contracts/tests/signet_real_proof.rs= | E |
| =crates/minocrab-contracts/tests/serialization/deployed.rs= | C — =misc_preimage= reduced to a wrapper |
| =bench.sh= | C — dumps preimages from the new target |

** Unresolved / left for later

- F's CHOICE (above) is dmd's, and =notes/zkir.org='s pin table is
  deliberately unedited.
- M30 B should fetch =LedgerParameters= from the node rather than assume
  =INITIAL_PARAMETERS=: D measured a 2.208 ms vs 1.303 ms gap in the
  declared transcript =compute_time= between the capture chain's
  parameters and ours.
- The DUST wallet is still zero code here (§2), so no gate in this
  milestone pays a fee or balances anything.
- =BENCHMARK.md= is untouched, as instructed; E's numbers live only in
  this note.
