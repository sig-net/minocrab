#+title: Deferred code-review queue

Fable-usage economy (dmd 2026-08-15, at 94% usage): supervision runs
light — gates (row/interface snapshots, 13 differential suites, spec
harness) are trusted, diffs are NOT read in-session. Anything that
would have warranted human-grade review is queued here instead; run a
/code-review session over these when convenient. Items are appended as
stages land; tick when reviewed.

* Session of 2026-08-15 — what to review first (dmd asked for this)
Everything below the "Standing instruction" heading dated today landed
in ONE session, gate-checked and diff-unreviewed. Suggested order for a
review session, highest risk first:
1. M9 phase 7's LITERALS commit (ea1c46f) — the only change in the whole
   session whose gate was a hand-review rather than an instrument, since
   the row snapshot cannot see a removed Copy.
2. M9 phase 6's thirty declarations + M9 closure's label strings — a
   disclosure report is the only place either error would surface.
3. M9 phase 8's widen soundness argument + the twin's PI-equality claim.
4. M11 stage 10's hand-written TypeScript reject rules (the generated
   half is self-checking; these are not).
5. The README, as a public artifact, after four rewrites in one day.
Landed and NOT in this queue because an instrument covers them fully:
the 87-circuit byte-identical ZKIR dumps (phases 5-8 and the closure),
both frozen snapshots, and the eleven differential suites.
Two agents were still running when this was written (M8 churn
resilience, M14 bounded integers). Both have since landed and their entries are below.
Read M8's DRIFT TAXONOMY early, since it is the document a future
session will follow without re-deriving it, and the in-place snapshot
regenerators it introduces are a new way to lose a baseline. In M14,
the reviewable part is not the new leaf (its differential is equal to
compactc up to identifier renaming) but the EXCLUSIVE-BOUND correction
it forced on three of our own records, and the three width formulas
that only a bounded type can tell apart.

Housekeeping recorded here so it is not rediscovered: M14's four
commits carry david@sig.network where the repo default is
dmillardurrant@gmail.com — cosmetic, both are dmd, no trailers
anywhere; unfixed because stage 1 sits nine commits down and a rebase
would rewrite M8's commits too. And target/ reached 26G with the volume
at 99% (the M14 agent hit zero bytes free and blocked mid-session):
target/debug alone went 9.9G → 21G when the dev profile started
building optimized deps. 4.8G of incremental cache was cleared at the
end of this session; the underlying growth is structural, so a future
session on a tight volume should expect to clear it again.

* Session of 2026-08-16 — M15 only
One milestone, three commits, gate-checked and diff-unreviewed like the
rest. Its entry is under the standing instruction below; read item (1),
the curve-point recognition, first — it is the only line in the session
where a mistake would produce a WRONG ALIGNMENT rather than a failing
test, and it is a positive/negative pair rather than a check.

Two things a reviewer should know before reading the diff. First, M15
closed a BUG as well as the fairness gap: `CompactType::flatten` could
not flatten the erc20-vault's own `initialize`, so our benchmark
contract was not importable through our own interface machinery, and
nothing would have noticed because the test comparing five FAB atoms
against five did not exist. Second, the milestone's own text guessed the
Rust shape wrong in both directions (an opaque DOES enter circuits; half
the corpus's `Opaque` nodes are curve points), so the design note's §0
is a correction to `milestones.org` rather than a summary of it.

Housekeeping: target/ was at 24G with 5.2G free at the start of this
session, and 1.2G of `target/debug/incremental` plus `target/flycheck0`
were cleared to make room for the --release row_snapshot gate. This is
the third session to do it; the growth is structural, as the 2026-08-15
note says. Commits again carry david@sig.network, matching M14's four
and not the repo default — cosmetic, both are dmd, no trailers anywhere.

* High value (protocol/soundness-adjacent)
- [ ] f102ffe M10 burn restructure (erc20_vault_opt withdraw/swap):
      the single-claimed-spend lowering + the well-formedness test
      (044a132). Gates were green; the ledger-semantics argument
      deserves human eyes.
- [ ] 66e5480 M10 refund branch merge: the cond_select authorization
      argument ("routing total and disjoint") — verify the written
      proof matches the code.
- [ ] e6dd119 Poseidon refund commitments: durability comment +
      concretization arms in tests/vault/prims.rs.
- [ ] 144b9c0 M10 verification harness: the nine spec_* functions vs
      the Compact source (the specs ARE the security definition; the
      agent verified against source, un-reviewed by human).
- [ ] 2857631 one-block userCommitment: the preimage layout change
      (43-byte, single SHA block) at all three sites.

* Medium (API/architecture)
- [ ] b353e8f/43cc92d #[circuit] attribute expansion + the four
      re-ports (thinness token test exists; macro hygiene worth a
      skim).
- [ ] a565cf5/1375c42 const-generic Signet event family + vault retype
      (older, all gates green since).
- [ ] M11 stage 0 conformance suite (when landed): spec-type widths vs
      signet.rs atoms — transcription-error risk.

* Standing instruction
Future stage agents' commits land gate-checked but diff-unreviewed;
append one line here per landed stage naming the commit + what a
reviewer should look at.
- [ ] ca0ebe2 M11 stage 0 conformance suite: spec-type widths vs
      signet.rs atoms (transcription risk); the two subset corrections
      (Option caught by fixed-width not dual-oracle; Tag<K> → u8) and
      the ByteArray<N> wrapper transparency bound.
- [ ] M11 stage 1 (minocrab_std::v3::borsh): look at the leaf table's
      atom widths vs the Borsh widths (a wrong atom is a wrong preimage
      and the ZKIR-equality test would agree with it, since both sides
      are ours — the independent check is the simulated
      bytes-vs-borsh::to_vec one); the CircuitBorshArg substitute for the
      un-spellable CircuitArg supertrait; and Tag<K>'s claim that
      compactc emits no `< K` bound (asserted from the corpus's absence
      of enum-typed arguments, not from an artifact).
- [ ] M11 stage 2 (BorshReader): the WitnessCheck soundness argument —
      witnessed leaves are pinned only by constrain_canonical + the
      re-pack limb equality, so check that read_witness_checked really
      emits both and that Serializer's in-range precondition is met by
      constrain_canonical for every leaf (a leaf whose canonical
      constraint were weaker than its width would break injectivity).
      Also Split's assumption that div_mod bounds the high rest.
- [ ] M11 stage 4 (erc20_vault_borsh, byte-identical fork): the fork is
      1,788 duplicated lines, so check it really is a verbatim copy of
      erc20_vault_opt modulo the module doc (the fork test proves the ZKIR
      matches, which is stronger, but a comment that drifted would not
      show); and check the two RefundScenario branches changed from
      `art == Art::Opt` to `!= Art::Compat` — that is a claim about which
      op stream the borsh artifact expects, and getting it backwards would
      have made the harness agree with a wrong model.
- [ ] M11 stage 6 (event-payload twins): the twins are byte-identical ZKIR,
      which is self-checking — what deserves eyes is the ARGUMENT that
      `MiscDepositEvent { name, payload: DepositEvent }` padded to 288 is
      the same byte string as Compact's `{name, payload: Bytes<256>}`
      (both pads contiguous), and the decision NOT to re-emit
      constrain_canonical on values whose leaves are already constrained —
      check that every leaf really is (amount, recipient: argument
      constraints; sequence: a Bytes<8> cell read; name: a constant).
- [ ] M11 stage 5 (attested outputs → Borsh): THE FORMAT DECISIONS ARE NOW
      THE SPEC — check the kind assignments and the digest layouts in
      notes/borsh-format.org §"As built — stage 5" against the circuit
      constants, since the MPC will implement them. Two arguments deserve
      eyes: (a) `assert_kind`'s claim that equality with one variant
      subsumes `constrain_canonical`'s `tag < K`, so the bound is not
      emitted; (b) the `verify_attestation_signature` extraction in
      signet.rs, a shared file all three artifacts import (snapshots say the
      ZKIR is unchanged). Also the spec-side acceptance predicates: the
      borsh arms of spec_claim/spec_complete_withdraw/spec_complete_swap/
      spec_refund are the definition the 20k-case gate checks against.
- [ ] M11 stage 3 (#[derive(CircuitBorsh)]): the impl_arg_traits
      refactor (existing derive/attribute output must be unchanged — the
      thinness tests and every differential suite say it is, but it is a
      shared code path); the where-clause trick that makes the argument
      impls apply only at Private for a Vis3-generic struct; and the
      minocrab-std `borsh-schema` optional feature — confirm with cargo
      tree that no shipping build enables it (checked in-session, worth
      a second look since it is a dependency-posture claim).
- [ ] M12 stage 7 (README showcase): the README now makes CLAIMS about
      landed features to a public audience — check each against the tree
      (the cross-contract snippet is copied from erc20_vault::notify_signet;
      the drift output is the mutation suite's real text with two long slot
      lists elided; the Borsh section's row deltas and the "already Borsh"
      finding come from notes/borsh-format.org). And re-read the corrected
      honesty marker: the previous one advertised label!/disclose_as/
      Discloses, which do not exist, so the same question is worth asking of
      every remaining claim in the file.
- [ ] M11 stage 8 (spec/borsh-subset.md + vectors): THE PROSE IS THE
      REVIEWABLE ARTIFACT — the tables and vectors are generated and
      self-checking, but §§1-8 and §11 are hand-written claims an external
      team will implement against. Read them against the code: the leaf
      table and exclusions vs v3::borsh's module docs, the reject classes
      vs what the circuits actually enforce, the response-kind table vs
      erc20_vault_borsh's RESPONSE_KIND_* constants, the 128-byte
      notification layout vs construct_notification_v1's limb packing
      (derived by hand from the packing weights — the one layout in the
      document with no generated table behind it), and §11's
      deployed-vs-specified split. Also the vector VALUES: they are
      synthetic, so nothing pins their keccak256 to a real request id.
- [ ] M11 stage 9 (hash flavors): the DOC CLAIM is the reviewable part —
      =v3::hash='s module docs tell a reader when =_compact= is needed,
      and a wrong list there is a wrong digest at an interop boundary
      (check it against the pinned preimages: tokenType, coin cm/nullifier,
      ep hash, comm, merkle interiors — the claim is that every one is
      already encapsulated so a user never picks). Also: the borsh
      =transient_hash='s limb REVERSAL (BytesNDyn is FAB slot order,
      string order is its reverse — one wrong line is a different digest
      that no ZKIR-equality test would catch, only the native-Poseidon
      one does), and =Serializer::finish_dyn= becoming pub.
- [ ] M12 stage 1 (the ABI table): the port of =emit-constraints-for=
      itself (crates/minocrab/src/v3/abi.rs) — the Bounded branch's
      even-width formula and the =Prim::unsigned= normalization are read
      off Scheme and exercised only by unit tests, since no corpus
      artifact reaches them; the two new =Circuit3= immediate methods
      (=assert_eq_imm= / =less_than_imm=, which bypass the =Wire3=
      visibility algebra by construction — the comment claims
      =V ⊓ Public = V=); and the claim that =CircuitArg::constrain='s
      new provided body reproduces all nine deleted hand-written bodies
      slot for slot (both snapshots say it does).
- [ ] M12 stage 2 (the call core): THE ORDERING ARGUMENT is the one to
      check — compactc evaluates a call's receiver before its argument
      expressions, Rust the reverse, so `Callee::pin` exists and the
      erc20-vault's notify_signet uses it while the other three sites do
      not. Verify the claim that the other three sites' argument
      expressions emit nothing between the cell read and the call.
      Also: the two layering deviations (`CircuitAbi` moved to L2;
      `Callee::Pinned` holds limbs rather than `ContractAddress`), and
      the hand-written `CircuitAbi` for `ShieldedCoinInfo3` — its
      `value` field is a bare wire, so the `Uint<128>` width is stated
      in the impl rather than carried by the field's type, which is
      exactly the kind of hand-typed width M9 set out to delete.
- [ ] M12 stage 3 (#[interface] + visibility-generic derive): the
      SYNTACTIC visibility check (`reject_undisclosed` decides
      Public/Private by looking for those words in the written type — it
      cannot see through a type alias, and a `pub type Addr =
      ContractAddress<Public>` would be rejected with a misleading
      message); the `ArgBounds` refactor, which now decides where FIVE
      impls apply for three macros; and the claim that emitting
      CallArg/CallResult only in visibility-generic mode is a
      restriction rather than a gap (an all-Public plain struct gets no
      call impls today).
- [ ] M12 stage 4 (signet-signer-interface + minocrab-abi): the FLATTENING
      TABLE in crates/minocrab-abi/src/info.rs is a second, independent
      transcription of the rule the minocrab-std leaves implement — check
      it against them (Bytes limbing, the Uint atom width, the claim that
      Compact structs add no slot). The hand-written `Deserialize` for
      `CompactType` (raw-text `maxval`) is parsing code with no fuzzing
      behind it. And the CORRECTION recorded in notes/interface-crates.org
      §"As built — stage 4": compactc DOES emit a `less_than` bound for an
      enum-typed circuit argument (advanced_casts/test17.zkir), so M11
      stage 1's stated justification for `Tag<K>` emitting none is wrong —
      confirm the conclusion still holds for `Tag<K>`'s actual position (a
      Borsh-decoded witness, not an argument), since nothing was changed.
- [ ] M12 stage 5 (minocrab-interface-gen): the TYPE-MAPPING TABLE is the
      thing to read (crates/minocrab-interface-gen/src/lib.rs
      `Registry::rust_type`) — a wrong row generates a crate that compiles,
      passes its own agreement test only if the artifact agrees, and is
      wrong at a call site otherwise; check especially the Maybe/Either
      name+shape matches (the shape guard only inspects the TAG field) and
      the `at_public` identifier substitution. Also `import_list`'s
      reproduction of rustfmt's three case groups (cosmetic, but it is the
      reason `--check` stays green) and `names::snake_case`'s round-trip
      test, which decides when an explicit #[entry_point] is emitted.
- [ ] M12 stage 6 (callee-side export): the DEVIATION is the thing to
      weigh — respond/respondBidirectional keep hand-declared arguments
      (converting them would rename inputs and move interface_snapshot),
      so `tests/contract_matches_its_interface.rs` is the ONLY link between
      those nine `c.arg` calls and SignatureRespondedEvent. (M9 phase 5
      narrowed this: the nine slots are now the interface crate's own
      AffinePoint/RequestId/B32/Uint<8>, one level flatter than its
      `{ signature: { .. } }` wrapper — so what the test alone still
      carries is the FLATTENING, not the leaves.) Check that the
      test really covers what the type would have: it compares input count,
      constraint prefix and output count, but NOT the FAB alignment (a
      circuit's IrSource does not carry one), so an argument split into the
      same slots under a different atom shape would pass. Also check
      `check_ir`'s reuse of `compare_zkir`, which was extracted from
      `Artifact::check_zkir` in the same commit.
- [ ] M9 phase 5 (6ad46e4 + b77f178, the remaining 52 circuits): two
      things to weigh. (1) The NEW CORE LEAF, minocrab_std::v3::
      Secp256k1Point: its slot is not a field wire, so CircuitArg::
      push_slots hands back nothing and the provided constrain filters
      Prim::Point out before zipping prims against slots — check that
      the filter cannot silently swallow a MISSING native slot (today
      an impl that forgot one would still trip the length assert, since
      only Point positions are removed), and check the five-atom
      alignment against notes/ledger-abi.org §3 now that
      common::secp256k1_point_atoms delegates to it. (2) The SETTLE
      CIRCUITS' constraint order moved (serializedOutput now constrained
      in parameter order, not last) in all three vault artifacts —
      argued harmless because the instruction multiset and the inputs
      are identical (checked by a full 78-circuit ZKIR dump) and the
      constraints are on distinct argument wires; worth a second pair
      of eyes on the "rows are position-independent" half of that. Also
      skim the ONE name-column change (xcall_with_payment::callOnce's
      coin_ prefix) and the unported-family argument in hashing.rs.
      (STILL OPEN after M9's closure commit, which touched the same two
      files without touching either concern: the Secp256k1Point
      push_slots/constrain filter and the settle constraint order are
      untouched, and so is callOnce's name column — the closure changed
      only how its coin fields are DISCLOSED. What it did change here is
      the hashing unported-family argument, which now carries a second
      paragraph saying the same sixteen circuits are the only ones in
      the corpus without a typed disclosure declaration; read the two
      together.)
- [ ] M11 stage 10 (718ba87, spec/ts): the GENERATOR is self-checking
      (committed files must be regenerator output, and the node suite is
      mutation-checked), so what deserves eyes is what neither test can
      see: primitives.ts's REJECT RULES are a second, hand-written
      statement of rules the circuits enforce elsewhere — getBool's
      0x00/0x01, the setters' range checks, unwrapMiscEnvelope's zero-pad
      rule — and a wrong one there is a decoder that accepts what the
      chain rejects. Also the DEVIATION: generated from the spec types'
      borsh schema rather than CircuitBorsh::layout(), justified by stage
      3's cross-check proving the two tables equal — confirm that
      cross-check really covers every type spec/ts emits (several
      published types have no circuit twin at all).
- [ ] M13 (063f91f, README overhaul) + the terse rewrite (d121430) and
      dmd's own edit (6efa903), then 2f0c209 pointing the snippet at the
      twin: the README makes PUBLIC CLAIMS, and the M12-era failure mode
      (advertising an API that did not exist) is the thing to re-check
      after four rewrites in one day — every snippet against the tree,
      every one of the ~47 links, and the numbers (78→87 circuits, "nine
      of twelve circuits at least one k lower", the 9M-case figure).
      Also the FAIRNESS SECTION is a public statement about what we
      cannot do: the audit rejected two candidates on code evidence
      (enum exhaustiveness — Compact has no match; ZKIR 3.x curve
      breadth — our pinned IrType already carries Curve25519/Secp256r1),
      and those rejections are the reviewable part, in
      notes/readme-research.org §"As built — M13 fairness audit".
- [ ] M9 phase 6 (e1b9954 core + 657d294 rollout, disclosure): the
      generated set-equality test proves the DECLARED set matches the
      DISCLOSED set — neither side is proven RIGHT, so read the 30
      declarations against what each circuit actually leaks. The phase
      found twelve circuits disclosing the xcall ep hash and comm
      commitment undeclared; that class is exactly what a wrong
      declaration would hide again. Also: Disclosure3 carries
      Vec<Identifier> (one record = one logical value) — check the
      disclose_all/disclose_slice groupings match that claim; the
      signature flip to v.disclose_as::<L>(c) (E0107, documented); and
      entry() widened to O: CircuitOut with assert_eq!(O::SLOTS, 0),
      which is what makes a declaration cost no output slot.
- [ ] M9 phase 7 (2b66be3 maps, ea1c46f literals, 860c3ea predicates):
      THE LITERALS COMMIT IS THE ONE WITH NO SNAPSHOT BEHIND IT — the
      row snapshot is blind to Copy removal, so its gate was a
      hand-reviewed instruction delta (47 removed instructions, all
      claimed to be `copy <immediate>`, streams claimed equal after
      canonicalization). That review is the artifact to re-do. For maps:
      LedgerRepr deliberately does NOT require CircuitAbi (a ledger read
      is popeq-checked, never range-constrained) — weigh that, since it
      means a record type's ledger shape and its argument shape are
      stated separately. For predicates: the width inference (widths
      come from the operand types) and check_literal_fits, the
      value-dependent panic that stayed.
- [ ] M9 phase 8 (0c6a88d stage 1, b6bccba the twin): widen::<WIDER>()
      claims a retype needs NO second range constraint (a value < 2^BITS
      is < 2^WIDER) — check that argument and that nothing else assumed
      a constraint per type. The agent DECLINED the coordinator's
      missing-impl form for width rejection (it broke ten sound call
      sites; the condition is a property of the operand PAIR) and used
      inline-const E0080 instead — worth confirming the rejection really
      fires at every call site shape. For the twin: all nine circuits
      claim PI-equality with the borsh artifact, so the SpecAnchored arm
      of its divergence ledger is asserted unused — verify that is true
      rather than vacuous, and check Art::is_borsh_format() really
      closes the `== Art::Borsh` hazard a fourth artifact introduces
      (the same class of bug the stage-4 fork test caught).
- [ ] Cargo profile change (3790a85, [profile.dev.package."*"]
      opt-level = 3) + CLAUDE.md's new Testing section (cabd8fa): the
      routine loop is now plain `cargo test`, which means most gates
      from here on run UNOPTIMIZED for our own crates. Timings measured
      warm (1.9s vs 10.5s incremental; row_snapshot 89s vs 70s); what
      deserves a check is whether any suite's runtime crossed a
      practical threshold under dev, and whether "row_snapshot with
      --release" is actually being honoured by later agents.
- [ ] Milestones audit (94f5135): three bullets ticked from the tree
      rather than from a session's work — M3's differential bullet
      (eleven suites) and M4's corpus-rewrite bullet, plus the recorded
      claim that M4's two remaining bullets are now pure pointers to M16
      and M17. Check the pointer claim: that nothing in M4's original
      scope fell between the two new milestones.
- [ ] M8 (bf9ddfa corpus fixes, b4f9486 in-place regenerators, ad226d3
      the workflow + bump.sh): THE TAXONOMY IS THE ARTIFACT — twelve
      instruments each mapped to what a post-bump failure MEANS and which
      of four responses is right, and a wrong row there sends a future
      session to accept a baseline it should have investigated. Read it
      against what each instrument actually asserts, especially the rows
      claiming a bump CANNOT move an instrument (those turn a firing into
      a surprise, so a false "cannot" is the expensive direction). Second:
      b4f9486 changed the regenerators from PRINTING tables to REWRITING
      files in place between GENERATED BEGIN/END markers — the frozen
      snapshots are our primary drift instrument, and a regenerator that
      can silently rewrite them is a new way to lose a baseline; check the
      marker handling and that `accept` cannot run without leaving the
      diff visible. Third, bump.sh's `pins` reads the pinned compactc
      release tag's OWN flake.lock to derive the ledger revs — that is the
      mechanical closure of M1's version-string-vs-rev trap, so verify it
      reads the right lock and the right field. The dry run is recorded as
      real (ledger main has DIVERGED from our rev, not advanced: the bump
      was attempted, hit ReverseBytes and the secp256r1/Curve25519 IrTypes
      being gone, and was REJECTED) — the useful follow-up is not review
      but the recorded remainder: nobody has yet read a corpus diff
      produced by a different compiler, because upstream has not shipped
      one.
- [ ] M14 (f56ee2b design, af13448 BoundedUint, e08ad8e the correction,
      7ed49cc README): THE EXCLUSIVE-BOUND CORRECTION is the first thing
      to check, because it changed prose in three places that had been
      wrong since M0-era note-taking (notes/builtin-lowering.org §1 and
      §8, minocrab-abi's "enum → Uint<0..k-1>" line, interface-gen's
      render) — the claim is that NOTHING COMPILED was affected because
      Prim::unsigned always took the inclusive maxval the artifact
      publishes. Verify that claim rather than the prose: if any code path
      ever derived a bound from a printed type name, it would be wrong in
      exactly the way the notes were. Second: THREE WIDTHS now diverge
      where they used to coincide — constraint 2·⌈intlen(n)/2⌉,
      comparison intlen(n−1), FAB atom ⌈intlen(n−1)/8⌉ (18/17/3 for
      Uint<0..70000>) — read the two new const fns on Prim against
      compactc's artifact, since a sized type cannot distinguish them and
      so no existing test would have caught a wrong formula. Third: the
      Borsh rule (a bounded int serializes at the next Borsh primitive
      width up, FAB 3 → wire 4) is a NEW row in the fixed-width subset
      that spec/borsh-subset.md's prose does not yet describe — check
      whether the spec needs it before an external team meets a bounded
      field. The differential itself is self-checking and strong (equal to
      compactc up to identifier renaming, plus first-illegal/largest-legal
      acceptance at every arm), so it needs less attention than the above.
      Also weigh candidate 2 from the agent's for-dmd list, which is a
      real footgun rather than an ergonomics wish: a literal is
      range-checked against the COMPARISON width, not the bound, so
      b.lt(500u64) on a BoundedUint<300> compiles and is always true.
- [ ] M15 (a96874b design, f32473b leaf+reader+generator, 0e3775b
      fixture+port+differential): FOUR THINGS, roughly in order of
      how badly a mistake would hurt.
      (1) THE CURVE-POINT RECOGNITION is the highest-risk line in the
      milestone, and it is a positive/negative pair rather than a check.
      `CompactType::curve_point` says an `Alias { name, Opaque { tsType } }`
      is a curve point when `name == tsType` and the name is one of two.
      Getting it too WIDE silently hands a user's own
      `Opaque<"Secp256k1Point">` five FAB atoms and a `Prim::Point` — a
      wrong alignment, which is a wrong PI stream, on a type nothing else
      in the system would question. Getting it too NARROW restores the bug
      it fixes. There is a test per shape
      (`only_the_exact_curve_spelling_is_a_point`: right ts-type under a
      different alias, bare with no alias, an alias matching a non-curve
      name), but the tests are the agent's own reading of compactc's
      output — check the reading, i.e. that compactc really cannot emit a
      curve point any other way, and that no OTHER curve type is spelled
      `Opaque` (the agent verified `JubjubScalar`/`Secp256k1Base`/
      `Secp256k1Scalar` publish under their own `type-name`, from a
      compiled probe; that is the load-bearing negative).
      (2) THE `compress` SEMANTICS, which every sentence in
      `minocrab_std::v3::Opaque`'s docs rests on: the slot is
      `transient_commit(bytes, len)`, zero for empty, one-way. It is read
      off upstream's `ValueAtom::field_repr_unchecked` and it is TESTED
      (`the_compress_atom_is_a_transient_commitment` plus its rejecting
      twin), so this is the best-warranted claim in the milestone — but
      the CONSEQUENCE stated in the docs is a soundness statement a
      contract author will rely on ("`eq` decides equality of the TS-side
      values, up to Poseidon collision resistance"), so it deserves a
      second reading as a claim rather than as a test.
      (3) THE THREE `copy`s. `identical_instruction_streams` does not
      compare eleven of fourteen circuits to compactc directly — it
      applies `without_named_immediates` to BOTH sides for `opDefault` /
      `opMapKey` / `opMaybe` and requires equality after. The
      justification is M9 phase 7's measurement (a `copy` of an immediate
      is zero rows, 47 of them removed with `row_snapshot` bit-identical),
      and the substitution is guarded by an `assert_ne!` and a count test.
      What deserves eyes is whether the substitution can hide anything
      ELSE: it drops every `copy` whose `val` starts `0x` and substitutes
      textually on the serialized JSON, so check it cannot coalesce two
      distinct wires or mask a reordering. Also weigh the recommendation
      in notes/opaque-bridging.org §"As built" (leave it) — that is a
      dmd-facing overhead-for-ergonomics call, not the agent's.
      (4) `LedgerSet<T>`, three milestones early. Two methods composed
      from existing ops, so there is no new lowering to audit; what to
      check is the CLAIM that `Set.member` is the same `Op::Member` a
      map's is (read off the fixture's `opSet`, and the reason `member`
      delegates to `map_member` rather than getting a `set_member` twin),
      and that M16 inheriting a half-Set is recorded where M16 will see it
      (it is, in the milestone).
      Lower priority, self-checking: the leaf's trait impls (byte-identical
      ZKIR for all 98 pre-existing circuits, dumped before and after), both
      frozen snapshots (+14 entries, 0 deletions), and the generator rows
      (--check green over every generated crate). Also NEW AND USEFUL
      rather than reviewable: tests/zkir_dump.rs commits the
      dump-and-diff procedure four previous sessions did by hand.
      Two corrections the milestone made to earlier records, both worth
      confirming: `CompactType`'s doc claimed to model every corpus
      type-name and did not (four are unmodelled, all ledger-only, and
      `ContractInfo` never parses `ledger` — verify that last part, since
      it is what makes the gap harmless); and a witness's result type is
      spelled "result type" WITH A SPACE in all 66 witness entries, which
      is a landmine for anyone adding witness parsing with a copied
      `#[serde(rename = "result-type")]`.
      NOT DONE, documented: no fixture circuit sends an opaque across a
      real cross-contract boundary. `CallArg`/`CallResult` are implemented
      and the facts come from a probe, but nothing in the suite asserts
      them — so that pair is the one part of the leaf with no test behind
      it.
- [ ] M9 closure (312c8ff, the 32 experiment declarations): the ZKIR is
      byte-identical for all 87, so what deserves eyes is the LABEL
      STRING mapping — every free-string `c.disclose(w, "x (hi)")` pair
      became one `label!(X = "x")`, and a REPORT is the only place a
      wrong string would show, since nothing else reads a disclosure
      label. Check the strings against the pre-change call sites,
      especially xcall_with_payment's two different spellings for the
      same coin ("nonce"/"color"/"value" on the caller side, "coin
      nonce"/… on the target side, both preserved deliberately). Also
      check the two `Discloses<()>` claims (serde_builtin::
      check_roundtrip, xcall::target_deposit_big) really are circuits
      that disclose nothing rather than circuits whose disclosure was
      dropped — the generated test proves the SET matches the code, not
      that the code is right.
- [ ] M16 (the ledger ADTs): the differential is instruction-for-
      instruction over all 31 circuits, so the LOWERINGS check themselves
      — what deserves eyes is the three places a human decision was made.
      (1) `AnyWire3` now carries an `Arg` rather than a `Val`, which
      changes a type in the FRONTEND for a reason found in M16. The
      argument is that it closes M9 phase 8's rule at the last operand
      position that lacked it, the field was private, `Wire3::erase` is
      the only other constructor, and all 112 pre-existing circuits are
      byte-identical. That last claim is MEASURED, not inferred:
      tests/zkir_dump.rs at HEAD against HEAD+M16 gives `0 files differ,
      31 files added`, and both frozen snapshots regenerate to insertions
      only. What is left to check by eye is whether any caller wanted
      `Val` back — `AnyWire3` is used by borsh.rs's `Limbs`, hash.rs's
      `persistent_hash_compact`, and attest.rs — and whether
      `AnyWire3::immediate`'s visibility parameter (the LIST's, not the
      value's) is the right reading of the lattice.
      (2) `LedgerSet` and `LedgerMap` now delegate five methods each to
      the same `minocrab_ledger` functions, on the claim that compactc
      emits identical streams for Set and Map. The fixture proves it for
      remove/size/isEmpty/resetToDefault; what to check is that the
      DELEGATION is stated where a reader will meet it and not just in
      the note (`LedgerSet`'s doc says it).
      (3) The `history_append` asymmetry — an insert closes `ins 1;
      insc 2`, a reset `insc 2; ins 1` — is now a comment plus two call
      sites rather than a parameter. Worth reading against
      midnight-ledger.ss:1183-1190 and :1211-1228, since it is the one
      thing in M16 that was wrong on the first pass.
      Lower priority, self-checking: the fourteen op sequences (31 of 31
      byte-identical to compactc), the `MerkleTreeDigest` leaf (its ABI
      is checked against contract-info.json), the `Maybe` `CircuitOut`
      (same), and the DEPTH const assert (compile_fail doctest beside a
      passing twin).
      NOT DONE, documented: the three `insertCoin`/`pushFrontCoin` arms
      and nested ADTs — both in the milestone and in the README's gap
      list, both with a reason rather than a shrug.
- [ ] M17 (21 of the fixture's 24, three commits):
      the differential is instruction-for-instruction, so the LOWERINGS
      check themselves. THE ONE THING THAT NEEDS EYES is the `token_type`
      change, because it is the only edit this session that MOVED
      pre-existing circuits.
      `token_type` stopped naming its constant domain prefix with two
      `copy`s and now inlines it (M16's `AnyWire3::immediate`). It has ten
      call sites across all four vault forks, so 26 circuits moved,
      INCLUDING THE BENCHMARK CONTRACT. What to check: (a) the diff really
      is only those two `copy`s plus renumbering in all 26 — the claim is
      from `diff -rq` over a full zkir_dump, and one circuit is quoted in
      notes/kernel-tokens.org; (b) that BENCHMARK.md's numbers do not
      depend on the removed instructions — row_snapshot regenerated to +2
      lines and ZERO deletions, so `(k, rows)` is unchanged for all 26, but
      the bench harness measures prove TIME and nobody has re-run it;
      (c) that no OTHER `B32::pad` call site wants the same treatment, or
      that leaving them is deliberate.
      Second, `kernel.checkpoint()` is recorded as impossible rather than
      unported, on the strength of one compactc invocation and a reading of
      `assemble1`. Worth reproducing the failure independently, since a
      README gap bullet now asserts it.
      Third, the accumulator generalization: `kernel_mint_shielded` became
      `kernel_effect_add(slot, key, amount)` with five callers, and the
      claim is that not one instruction changed for the pre-existing
      caller. zkir_dump says so (M17 stage 2's dump was 0 files differ);
      worth confirming the M4 mintShielded differential still pins it.
      Lower priority, self-checking: the nine primitives and twelve stdlib
      circuits (21 of 21 byte-identical to compactc), `UserAddress`,
      `Either`'s LedgerRepr/Disclose, and `UnshieldedToken` carrying its
      own constants.
      NOT DONE, named in the differential's `NOT_YET_PORTED` with a test
      that fails if the list drifts: `sendShielded`, `mergeCoin`,
      `mergeCoinImmediate`. `mergeCoin` is only 59 instructions and every
      gadget it needs exists (`coin_nullifier_contract`, `coin_commitment`,
      the kernel claims) — the nonce evolution is the one piece that wants
      care, since it is a transient hash degraded and re-upgraded.
- [ ] M17's shielded stage (the fourth commit — `sendShielded`,
      `mergeCoin`, `mergeCoinImmediate`, and the fixture at 24 of 24):
      the differential is instruction-for-instruction, so the LOWERINGS
      check themselves. THE THINGS THAT NEED EYES are the three
      primitives it added to the CORE, because they are reachable from
      every circuit, not just these:
      (1) `Circuit3::copy`. It exists so a port can spell compactc's
      cast (`as Uint<128>` names its result; `degradeToTransient` names
      the limb). It is zero rows and cannot be unsound, but it IS a
      footgun-shaped method — its doc says so twice and says there is no
      reason to reach for it in ordinary code. Worth checking that the
      doc is discouraging enough, and that no non-stdlib caller has
      appeared.
      (2) `transient_hash` now takes `impl Into<AnyWire3>` rather than
      `&[Wire3]`, so its operand list matches `persistent_hash`'s and a
      constant absorbs inline. Every existing call site still compiles
      unchanged (the blanket `From<Wire3> for AnyWire3` does it), which
      is the property to check by eye — an inference change here would
      be silent.
      (3) `coin_commitment` split into `coin_commitment_to` (tag and
      address already chosen) and `coin_commitment_to_contract` (static
      `right(addr)`, no select at all). The claim is that the general
      entry point is unchanged for its existing callers — the four vault
      forks — and the zkir dump says so modulo the inlined constants.
      What to read is whether the THREE-function surface reads as one
      idea or three, since a reader who picks the wrong one gets either
      two dead selects or a wrong commitment.
      Second, the MEASURED MOVEMENT: 28 pre-existing circuits lost 42
      `copy`s (constants inlined in the two coin digests). Checked by
      substituting each removed copy's immediate for its output name and
      comparing — all 28 identical, row_snapshot +3 lines and zero
      deletions. Same claim shape as stage 3's `token_type`, and the
      benchmark contract is again among the moved.
      NOT DONE, and it is the natural next change rather than a gap:
      `minocrab_contracts::common`'s `receive_shielded` / `burn_coin` /
      `mint_shielded_token` are ad-hoc twins of the stdlib's shielded
      compositions and should be lifted onto them. It moves rows (~2 per
      static recipient, from the dropped `cond_select`s — measured in
      `opcost`: a select is 1 row, a copy is 0), so it needs its own
      commit and its own regenerated row snapshot.
- [ ] The constant-folding pass (notes/ir-passes.org §8):
      ONE JUDGEMENT NEEDS DMD, and it is stated at the top of §8. The pass
      makes us strictly more inlined than compactc (compactc names some
      constants it inlines elsewhere, driven by its frontend, not by any
      rule a ZKIR pass can see), so the four instruction-for-instruction
      differentials now fold BOTH sides before comparing. Their criterion
      is "identical instruction for instruction, modulo the naming of
      constants". Everything else still compares exactly and the
      normaliser IS the pass, but this is a relaxation of the primary
      warrant and it is dmd's to veto — the alternative is putting the
      fold behind the opt profile, which would keep the ports byte-equal
      and give up the deletions the pass is for.
      Second, `Disclosure3` gained a `Constant` arm. Worth checking that
      no consumer wants the old identifier — the valued report is the only
      one in tree, and it prints the constant.
      Third, the exhaustive `operands_mut` match: an upstream ZKIR
      instruction with a new operand position breaks the build rather than
      escaping the fold, which is the property to preserve on a bump.
      Self-checking: the 768 removed copies over 91 circuits (each dump
      equals its baseline after substituting the immediate for the name),
      row_snapshot and interface_snapshot unchanged, full suite green.
- [ ] GAP FOUND while answering dmd on contract shape (2026-08-16), not
      yet fixed: nothing asserts that every `#[circuit]` in the workspace
      appears in `support::circuits()`. That list — 170 hand-written
      entries in a TEST module — is the only statement of which circuits
      exist, and it feeds row_snapshot, interface_snapshot, zkir_dump and
      the adversarial suite. There are 156 `#[circuit]` attributes against
      those 170 entries (the gap is the `entry()`-built families, which
      carry no attribute), so the counts cannot simply be compared.
      A circuit added and not listed is covered by NOTHING and nothing
      fires. The snapshots catch the opposite direction only (a listed
      circuit missing from the frozen table), which is what fired when
      M17's three landed.
      The fix is the same one M18/the contract-shape question wants: a
      `#[contract]` block that DERIVES the set, so the list stops being
      hand-written. Until then this is a known hole, written down rather
      than assumed away.
