#+title: Version bumps: the checklist, the instruments, and what a failure means (M8)

The M8 deliverable: moving a pin is a chore, not a project. This file is the
procedure; =../bump.sh= is the procedure made runnable. Read §"Drift
taxonomy" when something fires — that is the part with the value in it.

Everything below was exercised on 2026-08-15 against real upstream state;
§"Dry run" records what actually happened, including the two bumps that were
attempted and rejected.

* The four pins
There are exactly four things that can move underneath us, and they move
independently.

| pin | where | controls |
|-----+-------+----------|
| compactc release | =flake.nix= =compactcVersion= + =compactcHashes= | the compiler that produces =corpus/zkir/= — every differential's other side, plus the bundled =zkir= / =zkir-v3= binaries |
| midnight-* crate rev | =Cargo.toml= =[workspace.dependencies]= (one rev, ~11 crates) + =[patch.crates-io]= | our ZKIR types, the Impact op set, the reference VM, =Transaction::well_formed= |
| corpus source revs | =corpus/sources.json= (=corpus/update.sh=) | which contracts the corpus holds |
| rustc | =flake.nix= =rust-bin.stable.latest= — *floating*, no version pinned | the language level; see Hazard 6 |

The first two are coupled and the coupling is the whole hazard surface: the
crates must describe the ZKIR that the *binaries* speak. Hazard 1.

PIN 5, noted 2026-09-04 (not yet a moving part, will be): the =zkir=
crate has been SPLIT OUT of midnight-ledger into
=midnightntwrk/midnight-zkir= (default branch =zkir-v3=). We still
reach =midnight-zkir-v3= through the ledger git rev, which works only
as long as the ledger rev we pin still vendors it. midnight-zkir's tip
already carries an instruction batch (their PR #10, merged 2026-08-28:
=LoadConstant=, =And=/=Or=/=Xor= + =Bool=, =Byte=, =Bytes(n)= +
=Nth=/=Concat=/=Slice=, =Sha512=, stricter decode) that no compactc
release uses — compactc 0.34.0 still pins zkir-v3 at =04c9c5d=. When a
release adopts it, the corpus, our bindings, the 34-arm Lean semantics
(=crates/minocrab-zkir/lean=) and upstream's Agda spec (their PRs
16/17, notes/zkir-semantics.org §11) all move in one bump. Add the
midnight-zkir rev to =./bump.sh pins= when the crate is pinned
directly.

* The checklist
Run everything inside the direnv/nix shell (=nix develop -c …= also works).
Nix supplies BINARIES ONLY; every build is plain cargo.

** 0. Look before touching anything
#+begin_src sh
./bump.sh pins
#+end_src
Prints, on one screen: our four pins and the tool version strings; the
midnight-ledger revs the *pinned compactc release* was itself built from
(read out of that release tag's own =flake.lock=); whether a newer compactc
release exists; how midnight-ledger =main= relates to our rev; and which
corpus sources have moved. Needs network.

** 1. Move the pin
*** compactc
#+begin_src sh
# the new hash, verified reproducible 2026-08-15 (5.6s, hits the nix store):
nix store prefetch-file --json --hash-type sha256 \
  "https://github.com/LFDT-Minokawa/compact/releases/download/compactc-v<V>/compactc_v<V>_aarch64-darwin.zip" \
  | jq -r .hash
#+end_src
Edit =compactcVersion= and the per-arch entry in =compactcHashes=
(=notes/zkir.org= §"Option B" carries the older hashes and the URL scheme).
Then =direnv reload= (or re-enter =nix develop=) and check =compactc
--version= — bearing in mind Hazard 2.

*** midnight-* crates
One rev, everywhere:
#+begin_src sh
sed -i '' 's/<old-rev>/<new-rev>/g' Cargo.toml   # 11 workspace deps + 2 [patch] lines
cargo metadata --format-version 1 >/dev/null     # resolve only; seconds
#+end_src
=[patch.crates-io]= must move with them (Hazard 4). Do NOT split the rev
across crates: every =midnight-*= dep shares one rev so their =Fr=,
=ProofPreimage= and =IrSource= types unify (Hazard 3).

*** corpus sources
#+begin_src sh
./corpus/update.sh [name]     # repin to upstream HEAD, refetch, recompile
#+end_src

** 2. Recompile the corpus — compactc bumps only
#+begin_src sh
./corpus/compile.sh           # ~312 contracts; rewrites corpus/zkir/ + compile-report.tsv
#+end_src
Nothing else regenerates the other side of the differentials. A crate-rev
bump does not need this; a corpus-source bump does it for you.

** 3. Run the gates
#+begin_src sh
./bump.sh gates               # add --heavy for the elevated property run
#+end_src
Ordered by diagnosis value, cheapest first, with a PASS/FAIL/seconds summary
and, on failure, the taxonomy line for whatever fired. It aborts immediately
if the pin does not even resolve, since every later stage would only reprint
the same cargo error.

** 4. Read the taxonomy
§"Drift taxonomy" below. Each instrument's failure has exactly one of four
correct responses: ACCEPT the new baseline / FIX OUR LOWERING / REPIN /
INVESTIGATE UPSTREAM. Deciding which is the only part of a bump that is not
mechanical.

** 5. Accept the new baseline — only what step 4 justified
#+begin_src sh
./bump.sh accept              # every regenerator, in dependency order
./bump.sh gates               # and prove it
#+end_src
See §"Accepting a new baseline".

** 6. Numbers, if they moved
=./bench.sh= then refresh =BENCHMARK.md=. This is publishing, not a gate: no
bump is blocked on it, but a bump that moved =k= for any benchmarked circuit
leaves the published table wrong.

** 7. Commit shape
Two commits, not one: the *pin* (=flake.nix= / =Cargo.toml= / =Cargo.lock= /
=corpus/=) and the *accepted baselines* (snapshots, pins, spec, generated
crates). The second commit's message is where the reasons from step 4 go —
one line per moved instrument. A reviewer must be able to read the baseline
commit alone and know why each number moved.

* Drift taxonomy
What each instrument is, what a failure MEANS after a bump, and the response.

** 1. Dependency resolution — =cargo metadata=
Seconds. Fails when the new rev does not provide a crate we name.
- MEANS: upstream renamed, split or dropped a crate.
- RESPONSE: INVESTIGATE UPSTREAM — find the new name and decide whether the
  rename is the whole change or the visible edge of a bigger one. Observed
  for real: =midnight-ledger-v9= is =midnight-ledger= at main HEAD (dry run
  B).

** 2. Corpus compile report — =corpus/compile-report.tsv=
The gate compares the OK count against =git show HEAD:=. 312 ok / 166 fail
today.
- FEWER ok MEANS: the new compiler rejects contracts the old one accepted —
  a language-level break (a pragma range, a removed builtin, a stricter
  check). The failures are *data*, not bugs to fix in the corpus: read the
  new first-error lines in the report diff.
- MORE ok MEANS: the new compiler accepts contracts that used to fail —
  usually good, and it means the corpus grew new artifacts that everything
  downstream now sweeps.
- RESPONSE: ACCEPT (commit the report) once you can say which class each
  moved line is in. If a *sig-net* contract stopped compiling, stop: that is
  a REPIN decision, because those contracts are the benchmark and the ports.

** 3. ZKIR round-trip — =cargo test -p minocrab-zkir=
Parses, re-emits and re-parses every ZKIR v3 =.zkir= in the corpus (84 at
the current pin; the 722 v2 artifacts are counted and skipped — M36
retired the v2 bindings on 2026-09-05, since nothing in the workspace
targets v2 and the v2 reading answered a question no gate asks). The
=toolchain_accepts= v2 mock-compile test went with them; the v3 toolchain
is exercised in-process by the bench (keygen + prove through
=midnight-zkir-v3=) and by every differential's =IrSource::check=.
- MEANS: compactc's own output no longer round-trips through our bindings —
  the JSON envelope, an instruction's field set, or the instruction set
  itself moved. This is the most fundamental instrument in the tree: if it
  is red, nothing downstream tells you anything new.
- RESPONSE: FIX OUR LOWERING (the bindings in =crates/minocrab-zkir=), never
  regenerate anything. If the change is large, REPIN and wait: a
  pre-release toolchain is allowed to move the IR.

** 4. ABI + Impact baseline — =cargo test -p minocrab-ledger=
=entry_point.rs= sweeps every =contract-info.json= under =corpus/zkir= (312
contracts, 618 circuit names) through the entry-point hash rule;
=differential_baseline.rs= is the noop/base Impact op stream.
- MEANS: =contract-info.json='s shape, the entry-point hash rule, or the
  Impact op encoding moved.
- RESPONSE: FIX OUR LOWERING. Everything cross-contract is downstream of
  this — the interface crates' six checks, =contract_call='s commitment, the
  vault's calls to the Signet singleton — so fix it here before reading any
  other failure.

** 5. The differential suites — the test targets named =*differential*= (18 at 2026-08-17; count with =ls crates/*/tests/*differential*.rs= rather than trusting this line)
Our circuit vs compactc's artifact for the same contract: same typed I/O
schema, equal =Preprocessed::pis= / =pi_skips= on a shared =ProofPreimage=
(=notes/ledger-abi.org=). Instruction streams are free to differ.
- MEANS: our lowering and the new compactc's disagree about the STATEMENT.
  A bump can cause this legitimately — the compiler changed how it frames
  something — and it is the one failure class that is never "accept".
- RESPONSE: FIX OUR LOWERING, or REPIN if the new framing is a pre-release
  experiment. Read the failing side first: a *schema* mismatch is an
  argument-list change (see 8), a *pis* mismatch is a framing change.
- NOT covered here: =erc20_vault_opt=, =_borsh= and =_modern= diverged from
  compactc deliberately (M10/M11). Their gate is the fork ledger
  (=erc20_vault_*_fork.rs=) plus the spec harness, so a compactc bump cannot
  move them; only their own reference model can.

** 6. Artifact agreement + the pins — the interface crates
=crates/{signet-signer-interface,xcall-target-interface}/tests/artifact_agreement.rs=:
the committed =artifact/pin.json= must be the distillation of the corpus
artifact, its digests must match the bytes, and every declared circuit must
pass the six checks against the compiled =.zkir=. Nine mutation cases prove
the checker bites.
- Digest/pin failure MEANS: the corpus was recompiled, i.e. exactly what a
  compactc bump does. RESPONSE: ACCEPT — =./bump.sh accept= re-copies
  =contract-info.json= and re-distills the pin.
- =every_declared_circuit_matches_the_artifact= failure MEANS something
  else entirely: the callee's ARGUMENT LAYOUT moved. RESPONSE: this is a
  semver event for the published interface crate, not a regeneration; see
  =notes/interface-crates.org= §Versioning.
- =pin.json= also records =compiler_version= / =language_version= /
  =runtime_version= — after a compactc bump those three fields are the
  cheapest confirmation that the artifact really was rebuilt.

** 7. Codegen check — =cargo test -p minocrab-interface-gen=
Regenerates every crate carrying =artifact/generator.json= and compares byte
for byte with the committed =src/lib.rs=.
- MEANS: the artifact changed and the generated crate was not regenerated
  (or someone hand-edited it).
- RESPONSE: ACCEPT via =./bump.sh accept= step 3 — but only after 6 is
  understood, because the generator's input is the artifact.

** 8. Interface snapshot — 87 circuits' ordered (label, type) lists
=crates/minocrab-contracts/tests/interface_snapshot.rs=.
- MEANS: an argument, output or witness read moved, was renamed or was
  inserted. A toolchain bump reaches this only through OUR code (a stdlib
  port changing shape), so after a pure pin move this firing is a surprise
  and deserves the investigation.
- RESPONSE: ACCEPT only with a written reason per line; argument order feeds
  the communications commitment, so movement breaks deployed callers.

** 9. Row snapshot — =(k, rows)= for 87 circuits, =--release=
=crates/minocrab-contracts/tests/row_snapshot.rs=. The M7 note calls this
M8's asset and it is: a toolchain bump that changes lowering shows up as a
moved row count per circuit.
- MEANS: cost changed. After a compactc bump the corpus side may move
  without our side moving at all (the snapshot prices OUR circuits) — so a
  moved row here after a *pin-only* change means our own lowering depends on
  something in the crates that moved.
- RESPONSE: ACCEPT with a per-circuit reason, and re-run =./bench.sh= if any
  =k= crossed. BLIND SPOT (measured, =notes/const-generics.org=): an extra
  =Copy= of an immediate costs 0 rows, so this instrument cannot see one.
  Instruction-stream identity is an argument you make by reading a dump.

** 10. Spec, vectors and TypeScript — =serialization_conformance=
Three drift tests: the generated region of =spec/borsh-subset.md=, every
=spec/vectors/*.json= byte for byte (and the directory holding exactly the
generated names), and every file in =spec/ts/=. Plus the node suite (39
tests) behind =--ignored the_typescript_vectors_pass=.
- MEANS: the schema walk's output moved. A toolchain bump cannot cause this
  — the generator's input is OUR spec types, not compactc — so this firing
  after a pin move means something in =borsh=/=bincode= moved instead.
- RESPONSE: INVESTIGATE first, then ACCEPT. These files are what the TS and
  MPC sides implement against; a silent regeneration is a published format
  change.

** 11. The spec/adversarial property harness
=erc20_vault_spec.rs= (9 circuits) and =erc20_vault_adversarial.rs= (19),
=PROPTEST_CASES= scaled by =./bump.sh gates --heavy=.
- MEANS: the ledger semantics our reference model executes against changed —
  this is the crate-rev bump's instrument, since the model runs op streams
  through the pinned =QueryContext::query=.
- RESPONSE: FIX OUR LOWERING or the model, never the assertion. A failing
  case here is the harness doing its job; the M10 notes record two real bugs
  it found.

** 12. Bench — =./bench.sh=
Not a gate. Proving time and RSS, three artifacts. Run it when 9 moved a
=k=; its output changes =BENCHMARK.md=, which is a published claim.

* Known hazards
** Hazard 1 — a version string is not a rev (the M1 trap)
The =ledger-9.1.0.0-rc.3= tag's =zkir-v3= says version =3.0.0-rc.2= and so
does the =zkir-v3= binary in our compactc zip — and they are *different
code*. That cost an M1 session; it is why every =midnight-*= dep pins a
40-character rev and never a tag.

The authority is not the version string and not the branch: it is *the rev
the compact release's own =flake.lock= pins for the binaries it ships*.
=./bump.sh pins= prints it. Today (2026-08-15) it reads:

: onchain-runtime-v4  4823b5351b17cc49e30f19760dbd30a73cf95e22  ledger-9.1.0.0-rc.3
: zkir                4823b5351b17cc49e30f19760dbd30a73cf95e22  ledger-9.1.0.0-rc.3
: zkir-v3             b17df9d100812bfb2621a3e342108158e3c6b412  -
: zkir-v3-wasm        b17df9d100812bfb2621a3e342108158e3c6b412  -
: zkir-wasm           4823b5351b17cc49e30f19760dbd30a73cf95e22  ledger-9.1.0.0-rc.3

We pin ONE rev (=04c9c5d9=) where upstream pins two, because all
=midnight-*= crates must share a rev for their types to unify. So our pin
cannot equal both — and in fact equals neither: =04c9c5d9= is 2026-07-31,
sixteen days AFTER the =compactc-v0.33.0-rc.2= release, chosen in M1 because
it is what the compact flake pinned *on main* at the time and its =zkir-v3=
matches the binary's =3.0.0-rc.2= where the rc.3 tag's did not. What makes
that safe is not the rev choice but instrument 3: 788 of compactc's own
artifacts round-trip through these bindings, and 14 differential suites
agree with its lowering. The rev is a hypothesis; the corpus is the proof.

** Hazard 2 — =compactc --version= drops the pre-release suffix
The binary from =compactc-v0.33.0-rc.2= reports =0.33.0=. It cannot
distinguish rc.2 from a future final 0.33.0, so never use it to confirm a
bump landed; use =flake.nix= and the nix store path.

** Hazard 3 — one rev for every midnight-* crate
=Fr=, =ProofPreimage= and =IrSource= cross crate boundaries constantly. Two
revs in the graph gives two incompatible =Fr= types and an error message
that names the same type twice. If a bump forces a split, it is not a bump.

** Hazard 4 — the =[patch.crates-io]= mirror
=midnight-zkir= depends on =transient-crypto-old= ^2.2.0, never published to
crates.io. We mirror midnight-ledger's own workspace patches:
=midnight-transient-crypto= from tag =transient-crypto-2.2.0-rc.1=, and
=serialize= / =base-crypto= forced onto the pinned rev so old and new
transient-crypto share one set of core types. The patch block moves with the
rev — check upstream's own root =Cargo.toml= =[patch]= section at the new
rev and mirror it again. When it goes stale cargo does NOT fail; it warns
=patch `midnight-transient-crypto v2.2.0 (…)` was not used in the crate
graph= and carries on with the unpatched dependency. Dry run B printed
exactly that. Grep the build log for =was not used in the crate graph= after
any rev bump.

** Hazard 5 — per-source =--feature-zkir-v3=
The three sig-net sources carry =flags: ["--feature-zkir-v3"]= in
=corpus/sources.json=; everything else compiles v2. A toolchain that renames
the flag, or makes v3 the default, silently changes which IR 66 corpus
artifacts are in — visible as a mass round-trip or differential change, not
as a compile failure. Check the flag against the new release notes before
blaming the bindings.

** Hazard 6 — rustc floats
=flake.nix= pins =rust-bin.stable.latest.default=, i.e. whatever stable is
current when the flake inputs are updated (1.97.1 today). =nix flake update=
can therefore move the compiler with no diff in our files. Nothing in the
tree needs a feature newer than min-const-generics, but
=notes/const-generics.org= §"Toolchain caveat" lists designs that would be
revisited if =generic_const_exprs= stabilised.

** Hazard 7 — direnv does not follow a git worktree
A dry run in =git worktree= gets no toolchain: =.envrc= is untrusted at the
new path, so =cargo: command not found=. Export the environment from the
main checkout FIRST and then =cd=, or use =nix develop -c=. Cost of learning
this the hard way: one wasted gate run (dry run B).

** Hazard 8 — a diff that is not the bump
Two now fixed, both found by dry run A: =corpus/compile.sh= sorted its
report in the caller's locale (26 lines of churn on any recompile, since
en_US collation ignores =.= and =_=), and =corpus/update.sh= reformatted
=sources.json= through jq. The report now sorts under =LC_ALL=C= and
=sources.json= is stored in jq's own formatting, so a corpus bump's diff is
the revs and the artifacts.

** =target/= grows without bound, and a bump is when it bites (2026-08-16)

A bump rebuilds the world, and cargo NEVER prunes =target/=: every fingerprint
change writes a fresh generation and keeps the old one. Measured at 36 GB
before this was noticed, of which *29.8 GB was loose =.o= debug info across
218,997 files* — macOS's dev default is =split-debuginfo = "unpacked"=, so
DWARF lands in a per-codegen-unit object beside the artifact. There were 13
copies of one dependency rlib and 6 of the bench binary.

This workspace amplifies it three ways: 29 test targets in minocrab-contracts
alone (each its own binary), minocrab-std built twice (the =borsh-schema=
feature duplicates the graph below it), and dependencies at =opt-level = 3=.

FIXED at the profile: =[profile.dev] debug = "line-tables-only"= keeps file
and line in every backtrace and panic and drops the type/variable DWARF
nothing here reads. A full clean test run now leaves 3.5 GB rather than 36.
Before a bump, =cargo clean= anyway — the pre-bump generations are dead by
definition, and the bump's own rebuild is a full one regardless.

* Accepting a new baseline
=./bump.sh accept= runs every regenerator in dependency order, because after
a real bump several move together and regenerating them one at a time is how
a session ends with a half-accepted tree:

1. re-copy each interface crate's =artifact/contract-info.json= from the
   corpus (nothing else does this — =Pin::distill= reads the corpus but the
   committed copy is the crate's own);
2. =regenerate_pin_and_schema= per interface crate;
3. =minocrab-interface-gen --crate= per generated crate;
4. =regenerate_spec= — =spec/borsh-subset.md='s generated region,
   =spec/vectors/*.json=, =spec/ts/=;
5. =regenerate_row_snapshot= and =regenerate_interface_snapshot=
   (=--release=; it prices 87 circuits).

Then it prints =git status --short= and says the only thing that matters:
every line of that diff needs a reason before it is committed.

The two snapshot regenerators WRITE their tables in place now (M8) rather
than printing them for a human to paste — the table sits between =GENERATED
BEGIN= / =GENERATED END= markers and =support::rewrite_generated_region=
splices it. Verified 2026-08-15: regenerating at HEAD reproduces both
committed tables byte for byte.

NOT regenerated, deliberately: =LAYOUT_SNAPSHOT= in
=serialization_conformance.rs= (=print_layout_snapshot= still prints). It
freezes the Borsh spec types' own layout, which no toolchain bump can move —
only our own type definitions can, and that is a code review, not a chore.

* Dry run — 2026-08-15
Timings from an M4 Mac with a warm build cache unless stated.

** Baseline, before anything moved
| =cargo test --workspace= | 469 passed / 0 failed / 27 ignored | 1m50s |
| =cargo test --release … row_snapshot= | 1 passed, table unmoved | 1m25s |

** What upstream actually offers (=./bump.sh pins=)
- *compactc: nothing newer exists.* The newest release of
  LFDT-Minokawa/compact is still =compactc-v0.33.0-rc.2= (2026-07-15), the
  one we pin. Three =compactc-dev-<sha>= pre-releases predate it. So the
  toolchain bump this milestone is about could not be performed for real —
  there is nothing to bump to.
- *midnight-ledger main HEAD =c0d37686= (2026-08-14) has DIVERGED from our
  pin*, it is not ahead of it. Merge base =d34a6723= (2026-06-25); main
  carries 85 commits we do not have, our rev carries 342 that main does not.
  Our pin is on the line the compact toolchain builds from, not on main.
- *Corpus: two sources moved* — =example-counter= and =example-bboard=.
  Everything else, including all three sig-net repos and the compact repo
  itself, is at its pinned rev.

** Exercise A — the corpus source bump (performed, then reverted)
#+begin_src sh
./corpus/update.sh example-counter    # 2.0s
./corpus/update.sh example-bboard     # 2.0s
#+end_src
: >> example-counter -> 273f083ab36a
:    1 files (+0 negative, +0 extra)
: compiled 1/1 OK
: >> example-bboard -> b687a824fad0
:    1 files (+0 negative, +0 extra)
: compiled 0/1 OK

RESULT: *content-neutral*. =corpus/src/= and =corpus/zkir/= did not change
at all — both repos moved without touching their =.compact= files — and the
report stayed at 312 ok / 166 fail (bboard's single contract was already
failing on its pragma before the bump). The two "changes" in the diff were
both artifacts of our own scripts, and both are now fixed (Hazard 8): 26
locale-sorted lines in =compile-report.tsv=, and a jq reformat of
=sources.json=.
DECISION: reverted. The bump buys nothing and the pins stay where they are.
Recorded here so the next session does not re-derive it.

** Exercise B — midnight-ledger =04c9c5d9= → main HEAD =c0d37686=
In a throwaway worktree, following the checklist.

Step 1, =sed= the rev in =Cargo.toml= (14 occurrences), then =./bump.sh
gates=. It aborted in *2 seconds* at stage 1:

: error: no matching package named `midnight-ledger-v9` found
: location searched: git+https://github.com/midnightntwrk/midnight-ledger?rev=c0d37686…

Upstream renamed the crate: at main HEAD it is =midnight-ledger= (edition
2024), no =-v9= suffix. One line fixed it (=package = "midnight-ledger"=),
and resolution then succeeded — Cargo re-locked the whole =midnight-*= graph
without complaint, which is exactly the trap: *resolution passing means
nothing*.

Step 2, the build. =cargo test -p minocrab-zkir= reached
=Compiling minocrab-ir= and stopped with 17 errors, all of them in the same
three classes:

: error[E0599]: no variant … named `Secp256r1Point` found for enum `IrType`
: error[E0599]: no variant … named `Curve25519Point` found for enum `IrType`
: error[E0559]: variant `Instruction::PersistentHash` has no field named `output`
: error[E0559]: variant `Instruction::Keccak256` has no field named `output`
: error[E0599]: no variant named `ReverseBytes` found for enum `Instruction`

Confirmed against the source at both revs: =zkir-v3/src/ir_types.rs= has 39
=Secp256r1= mentions at =04c9c5d9= and *zero* at =c0d37686=; =ir.rs= has
=ReverseBytes= at ours and not at theirs. This is not a forward change we
have to catch up with — it is the divergence above. The secp256r1/Curve25519
typed leaves (04c9c5d9 IS the commit "Add secp256r1 operations to ZKIR") and
=ReverseBytes= are on our line and not on main's. =ReverseBytes= in
particular is load-bearing: M7 pass 1 spent it to replace ~4,600 rows of
explode/rebuild with ~150.

Two further things the attempt printed, both worth keeping:
- =warning: patch `midnight-transient-crypto v2.2.0 (…transient-crypto-2.2.0-rc.1)`
  was not used in the crate graph= — Hazard 4 firing exactly as described:
  the =[patch]= block is pinned to the old line and goes silently inert.
- rustc's own suggestion, =help: there is a variant with a similar name:
  IrType::Secp256k1Base= — main HEAD has secp256*k*1 only.

VERDICT: *not a bump — a branch change*, and one that would cost the M7
optimisation and the v3 curve leaves. Rejected.

COST OF FINDING OUT, and a fix it paid for: the first run took *8m33s*,
because after the compile error every remaining stage rebuilt and reprinted
the same 17 errors (=FAIL 268s / 24s / 70s / 122s / 28s=). That is a defect
in the gate runner, not in the bump, so =bump.sh= gained a =workspace
builds= stage (=cargo test --workspace --no-run=) that aborts the sequence.
Re-run with it: *7.2 seconds*, one error block, one line of diagnosis.

** Exercise C — synthetic drift, to prove the instruments fire
Exercise B never reached a test, so the test instruments were exercised by
deliberately breaking things at HEAD and checking each fired *as the
taxonomy predicts* — including which ones stay silent, which is the half
that is easy to get wrong. All reverted.

*** C1 — an extra constraint (a lowering change that preserves the statement)
One duplicated =c.assert(le(amount, u64::MAX))= in =erc20_vault::deposit=.

| instrument | predicted | observed |
|------------+-----------+----------|
| row snapshot (9) | FAIL, deposit only | FAIL: =erc20_vault::deposit: expected k=15 rows=17502, got k=15 rows=17512 (+10)= |
| differentials (5) | PASS | PASS — all 30, including =deposit_matches_corpus= |
| interface snapshot (8) | PASS | PASS |

This is the M3 equivalence criterion made visible: instruction streams are
free under PI-equality, so a redundant constraint is invisible to compactc's
own artifact and *only* the row snapshot sees it. A toolchain bump that
changes lowering without changing the statement looks exactly like this.

*** C2 — two same-typed arguments swapped (a wire-contract change)
=max_fee_per_gas= and =max_priority_fee_per_gas= exchanged in =deposit='s
parameter list — both =Uint<128>=, so the type column cannot see it.

| instrument | predicted | observed |
|------------+-----------+----------|
| interface snapshot (8) | FAIL with the reorder named | FAIL: =- in maxFeePerGas= / =+ in maxFeePerGas= around =maxPriorityFeePerGas= |
| differentials (5) | FAIL on PI | FAIL, 1 of 30: =deposit_matches_corpus= — =public transcript input mismatch for input 65; expected: Some(7e), computed: Some(06)= |
| row snapshot (9) | PASS | PASS (a reorder costs nothing) |

The pair is the point: C1 moves rows and not PIs, C2 moves PIs and not rows.
Neither instrument alone would have caught both.

** Exercise D — checklist steps 2 and 5, which B never reached
A compactc bump's two remaining steps are "recompile the corpus" and
"accept". Both were run at HEAD, where the answer is knowable: recompiling
with the SAME compiler must reproduce the corpus, and accepting at HEAD must
change nothing. If either is false, neither is usable as a bump instrument.

*** D1 — is a corpus recompile a diff?
=./corpus/compile.sh signet-midnight-examples= (1.8s, 1/1 OK) and
=./corpus/compile.sh signet-midnight-integration= (4.3s, 6/6 OK) — the
erc20-vault and the Signet singleton, the two artifacts the benchmark and
the differentials lean on hardest — left =corpus/zkir/= *byte-identical*.
Then the whole corpus:

: ./corpus/compile.sh   →  compiled 312/478 OK   3m11s
: git status --short corpus/zkir   →  (nothing)

*Zero* of the 788 artifacts moved, and the report's ok/fail counts are the
committed ones. So compactc is deterministic over this corpus and step 2
costs three minutes. That is what makes the bump diff readable: after a real
bump, every changed artifact is a change the NEW COMPILER made.

*** D2 — is =./bump.sh accept= a no-op at HEAD?
It has to be, or it is not a tool, it is a random number generator. Run at
HEAD it rewrote, in order: two =contract-info.json=, two =pin.json= + two
=interface-schema.txt=, two generated =src/lib.rs=, the generated region of
=spec/borsh-subset.md=, five =spec/vectors/*.json=, seven =spec/ts/= files,
and both snapshot tables. =git status= afterwards listed *not one* of them.
Exit 0, 8m03s wall — of which most was cargo rebuilding the =midnight-*=
graph between per-crate invocations, since resolving features for one
package at a time is not the same resolution as for the workspace. Step 2
now batches its packages into a single =cargo test= for that reason.

** What the dry run says about the exit criterion
Cost of the whole loop, measured: =pins= ~15s, corpus recompile 3m11s,
=gates= on a green tree ~6-9m (dominated by one debug and one release build
of the 87-circuit snapshot), =accept= a few minutes. A bad pin is rejected
in *2 seconds* by resolution and in *7 seconds* by the build stage — cheap
enough that attempting a bump costs nothing, which is the property that
makes it a chore.

What the dry run could NOT do is take a real compactc release, because none
exists past the one we pin, so the corpus never changed and the
accept-a-moved-baseline path was only exercised as an idempotent no-op. The
instruments that a compactc bump would move (2, 3, 4, 5, 6, 9) were fired
synthetically instead (Exercise C), and the two halves of the missing
experiment — a deterministic recompile, and an =accept= that writes exactly
what was already there — were each verified on their own (Exercise D).

Left for the first real bump, and it is the honest remainder: nobody has yet
read a corpus diff produced by a *different* compiler and decided what it
means. The taxonomy says how; only the event can prove the saying.

* Bump 1 — 2026-09-05: compactc 0.33.0-rc.2 → 0.34.0 (language 0.26.0)
The first REAL bump (M31). The dry run above could not take one because
none existed; this one is a FULL release, shipped 2026-08-25, and the
compiler genuinely changed for its own reasons.

Written in two passes, deliberately: §"Step 0" (the pins and the
PREDICTIONS) was committed with the pin commit, BEFORE the corpus was
recompiled, so the reading in §"The reading" is a prediction checked
rather than a story fitted afterwards. The revision history is the
evidence for that claim: the prediction list has an earlier commit than
the artifacts it predicts.

** Step 0 — the pins, before anything moved
=./bump.sh pins=, 2026-09-05:

: flake.nix compactc            0.33.0-rc.2
: Cargo.toml midnight-ledger    04c9c5d9bcebb8d4427d8589fb54d58a55599c14
: Cargo.toml [patch] tag        transient-crypto-2.2.0-rc.1
: compactc --version            0.33.0
: zkir --version                midnight-zkir 2.2.0
: zkir-v3 --version             midnight-zkir-v3 3.0.0-rc.2
: rustc --version               rustc 1.97.1 (8bab26f4f 2026-07-14)
: locked crate versions         2.2.0 3.0.0-rc.2
:
: compactc releases (newest first):
:   compactc-v0.34.0       Latest        2026-08-25   <- the bump
:   compactc-v0.34.0-rc.1  Pre-release   2026-08-24
:   compactc-v0.34.0-rc.0  Pre-release   2026-08-19
:   compactc-v0.33.0-rc.2  Pre-release   2026-07-15   <- the old pin
: midnight-ledger HEAD    c8365dc6dd3f  diverged: main has 99 commits we
:                         do not, we have 342 main does not (merge base
:                         d34a672369f0, 2026-06-25)
: corpus sources MOVED: compact, oz-compact-contracts, example-counter,
:                       example-bboard, aa-midnight-evm-experiment
:                       (all three sig-net sources: same)

TWO PINS DELIBERATELY NOT MOVED IN THIS BUMP, so the diff is readable
(hazard 8 at scale): the corpus source revs — five moved and stay where
they are, because a source bump and a compiler bump in one diff is the
thing the taxonomy cannot separate — and the midnight-ledger rev, which
is DIVERGED from main, not behind it (dry run B's verdict stands).

*** Hazard 1 re-read: what 0.34.0 was itself built against
=compactc-v0.34.0='s own =flake.lock=:

: onchain-runtime-v4  4823b5351b17cc49e30f19760dbd30a73cf95e22  ledger-9.1.0.0-rc.3
: zkir                f227f1e5771c165d829501b830e36b4acbb411ec  -
: zkir-v3             04c9c5d9bcebb8d4427d8589fb54d58a55599c14  -   <-- OUR Cargo pin
: zkir-v3-wasm        04c9c5d9bcebb8d4427d8589fb54d58a55599c14  -
: zkir-wasm           4823b5351b17cc49e30f19760dbd30a73cf95e22  ledger-9.1.0.0-rc.3

This is the fact that bounds the blast radius, and it moved IN OUR
FAVOUR: 0.33.0-rc.2 pinned =zkir-v3= at =b17df9d1= while we pin
=04c9c5d9=, so the old pin's binaries and our crates were the same
version STRING from different code (hazard 1's whole point). 0.34.0
pins =04c9c5d9= — exactly our rev. The bundled =zkir-v3= binary and our
=midnight-zkir-v3= crate are now the same code. So the ledger pins do
not move in this bump, and nothing in =Cargo.toml= is touched.

The v2 =zkir= dependency DID move (=4823b535= → =f227f1e5=); that is
the printer behind the 722 v2 corpus artifacts, which nothing in the
workspace targets since M36 retired the v2 bindings. Prediction P10.

*** Hazard 2 re-read
=compactc --version= reports =0.34.0= — and =compactc-v0.34.0-rc.1=
would report the same string. The authority is =flake.nix= plus the nix
store path (=/nix/store/wxf3jda7d8590l1d32x5nqx70f6amnna-compactc-0.34.0=),
never =--version=. Confirmed in the shell after the pin moved. The old
binary reported =0.33.0= for an rc.2, which is the same trap.

*** THE PREDICTIONS — what could plausibly move a corpus artifact
Sources read before any recompile: the =compactc-v0.34.0= release notes
(https://github.com/LFDT-Minokawa/compact/releases/tag/compactc-v0.34.0)
and, because those summarise only, the repo's own per-patch
=CHANGELOG.md= at that tag
(https://raw.githubusercontent.com/LFDT-Minokawa/compact/compactc-v0.34.0/CHANGELOG.md),
entries =0.33.100= through =0.34.0= — 23 patch releases between our pin
and this one. Ranked by how likely each is to move an artifact:

- *P1 — =Uint<0..1>= alignment =bytes:0= → =bytes:1= (0.33.108, issue
  #588).* The single highest-risk item. Single-variant enums lower to
  =Uint<0..1>=; the old compiler declared alignment =bytes:0= yet still
  put one (always-zero) value in the transcript. Predict: every artifact
  with such a field moves its transcript AND its =contract-info.json=,
  i.e. taxonomy 2/4/5, not merely 3. Corpus reach: midnight-serde's
  =serde-fixtures.compact= (=Bounded.unit: Uint<0..1>=). OUR reach: this
  is a behaviour we MIRRORED — =crates/minocrab-std/src/v3/borsh.rs=
  §754 and notes/bounded-integers.org §3 both state "Uint<0..1> FAB 0
  bytes". If the fix is real, our mirror is now wrong: answer (b).
- *P2 — hashing alignment for point/scalar types (0.33.105 #608,
  0.33.107).* =persistentHash= / =persistentCommit= / =keccak256= did
  not handle alignment for =JubjubPoint=, =Secp256k1Point=,
  =JubjubScalar=, =Secp256k1Base=, =Secp256k1Scalar= in the ZKIR v3
  backend; #608 CRASHED the compiler on =keccak256<Secp256k1Point>=.
  Predict: the sig-net v3 artifacts (Signet.compact, circuits.compact,
  erc20-vault.compact, test-caller-contract.compact — all four use
  =Secp256k1Point=) move, and something that used to fail may compile.
- *P3 — =secp256k1EthereumAddress= now asserts a non-identity input
  (0.33.107).* A NEW CONSTRAINT in a stdlib circuit: rows up, stream
  moves, statement changes. Corpus reach: =compact/examples/ecdsa=
  re-exports it; the Signet port if it uses it.
- *P4 — ECDSA message hash read as a big-endian secp256k1 scalar z
  (0.33.100).* =secp256k1EcdsaVerify= / =…Recover= changed HOW the
  message hash is interpreted. Predict: the Signet ECDSA circuits move,
  and this is semantic — which signatures verify — so if our port
  mirrors the old reading it is answer (b), and if the artifact's PI
  framing moves it is (c).
- *P5 — =secp256k1EcdsaRecover= and =Secp256k1EcdsaSignatureWithRecovery=
  REMOVED from the standard library (0.33.100).* Predict FEWER ok for
  any source naming them: =compact/compiler/zkir-v3-library.compact= is
  the only corpus hit.
- *P6 — point/scalar types are no longer built-in and no longer
  exportable from a contract's top level (0.33.118, BREAKING).*
  Predict FEWER ok. Corpus hits that export such a ledger field at top
  level: =compact/examples/secp256k1/arithmetic.compact= (=export ledger
  base: Secp256k1Base=), =signet-midnight-examples= erc20-vault
  (=export ledger mpcResponseKey: Secp256k1Point=) and
  =signet-midnight-integration= test-caller-contract (same). *If the
  sig-net two stop compiling this is a finding for their owners* and it
  takes the erc20-vault differential and an interface crate with it.
- *P7 — stdlib =add= / =mul= removed (0.33.113).* Only the
  =Secp256k1Base= / =Secp256k1Scalar= ones; every corpus =add(= / =mul(=
  is a user-defined circuit (oz-midnight-apps math, test-center
  composable). Predict NO change.
- *P8 — =Uint<0>= is now legal (0.33.111).* Predict no change: the
  corpus negatives are never compiled.
- *P9 — nested ZKIR-v3 native values get proper ledger conversions
  (0.33.122, issue #704).* The fix unified three near-duplicate code
  paths (hashing / Impact =popeq= / every other Impact instruction), so
  it can move IMPACT OP STREAMS, not only ZKIR. Predict: contracts
  nesting a point in a ledger structure move — oz-compact-contracts
  =ConfidentialFungibleToken= (=Map<Bytes<32>, JubjubPoint>=), the
  sig-net =Secp256k1Point= ledger fields — and taxonomy 4 is in scope.
- *P10 — the v2 =zkir= printer moved with the release's flake.lock.*
  Predict wholesale churn across the 722 v2 artifacts, invisible to
  every gate (round-trip counts and skips v2 since M36) and visible only
  as corpus diff volume.
- *P11 — the v3 printer now respects =--no-communications-commitment=
  (0.33.112).* We never pass it (=corpus/sources.json= passes only
  =--feature-zkir-v3=, hazard 5). Predict NO change.
- *P12 — manifest hashes computed in-process (0.33.121).* Build speed.
  Predict no artifact change.
- *P13 — cross-contract callees may do Zswap coin operations
  (0.33.119).* Runtime and =CircuitContext= work. Predict no =.zkir=
  change; possible =contract-info.json= movement for callee coin ops.

PREDICTED TO STAY STILL, and worth saying because it is the half that is
easy to get wrong: our own artifacts (the =zkir_dump= instrument), the
row and interface snapshots, =spec/vectors= and =spec/ts=. Our lowering
does not read compactc. If any of those move, the bump is not what it
appears to be and the session stops.

A STRUCTURAL NOTE recorded before the recompile, because it decides what
"the corpus moved" even means: seven differential fixtures
(=crates/minocrab-contracts/tests/fixtures/{adts,bounded,coins,kernel,nested,opaque,wide}/out/zkir=)
are compactc artifacts committed BY HAND, not produced by
=corpus/compile.sh=. A pin bump does not touch them, so those
differentials keep comparing against 0.33.0-rc.2 output until someone
recompiles them. That is a decision for step 5, and it is listed in the
decision list below.

** The gates, in taxonomy order
=nix develop -c ./bump.sh gates=, on the recompiled corpus. The
workspace stage was re-run with =--no-fail-fast= afterwards, because
=cargo test --workspace= stops at the first failing TARGET and a
classification made from a truncated list is not a classification.

| # | stage | taxonomy | result |
|---+-------+----------+--------|
| 1 | dependency resolution | 1 | PASS (0s) — no pin moved but compactc |
| 2 | workspace builds | — | PASS (104s) |
| 3 | corpus compile report | 2 | PASS (0s) — 316 ok, was 315 |
| 4 | ZKIR round-trip | 3 | FAIL, then PASS — see below |
| 5 | ABI + Impact baseline | 4 | FAIL, then PASS — see below |
| 6 | workspace | 5/6/7/8/10/11 | FAIL — three tests, two classes |
| 7 | row snapshot (release) | 9 | PASS (173s) |
| 8 | TypeScript vectors (node) | 10 | PASS (2s) |

*** Stage 4 — taxonomy 3, and it is the corpus's size, not the IR
: round-tripped 92 v3 corpus .zkir files (739 v2 files skipped)
: corpus size moved: 831 files (92 v3) … left: (831, 92) right: (814, 92)
ZERO round-trip failures. What fired is the ASSERTED COUNT, which exists
so a partial checkout cannot be silently green — and it fired because
the corpus grew by the 17 artifacts of the newly-compiling
=compact/examples/types=. The v3 count is unchanged at 92. Moved in the
corpus commit with the source named, per the assertion's own message.
The same for =lean_roundtrip='s "722 v2 skipped" -> 739 (the Lean
round-trip agrees with the Rust one on the new corpus) and, at stage 5,
=entry_point='s 315 -> 316. =lean_dataflow='s 16666 did NOT move, and
that is a result: it walks the v3 artifacts, and no v3 artifact moved.

*** Stage 5 — taxonomy 4, and again a count
=ep_hash_matches_upstream_for_every_corpus_circuit=: 316 artifacts where
315 was asserted. The hash RULE is untouched — every circuit name in the
corpus, including the new source's, still hashes to upstream's entry
point — and =differential_baseline= (the noop/base Impact op stream) is
green. Pin moved in the corpus commit.

*** Stage 6 — three failures, two classes, and everything else green
Green, and worth listing because silence is the half that is easy to get
wrong: 19 differential targets (taxonomy 5) of which 18 fully pass and
the 19th fails only in a parse, =interface_snapshot= (8),
=serialization_conformance= and the 52 node vectors (10), the
=erc20_vault_spec= / =erc20_vault_adversarial= harness (11), and 101 of
105 test targets overall.

**** 6a. =opaque_differential::the_vaults_own_initialize_now_flattens= — taxonomy 4, FIX OUR LOWERING
: contract-info parses: Error("unknown Compact type-name `Secp256k1Point`
:   — compactc grew a type this crate does not model", line 145)
Our own error message, doing its job. =minocrab-abi='s
=parse_type= (=crates/minocrab-abi/src/info.rs:201-236=) already models
=JubjubScalar=, =Secp256k1Base= and =Secp256k1Scalar= as first-class
=type-name=s, because compactc already emitted those that way; the two
POINT types arrived as =Alias=-of-=Opaque= and 0.33.118 made them
first-class too. The fix is two arms in that match plus whatever
=CurvePoint= mapping they need — it is a code change, not an accepted
baseline, so it is on the decision list rather than in this branch.

**** 6b. both interface crates' =the_committed_pin_is_the_artifacts_distillation= — taxonomy 6, ACCEPT
=signet-signer-interface= and =xcall-target-interface=. The diff of the
two =Pin= values is the whole reading:
: compiler_version: "0.33.0" -> "0.34.0"
: language_version: "0.25.0" -> "0.26.0"
: runtime_version: "0.18.0-rc.1" -> "0.19.0"
: contract_info_sha256: eba48362… -> b30f60ef…   (signer)
: contract_info_sha256: a65b0b3f… -> ff78362c…   (xcall target)
and *every other field is identical*: each circuit's =zkir_sha256=, its
=inputs=, its =do_communications_commitment=, and its full =constraints=
list, on all six circuits. This is the taxonomy's "the corpus was
recompiled, i.e. exactly what a compactc bump does" — ACCEPT via
=./bump.sh accept=, which re-copies =contract-info.json= and re-distills
the pin. Note what did NOT fire:
=every_declared_circuit_matches_the_artifact= passed on both crates, so
the callee ARGUMENT LAYOUT did not move and this is not a semver event
for either published crate (notes/interface-crates.org §Versioning).

*** Our own side did not move, and here is the evidence rather than the claim
- =row_snapshot= (release, 209 circuits) PASS against the table frozen
  before the bump; =interface_snapshot= PASS likewise.
- =git diff= of the whole bump under =crates/= is FOUR count assertions
  in three test files. No source that builds a circuit changed, and the
  dump is deterministic, so the 209 ZKIR dumps cannot have moved;
  =zkir_dump= runs green and writes all 209.
- =spec/vectors=, =spec/borsh-subset.md= and =spec/ts= untouched, node
  suite 52/52.
Exactly as predicted. Our lowering does not read compactc, and this bump
is the first evidence for that claim from a compiler that actually
changed.

** The reading — per family, with the answer
Five families cover every moved byte.

*** Family A — the 814 unchanged .zkir. No answer needed, and it is the finding
Every committed artifact compiled by 0.33.0-rc.2 is byte-identical under
0.34.0. Dry run D1 established that a recompile with the SAME compiler
reproduces the corpus; this is the stronger statement, and it is what
makes the rest of the diff readable. Whatever 23 patch releases changed,
they did not change the lowering of anything this corpus contains.

*** Family B — the version restamp (630 files, all of them) → ACCEPT
=contract-info.json= and =contract-manifest.json= carry
=compiler-version= / =language-version= / =runtime-version=, and the
manifests carry the sizes and hashes of the generated JS those strings
live in (index.js is 5 bytes shorter: "0.18.0-rc.1" -> "0.19.0"). Not
one of (a)/(b)/(c): it is the recompile's own fingerprint. The taxonomy
already names it — §6's "after a compactc bump those three fields are
the cheapest confirmation that the artifact really was rebuilt".

*** Family C — the point types stop being aliases (8 contracts, 12 sites) → (a)
: - "type-name": "Alias", "name": "Secp256k1Point",
: -   "type": { "type-name": "Opaque", "tsType": "Secp256k1Point" }
: + "type-name": "Secp256k1Point"
CHANGELOG 0.33.118. The answer is (a) — upstream changed how it FRAMES
something and the statement is preserved — and the evidence is specific
rather than inferred:
- the =.zkir= of all eight contracts is byte-identical, so no
  constraint, no instruction and no public input moved;
- both interface pins' per-circuit =zkir_sha256=, =inputs= and
  =constraints= are unchanged;
- =every_declared_circuit_matches_the_artifact='s six checks pass on
  both interface crates, so the argument LAYOUT is unchanged;
- =row_snapshot= and the 18 green differentials say the same from our
  side.
It is emphatically NOT (c): a wire-contract change would have moved the
argument list, the entry-point hash or a PI, and each of those has an
instrument that stayed silent. What it does cost is one code change in
=minocrab-abi= (6a above), because our ABI model is a closed enumeration
of compactc's =type-name=s BY DESIGN — the error message it produced is
the design working.

*** Family D — a source the corpus gained (17 new v2 artifacts) → data
=compact/examples/types/examples.compact=, rejected by 0.33.0-rc.2 with
"Uint width 0 is not between 1 and the maximum Uint width 248", accepted
by 0.34.0 because =Uint<0>= became legal (0.33.111). Taxonomy 2's "MORE
ok MEANS the new compiler accepts contracts that used to fail — usually
good, and the corpus grew new artifacts that everything downstream now
sweeps". All 17 round-trip; all 17 are v2.

*** Family E — issue #588, which moved NOTHING here and is still a live divergence → (b)
This is the one worth the milestone. P1 predicted that 0.33.108's
=Uint<0..1>= alignment fix (=bytes:0= -> =bytes:1=) would move
artifacts. Nothing in the corpus or in the seven fixtures moved. That is
not evidence the fix is absent — it is evidence about what we compile,
and the difference matters, so it was PROBED rather than assumed.

A three-circuit probe (a ledger cell of =Uint<0..1>=, a ledger cell of a
single-variant enum, and a read of the first), compiled under both
pinned compilers with =--feature-zkir-v3=:

: setUnit.zkir, setOnly.zkir, readUnit.zkir all differ, each by ONE byte:
: - { "op": "impact", "guard": "0x01", "inputs": ["0x11","0x01","0x01","0x00","%x.0"] }
: + { "op": "impact", "guard": "0x01", "inputs": ["0x11","0x01","0x01","0x01","%x.0"] }
: - { "op": "impact", "guard": "0x01", "inputs": ["0x0c","0x01","0x00","%t.0"] }
: + { "op": "impact", "guard": "0x01", "inputs": ["0x0c","0x01","0x01","%t.0"] }

The alignment operand of the Impact op: 0 -> 1. The fix is real, it
lands in the IMPACT stream (taxonomy 4, not 3), and it fires only when
such a value sits in LEDGER position. No corpus contract and no fixture
puts one there — the =bounded= fixture's =b1(x: Uint<0..1>)= takes it as
a circuit ARGUMENT and is byte-identical under both compilers — which is
why every gate stayed silent.

OUR SIDE MIRRORS THE OLD BEHAVIOUR, in exactly one place:
=crates/minocrab/src/v3/abi.rs:183= =uint_atom_bytes(maxval) =
integer_length(maxval).div_ceil(8)=, which is 0 at =maxval = 0=, with
the doc comment "Compact's =Uint<0..1>= occupies no bytes, which is what
the corpus's own serde fixtures say about it". It reaches
=AlignmentAtom::Bytes { length: 0 }= through =BoundedUint='s
=CircuitAbi= (=minocrab-std/src/v3/entry.rs:215=) and
=minocrab-abi/src/info.rs:443= (where a single-variant ENUM takes the
same path), and from there into =minocrab-ledger='s =push_cell=
alignment header — i.e. into the same operand the probe shows moving.
Three assertions pin the old value: =v3_bounded.rs:185=
(=atom(BoundedUint::<1, Private>::atoms()) == 0=), =abi.rs:542=
(=uint_atom_bytes(0) == 0=), and notes/bounded-integers.org §3.

So this is answer *(b): upstream FIXED something we mirrored, and the
mirror should move with it.* It is not urgent — nothing deployed, no
contract of ours uses a =Uint<0..1>= or single-variant enum in ledger
position, which is why no instrument fires — and it is not mine to
change under this milestone's scope. It is decision-list item 2, and it
is worth noticing that =bounded_differential='s
=compactc_s_abi_agrees_with_the_leafs= CANNOT catch it: it flattens
compactc's =contract-info.json= through OUR =uint_bytes=, and
contract-info publishes =maxval=, not the alignment. Both sides of that
equality come from the same formula. Whatever fixes the mirror should
bring an instrument that would have seen this.

** The predictions, checked
Thirteen were written down before the recompile. Four held, one held in
mechanism and failed in reach, seven were right to check and did not
fire, and one — the most alarming — was wrong in the good direction.

| # | prediction | outcome |
|---+------------+---------|
| P1 | Uint<0..1> alignment moves artifacts | MECHANISM CONFIRMED, REACH ZERO — family E; a probe shows the one-byte Impact change, no artifact we compile has one in ledger position |
| P2 | point/scalar hashing alignment moves the sig-net v3 artifacts | DID NOT FIRE — all four are byte-identical |
| P3 | secp256k1EthereumAddress's new assert moves rows | DID NOT FIRE — the only corpus source exporting it does not compile on the v2 path, before or after |
| P4 | the ECDSA big-endian z change moves the Signet circuits | DID NOT FIRE, and PROBED rather than assumed: a =secp256k1EcdsaVerify= probe compiled under both pins is byte-identical (97 lines each), and so are signet-contract's three compiled circuits |
| P5 | sources naming the removed ECDSA stdlib names lose ok | INCONCLUSIVE — the one corpus hit (zkir-v3-library.compact) fails before and after, now on the type import instead |
| P6 | the point/scalar types stop being built-in → FEWER ok, sig-net at risk | HELD FOR THE COMPACT REPO, WRONG FOR SIG-NET — two compact-repo files change their error to "unbound identifier Secp256k1Scalar"; all 23 sig-net rows stay ok. Exporting a LEDGER FIELD of a point type is still fine; the breaking change is exporting the TYPE |
| P7 | stdlib add/mul removal changes nothing here | HELD |
| P8 | Uint<0> legality changes nothing (negatives are never compiled) | HALF WRONG, and it is the +1: it did not change a negative, it turned =examples/types/examples.compact= from fail to ok |
| P9 | #704's nested-native fix moves Impact streams | DID NOT FIRE — no contract-info's ledger shape and no artifact moved |
| P10 | the v2 zkir printer moved, so the 722 v2 artifacts churn | WRONG — every v2 artifact is byte-identical too, despite the release's flake.lock moving zkir from 4823b535 to f227f1e5 |
| P11 | --no-communications-commitment: no change | HELD |
| P12 | in-process manifest hashing: no change | HELD |
| P13 | callee Zswap ops: no .zkir change | HELD |
| — | OUR artifacts, snapshots, spec and vectors stay still | HELD, and it is the load-bearing one |

The pattern in the misses is worth keeping: every prediction that
over-reached did so by reasoning from a CHANGELOG entry to "an artifact
must move" without asking whether anything we compile is in the shape
the entry describes. The corpus is narrower than the language. The one
prediction that was right about the mechanism (P1) needed a purpose-made
probe to show it at all — which is the argument for probes as a standard
part of a bump reading, not an extra.

** Step 6 — the numbers
notes/compact-findings.org §4 deferred a re-measurement to "the
bump-time bench", because 0.34.0's notes mention smaller circuits and
better CSE. Here it is, and the answer is that nothing moved.

Method: the two benchmark contracts — signet-contract (the Signet
singleton, 3 circuits) and erc20-vault (17 circuits since M28) — were
recompiled from the SAME sources with BOTH pinned compilers into a
scratch tree, and priced with Midnight's own cost model
(=minocrab_sim::v3::cost=, the =zkircost= example, which is what
=row_snapshot= freezes for our circuits). Proving was not re-run: the
artifacts are byte-identical, so prove time and RSS cannot move, and
=./bench.sh='s value here would be its noise.

: diff -rq old/zkir new/zkir  →  (nothing) for both contracts

| circuit | 0.33.0-rc.2 (k, rows) | 0.34.0 (k, rows) | Δ |
|---------+----------------------+------------------+---|
| initialise | 10, 891 | 10, 891 | 0 |
| approveStata | 14, 8483 | 14, 8483 | 0 |
| approveRouter | 14, 8516 | 14, 8516 | 0 |
| startDeposit | 14, 11406 | 14, 11406 | 0 |
| completeDeposit | 16, 40776 | 16, 40776 | 0 |
| startWithdraw | 15, 32751 | 15, 32751 | 0 |
| completeWithdraw | 16, 40553 | 16, 40553 | 0 |
| refundWithdraw | 16, 40273 | 16, 40273 | 0 |
| startSwap | 16, 43592 | 16, 43592 | 0 |
| completeSwap | 16, 52536 | 16, 52536 | 0 |
| refundSwap | 16, 40279 | 16, 40279 | 0 |
| startSupply | 15, 32693 | 15, 32693 | 0 |
| completeSupply | 16, 42639 | 16, 42639 | 0 |
| refundSupply | 16, 40283 | 16, 40283 | 0 |
| startRedeem | 16, 35646 | 16, 35646 | 0 |
| completeRedeem | 16, 42639 | 16, 42639 | 0 |
| refundRedeem | 16, 40283 | 16, 40283 | 0 |
| signBidirectional | 16, 50429 | 16, 50429 | 0 |
| respond | 16, 40931 | 16, 40931 | 0 |
| respondBidirectional | 16, 40931 | 16, 40931 | 0 |

Twenty circuits, no =k= crossed, no row moved. BENCHMARK.md is
therefore still correct and is NOT re-stamped — the taxonomy's rule for
§12 is that the bench is publishing, run when 9 moved a =k=, and 9 did
not move at all. compact-findings §4 stands as written: the
explode/rebuild serialization cost 0.34.0 might have improved, it did
not improve here.

** Decision list for dmd
The milestone's compromise says: if the reading is broad, deliver the
classified reading plus a decision list rather than a forced accept.
This bump was NARROW, so the list is short — but =./bump.sh accept= was
deliberately not run, and nothing was regenerated. Four items:

1. *ACCEPT the two interface pins* (family B/C, taxonomy 6). Run
   =./bump.sh accept=; the reviewed diff should be the three version
   fields and the two =contract_info_sha256= in each =pin.json=, plus
   the re-copied =contract-info.json=, and NOTHING else — every
   =zkir_sha256= and every =constraints= list must come back
   unchanged. If anything else moves, that is not this bump.
   =interface-schema.txt= should also be unchanged, since the argument
   layout did not move.
2. *FIX =minocrab-abi='s type model* (family C, taxonomy 4): add
   =Secp256k1Point= and =JubjubPoint= as first-class =type-name= arms in
   =crates/minocrab-abi/src/info.rs='s =parse_type=, alongside the
   scalar arms already there, and give them the same =CurvePoint=
   answer the =Alias=-of-=Opaque= spelling gets today. One test then
   goes green (=opaque_differential::the_vaults_own_initialize_now_flattens=)
   and =opaque.compact='s own fixture should be recompiled with it (5
   below). This is our lowering following theirs, not an accepted
   baseline.
3. *DECIDE the =Uint<0..1>= alignment* (family E, answer (b)). Upstream
   now emits alignment 1 where we emit 0, for a =Uint<0..1>= or
   single-variant enum in ledger position. Nothing of ours has one, so
   nothing fires; the question is whether =uint_atom_bytes(0)= becomes
   1 now or when something needs it. Whatever is decided, the gap is
   that no instrument would have caught it — =bounded_differential='s
   ABI check reads =maxval= and recomputes the alignment with OUR
   formula, so both sides agree by construction. A probe contract in
   ledger position, compiled and committed like the other seven
   fixtures, is what would have seen it.
4. *DECIDE the seven hand-compiled fixtures.*
   =crates/minocrab-contracts/tests/fixtures/{adts,bounded,coins,kernel,nested,opaque,wide}/out=
   are compactc artifacts committed by hand, so a pin bump leaves them
   at the OLD compiler while everything around them moves. Recompiled
   under 0.34.0 in a scratch tree for this reading: all seven compile,
   every =.zkir= is byte-identical, and only =opaque='s
   =contract-info.json= changes beyond the version strings (the same
   point-type reshape, four sites). So refreshing them is safe and
   nearly empty — but it is a decision, and it should be one commit
   that says the artifacts did not move.
5. *An upstream finding to consider reporting* (theirs, not ours): on
   the ZKIR v2 path 0.34.0 lost a good diagnostic. 0.33.0-rc.2 said
   "secp256k1 is not supported in ZKIR v2: try recompiling with the flag
   =--feature-zkir-v3="; 0.34.0 says "unbound identifier
   Secp256k1Scalar", because 0.33.118 moved those names into a
   standard library injected only for v3. Two corpus sources show it.
   Recorded in notes/compact-findings.org for dmd's review.
6. *A latent diff-that-is-not-the-bump* (hazard 8):
   =corpus/.compact-path/@sig-net/midnight= is a tracked symlink with an
   ABSOLUTE target, so =corpus/compile.sh= rewrites it in any checkout
   that is not =/Users/dmd/minocrab= — it was restored by hand in this
   session rather than committed. Make it relative, or untrack it and
   let compile.sh build it.

M8's honest remainder is answerable now, and the answer is on the
narrow side of what the taxonomy braced for: a corpus diff produced by a
different compiler HAS been read, and it was 630 metadata files, twelve
ABI type nodes, one gained source, and not a single moved instruction.
