#+title: The decision backlog — everything waiting on dmd (2026-09-06)

STATUS 2026-09-06 (evening): dmd answered inline (the indented lines
under each item). Ticked = answered and routed to its milestone entry;
IN FLIGHT = an agent is building it on a worktree branch.

One line per decision: what it is, where the reasoning lives, the options,
the recommendation, and what it unblocks. Ordered by what it unblocks.
Nothing here is built; nothing here is blocking anything an agent can do
without it. Tick and annotate in place; the answer goes back into the
milestone entry it belongs to.

* TODO for dmd (2026-09-06 evening, after the backlog above was cleared)
Everything below is yours to decide, review or do; nothing an agent can
take without you. Ordered by what it unblocks.
- [X] T1. THE ATTESTATION DIGEST (notes/signet-async.org §10; the pinned
  test =the_typed_settle_digest_is_not_the_digest_the_mpc_signs=). Under
  Poseidon the typed =Pending= settle hashes one limb per Borsh leaf; the
  MPC hashes the output packed as one 31-byte-limb byte string. Options:
  (i) pack the output as one =BytesN= in-circuit before hashing —
  unilateral, RECOMMENDED; (ii) a typed =compute_response_hash= on the
  MPC side, coordinated release. UNBLOCKS: M35 E (generated round trips),
  and the typed lineage's deployability at all.
  dmd 2026-09-07: "Yes" — (i), pack in-circuit. DONE the same day: =calculate_attestation_digest_borsh= (and =Pending::refund='s two candidates, through =attestation_transient_hash_borsh=) pack the output as one byte string via =Serializer::finish_limbs=; the models mirror it; =signet_round_trip.rs= settles the deposit/claim pair against the sim's OWN attestation and refuses a rogue root's. notes/signet-async.org §10.1.
- [ ] T2. HAND OVER notes/event-attestation-cases.org to the MPC team.
  Ask them one question it could not answer: can the respond schema
  express an array of tuples (Multicall3's =aggregate3= return)?
  dmd 2026-09-07: "I'll do that" — with dmd.
- [X] T3. THE BATCH'S CHANGED DEFAULT (notes/nonce-admin.org §8): the
  handle is the slot's OWN insert counter, not the Signet request
  counter, because the flush consumes the Signet counter once per
  record. Accept, or say what you would rather key the queue by.
  dmd 2026-09-07: "that's fine" — accepted; the handle stays the slot's own insert counter.
- [ ] T4. THE REQUEST COUNTER'S REPLACEMENT — you said you would design
  it (§1.1/§2.1). The batch is built around the counter staying; the
  spent-id set (§2.1) is the recorded alternative for unbatched paths.
  dmd 2026-09-07: "Number the requests the same way as the nonce (let me know if this is impossible)". POSSIBLE for every batched request, and cheap — nonce-admin.org §9 has the reading and the one caveat (the unbatched, user-keyed paths keep the counter or gain the spent set); the build goes with the Admin rung.
- [X] T5. THE ADMIN HALF OF M39 C, two calls before an agent builds it:
  the administrator is the deployer key or a ROTATABLE commitment (§3
  recommends rotatable); and the shape of the generic EVM-transaction
  layer the fee material moves into (your A3 answer: ERC-20 token > EVM
  transaction > sig transaction — say where nonce, fees and value sit).
  dmd 2026-09-07: the ROTATABLE admin commitment; nonce, fees and value sit on the BASE EVM-transaction layer "because everything needs it". Recorded in nonce-admin.org §9; the build is the next M39 C rung (an agent can take it).
- [ ] T6. REVIEW the "kept, for dmd" list in notes/edsl-trim.org §D —
  the one-spelling audit removed two forms and kept the rest with
  reasons; a verdict on each closes M36.
- [ ] T7. REVIEW the three cargo-deny ignores (deny.toml: bincode and
  paste unmaintained, chacha20 yanked — all via the pinned ledger rev).
- [ ] T8. THE REVIEW QUEUE: notes/review-queue.org's evening entry —
  seven landings, the executor first (its termination argument and
  =decode_program= are the two things a human should read).
- [ ] T9. Still open from E above: push/close the GitHub items (E1),
  the M27 hand-back (E2), the upstream ZKIR spec watch ~2026-09-18
  (E3), the M39 formal model (E4).
  dmd 2026-09-07: "close what you can" — E1 DONE: issue #6 closed with the workflow link, issue #11 filed pointing at TRUST.md. E2, E4 are dmd's; E3 waits for the date.
Agent work waiting on none of this (already queued in milestones.org):
M26's harness distillation from tests/support on the executor; M35 E's
unread-field sweep; PUBLISHING.md's update for the signet-* crates;
retiring the vault's prover-chosen =evm_nonce= onto =Queued= when
sig-net's contract lands (P9).

* A. Decisions that unblock scheduled work
- [X] A1. THE M29 F PIN (notes/mpc-publisher.org §9 "The decision this
  leaves"). This repo pins one midnight rev (04c9c5d); mpc pins per-crate
  release tags at the same semvers. Options: keep our single rev / adopt
  mpc's tags. RECOMMEND: keep the single rev — =zkir-v3= has no release
  tag and the official compact flake pins the same rev; revisit when mpc
  actually links =minocrab-publisher=. UNBLOCKS: M30 C (the DUST wallet)
  and =notes/zkir.org='s pin table.
  
Use the single rev

- [X] A2. (Hold lifted; the executor BUILT and merged 2026-09-06 — M35 D done, M35 E waits on the attestation-digest finding in signet-async.org §10, M26's harness distillation next.) THE M26 HOLD on the authoring path — "not before the super
  review" (milestones.org M26, last open item). The super review is now
  run externally (M23's closing line). Options: lift / keep. RECOMMEND:
  lift, and build the v3 TRANSCRIPT EXECUTOR first (signet-async.org
  §9 "BLOCKED": the instruction walk in gather mode against live
  state) — it is the one missing piece behind two rungs. UNBLOCKS: M35
  D's state-driving half, M35 E (generated per-slot round trips), and
  the typed contract-testing harness in minocrab-sim.

Remove the super review.

- [X] A3. THE FEE ENVELOPE'S HOME (notes/evm-interfaces.org §4 adj. 5,
  §5 q6, §6 q5; notes/nonce-admin.org §3). =FIXED_PRIORITY_FEE= /
  =FIXED_MAX_FEE= still live in =erc20_vault= and =evm::build_tx=
  imports them from a deployment. Options: (i) move them into the
  library as =Envelope::fixed='s default now; (ii) leave until M39's
  =FeePolicy= cell. RECOMMEND: (i) now (a one-commit zero-movement
  move), and (ii)'s cell later under a ROTATABLE admin commitment rather
  than the deployer key (nonce-admin.org §3's recommendation). UNBLOCKS:
  the library no longer importing from a deployment; M39 C's shape.
  NOTE: M39 B–D stay parked until your formal model, per your call.
  
Wait then move all the fee stuff into a generic EVM transaction.

The heirachy should be something like ERC-20 token > EVM transaction > sig transaction, with relatively clean boundaries between them.

- [X] A4. M38 E — dynamic ABI types (=bytes=, =string=, =T[]=). The
  milestone says "until a consumer asks". Known would-be consumers:
  ERC-721 =safeTransferFrom(…, bytes)= (evm-interfaces.org §6 q2), Safe
  =execTransaction=, Multicall3. RECOMMEND: wait for a consumer; the
  =AbiArg= bound already refuses a dynamic type at compile time.
  
Wait until we have a consumer

* B. API judgement calls recorded by the rungs (accept or reverse)
- [X] B1. Three completions KEEP their owner gate (=complete_swap= / =complete_supply= / =complete_redeem= mint proceeds to the caller; notes/evm-calls.org §11). Reversing means the recipient comes out of the stored environment — a protocol change. RECOMMEND: accept.

  Accept

- [X] B2. =Envelope::literal= — a public knob whose only effect is ZKIR identifier numbering, kept so =swap= / =redeem= stay byte-identical (evm-calls.org §11 item 5). Alternative: renumber after folding in the serializer, which moves every circuit in the tree once. RECOMMEND: keep for now; schedule the renumbering only alongside another deliberate tree-wide movement.

  Do reccomended.
  
- [X] B3. (DONE 2026-09-06, merged — M38 H.) =Filing::GAS_LIMIT= override (evm-interfaces.org §2.3, §5 q1, §6 q5). Two would-be consumers so far (=Permit= at the 100k default; none needing an override yet). RECOMMEND: add it now — three lines, zero movement, closes the question.
Do it
  
- [X] B4. =IncreaseAllowance= / =DecreaseAllowance= are NOT in OpenZeppelin v5 (§5 q2). RECOMMEND: keep — the deployed tokens that matter predate v5.

Keep

- [X] B5. =erc721::TransferFrom='s =tokenId= as =U256= not =U128= (§6 q1). RECOMMEND: keep U256 (hash-derived ids exist).

Keep

- [X] B6. (DONE 2026-09-06, merged — M38 H; the signature is eight flat words, not a struct.) Add Aave =supplyWithPermit= (static, nested-tuple signature; §6 q3) — the natural friend of ERC-2612 =Permit= for an MPC-signed caller. RECOMMEND: yes, one oracle row and one call type.

Yes

- [X] B7. A payable =request_with= / =Fired::request_with= (§5 q3). No consumer. RECOMMEND: wait.
  
Wait
  
- [X] B8. (DONE 2026-09-06: notes/event-attestation-cases.org — 12 cases verified against source, PAXG dropped, Aave supply confirmed sufficient today, Multicall3's schema expressibility left unverified; hand to the MPC team.) =Unit=-return tickets are worth strictly less (UsdtLike, Aave =Supply=/=Borrow=, ERC-721 transfers: "mined without reverting" only; §5 q5, §6 q4). The only fix is success from the EVENT, on sig-net's side (evm-calls.org §3.1 option ii). RECOMMEND: raise with sig-net; nothing to decide here.

OK, can you write up (as tersely as possible) some concrete examples where what we have isn't good enough, and we need attestations on events? Aim for commonly used specific cases and verify hard that they're the case. I'll hand that over to the MPC team.
  
- [X] B9. WORDS inference (=Pending<F, Env, WORDS>= still names the word count; evm-calls.org §3). A =generic-array= rewrite of =EventRecordV2= would infer it and re-type every record user. RECOMMEND: no.

  No

* C. The publisher (notes/mpc-publisher.org §10 "Open questions")
- [X] C1. (DONE 2026-09-06, merged — crates/signet-protocol; M30 C1.) Hoist the Misc envelope constants and the three event names (~6 constants) into a small PUBLISHABLE crate so mpc's graph does not pull the corpus crate. RECOMMEND: yes, when mpc links the crate (or now — it is an hour).

  Sounds good
  
- [X] C2. (DONE 2026-09-06, merged — M30 C2.) Build =Deployment= from the package's =expectedVk.json= rather than from the key files, so a table/keys mismatch fails HERE. RECOMMEND: yes.

Yes

- [X] C3. The preimage carries the BARE circuit id, not compact-js's encoded key location; both resolvers accept both. RECOMMEND: keep bare; flip only if mpc ever feeds a preimage to a proof server that parses the encoded form only.

Keep bare

* D. Checks, not decisions — done or delegable
- [X] D1. =U8::RESPOND = "uint8"= and the MPC's schema vocabulary (§5
  q4): CHECKED 2026-09-06 — the respond schema is a JSON ABI parameter
  list (=[{"name":…,"type":…}]=, node/src/respond_bidirectional.rs's
  fixtures), decoded on the MPC side by alloy's dynamic ABI
  (=chain-ethereum/src/respond_bidirectional.rs=, =DynSolType=), which
  accepts every =uintN=. No action.
- [X] D2. (DONE 2026-09-06, merged: deny.toml + the =deny:= CI job + notes/supply-chain.org; three transitive ignores recorded with reasons, no live vulnerability.) A =cargo deny= (or =cargo audit=) CI job: the tree took its first sizeable crates.io graph (alloy, dev-only) in M38 D and CI has no supply-chain job (evm-interfaces.org §7). RECOMMEND: yes, cheap; an agent can add it.

Yes

- [X] D3. The oracle pins TYPES, not parameter NAMES (§6 q6; Aave =Borrow='s =referralCode= between =interestRateMode= and =onBehalfOf=). Names only matter for the respond schema field names, which =RETURN_FIELD= owns. No action unless a consumer files an Aave call and the record's field names matter to a reader.
  
- [X] D4. (DONE 2026-09-06, merged — M36 D; two removals, the kept list in edsl-trim.org §D is for dmd's review.) edsl-trim.org §B: a fuller history-narration sweep and the "one spelling per idea" audit of =Wire3= / =Operand= / =impl Into= forms — a judgement pass over the API surface. RECOMMEND: schedule as an M36 rung; an agent can do the sweep, you review the audit.

  Yup do that, let's trim so ideally there's generally only one "correct" way to do something.

* E. Actions only you can take
- [X] E1. (2026-09-07: #6 closed, #11 filed.) Push/close on GitHub: the M20 issue pointing at TRUST.md (M33
  D) and closing issue #6 with the workflow link (M34 C).
- [ ] E2. M27: the hand-back of the leakage inventory to the analysis
  draft's authors (milestones.org M27, the vault prong).
- [ ] E3. M27 upstream spec watch: check midnight-zkir PRs #16 / #17
  around 2026-09-18 (notes/zkir-semantics.org §11) before acting on the
  conditional items.
- [ ] E4. M39 B–D: your formal model of the anti-contention design
  (nonce-admin.org's P1–P6 are the predictions to check against
  sig-net's Compact when it lands).

* F. Still in notes/review-queue.org (review, not decision)
The standing-instruction queue there still lists M11 stages 0/1/2/4/7 and
M23 R1 as diff-unreviewed landings. Unchanged by this note.
